// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"defly-defender/ent/defender"
	"defly-defender/ent/guard"
	"defly-defender/ent/user"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// GuardCreate is the builder for creating a Guard entity.
type GuardCreate struct {
	config
	mutation *GuardMutation
	hooks    []Hook
}

// SetName sets the "name" field.
func (gc *GuardCreate) SetName(s string) *GuardCreate {
	gc.mutation.SetName(s)
	return gc
}

// SetExpiredAt sets the "expired_at" field.
func (gc *GuardCreate) SetExpiredAt(t time.Time) *GuardCreate {
	gc.mutation.SetExpiredAt(t)
	return gc
}

// SetNillableExpiredAt sets the "expired_at" field if the given value is not nil.
func (gc *GuardCreate) SetNillableExpiredAt(t *time.Time) *GuardCreate {
	if t != nil {
		gc.SetExpiredAt(*t)
	}
	return gc
}

// SetID sets the "id" field.
func (gc *GuardCreate) SetID(u uuid.UUID) *GuardCreate {
	gc.mutation.SetID(u)
	return gc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (gc *GuardCreate) SetNillableID(u *uuid.UUID) *GuardCreate {
	if u != nil {
		gc.SetID(*u)
	}
	return gc
}

// AddUserIDs adds the "users" edge to the User entity by IDs.
func (gc *GuardCreate) AddUserIDs(ids ...uuid.UUID) *GuardCreate {
	gc.mutation.AddUserIDs(ids...)
	return gc
}

// AddUsers adds the "users" edges to the User entity.
func (gc *GuardCreate) AddUsers(u ...*User) *GuardCreate {
	ids := make([]uuid.UUID, len(u))
	for i := range u {
		ids[i] = u[i].ID
	}
	return gc.AddUserIDs(ids...)
}

// AddDefenderIDs adds the "defenders" edge to the Defender entity by IDs.
func (gc *GuardCreate) AddDefenderIDs(ids ...uuid.UUID) *GuardCreate {
	gc.mutation.AddDefenderIDs(ids...)
	return gc
}

// AddDefenders adds the "defenders" edges to the Defender entity.
func (gc *GuardCreate) AddDefenders(d ...*Defender) *GuardCreate {
	ids := make([]uuid.UUID, len(d))
	for i := range d {
		ids[i] = d[i].ID
	}
	return gc.AddDefenderIDs(ids...)
}

// Mutation returns the GuardMutation object of the builder.
func (gc *GuardCreate) Mutation() *GuardMutation {
	return gc.mutation
}

// Save creates the Guard in the database.
func (gc *GuardCreate) Save(ctx context.Context) (*Guard, error) {
	gc.defaults()
	return withHooks(ctx, gc.sqlSave, gc.mutation, gc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (gc *GuardCreate) SaveX(ctx context.Context) *Guard {
	v, err := gc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (gc *GuardCreate) Exec(ctx context.Context) error {
	_, err := gc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (gc *GuardCreate) ExecX(ctx context.Context) {
	if err := gc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (gc *GuardCreate) defaults() {
	if _, ok := gc.mutation.ID(); !ok {
		v := guard.DefaultID()
		gc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (gc *GuardCreate) check() error {
	if _, ok := gc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "Guard.name"`)}
	}
	if v, ok := gc.mutation.Name(); ok {
		if err := guard.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf(`ent: validator failed for field "Guard.name": %w`, err)}
		}
	}
	return nil
}

func (gc *GuardCreate) sqlSave(ctx context.Context) (*Guard, error) {
	if err := gc.check(); err != nil {
		return nil, err
	}
	_node, _spec := gc.createSpec()
	if err := sqlgraph.CreateNode(ctx, gc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	gc.mutation.id = &_node.ID
	gc.mutation.done = true
	return _node, nil
}

func (gc *GuardCreate) createSpec() (*Guard, *sqlgraph.CreateSpec) {
	var (
		_node = &Guard{config: gc.config}
		_spec = sqlgraph.NewCreateSpec(guard.Table, sqlgraph.NewFieldSpec(guard.FieldID, field.TypeUUID))
	)
	if id, ok := gc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := gc.mutation.Name(); ok {
		_spec.SetField(guard.FieldName, field.TypeString, value)
		_node.Name = value
	}
	if value, ok := gc.mutation.ExpiredAt(); ok {
		_spec.SetField(guard.FieldExpiredAt, field.TypeTime, value)
		_node.ExpiredAt = &value
	}
	if nodes := gc.mutation.UsersIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2M,
			Inverse: true,
			Table:   guard.UsersTable,
			Columns: guard.UsersPrimaryKey,
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(user.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := gc.mutation.DefendersIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2M,
			Inverse: true,
			Table:   guard.DefendersTable,
			Columns: guard.DefendersPrimaryKey,
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(defender.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// GuardCreateBulk is the builder for creating many Guard entities in bulk.
type GuardCreateBulk struct {
	config
	err      error
	builders []*GuardCreate
}

// Save creates the Guard entities in the database.
func (gcb *GuardCreateBulk) Save(ctx context.Context) ([]*Guard, error) {
	if gcb.err != nil {
		return nil, gcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(gcb.builders))
	nodes := make([]*Guard, len(gcb.builders))
	mutators := make([]Mutator, len(gcb.builders))
	for i := range gcb.builders {
		func(i int, root context.Context) {
			builder := gcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*GuardMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, gcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, gcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, gcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (gcb *GuardCreateBulk) SaveX(ctx context.Context) []*Guard {
	v, err := gcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (gcb *GuardCreateBulk) Exec(ctx context.Context) error {
	_, err := gcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (gcb *GuardCreateBulk) ExecX(ctx context.Context) {
	if err := gcb.Exec(ctx); err != nil {
		panic(err)
	}
}
