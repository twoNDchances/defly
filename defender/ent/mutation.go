// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"defly-defender/ent/action"
	"defly-defender/ent/decision"
	"defly-defender/ent/defender"
	"defly-defender/ent/engine"
	"defly-defender/ent/group"
	"defly-defender/ent/pattern"
	"defly-defender/ent/permission"
	"defly-defender/ent/predicate"
	"defly-defender/ent/principle"
	"defly-defender/ent/report"
	"defly-defender/ent/rule"
	"defly-defender/ent/target"
	"defly-defender/ent/user"
	"defly-defender/ent/wordlist"
	"defly-defender/internal/types"
	"errors"
	"fmt"
	"sync"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

const (
	// Operation types.
	OpCreate    = ent.OpCreate
	OpDelete    = ent.OpDelete
	OpDeleteOne = ent.OpDeleteOne
	OpUpdate    = ent.OpUpdate
	OpUpdateOne = ent.OpUpdateOne

	// Node types.
	TypeAction     = "Action"
	TypeDecision   = "Decision"
	TypeDefender   = "Defender"
	TypeEngine     = "Engine"
	TypeGroup      = "Group"
	TypePattern    = "Pattern"
	TypePermission = "Permission"
	TypePrinciple  = "Principle"
	TypeReport     = "Report"
	TypeRule       = "Rule"
	TypeTarget     = "Target"
	TypeUser       = "User"
	TypeWordlist   = "Wordlist"
)

// ActionMutation represents an operation that mutates the Action nodes in the graph.
type ActionMutation struct {
	config
	op             Op
	typ            string
	id             *uuid.UUID
	name           *string
	_type          *action.Type
	configurations *map[string]interface{}
	clearedFields  map[string]struct{}
	rules          map[uuid.UUID]struct{}
	removedrules   map[uuid.UUID]struct{}
	clearedrules   bool
	reports        map[uuid.UUID]struct{}
	removedreports map[uuid.UUID]struct{}
	clearedreports bool
	done           bool
	oldValue       func(context.Context) (*Action, error)
	predicates     []predicate.Action
}

var _ ent.Mutation = (*ActionMutation)(nil)

// actionOption allows management of the mutation configuration using functional options.
type actionOption func(*ActionMutation)

// newActionMutation creates new mutation for the Action entity.
func newActionMutation(c config, op Op, opts ...actionOption) *ActionMutation {
	m := &ActionMutation{
		config:        c,
		op:            op,
		typ:           TypeAction,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withActionID sets the ID field of the mutation.
func withActionID(id uuid.UUID) actionOption {
	return func(m *ActionMutation) {
		var (
			err   error
			once  sync.Once
			value *Action
		)
		m.oldValue = func(ctx context.Context) (*Action, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Action.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withAction sets the old Action of the mutation.
func withAction(node *Action) actionOption {
	return func(m *ActionMutation) {
		m.oldValue = func(context.Context) (*Action, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m ActionMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m ActionMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of Action entities.
func (m *ActionMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *ActionMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *ActionMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Action.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetName sets the "name" field.
func (m *ActionMutation) SetName(s string) {
	m.name = &s
}

// Name returns the value of the "name" field in the mutation.
func (m *ActionMutation) Name() (r string, exists bool) {
	v := m.name
	if v == nil {
		return
	}
	return *v, true
}

// OldName returns the old "name" field's value of the Action entity.
// If the Action object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ActionMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// ResetName resets all changes to the "name" field.
func (m *ActionMutation) ResetName() {
	m.name = nil
}

// SetType sets the "type" field.
func (m *ActionMutation) SetType(a action.Type) {
	m._type = &a
}

// GetType returns the value of the "type" field in the mutation.
func (m *ActionMutation) GetType() (r action.Type, exists bool) {
	v := m._type
	if v == nil {
		return
	}
	return *v, true
}

// OldType returns the old "type" field's value of the Action entity.
// If the Action object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ActionMutation) OldType(ctx context.Context) (v action.Type, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldType is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldType requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldType: %w", err)
	}
	return oldValue.Type, nil
}

// ResetType resets all changes to the "type" field.
func (m *ActionMutation) ResetType() {
	m._type = nil
}

// SetConfigurations sets the "configurations" field.
func (m *ActionMutation) SetConfigurations(value map[string]interface{}) {
	m.configurations = &value
}

// Configurations returns the value of the "configurations" field in the mutation.
func (m *ActionMutation) Configurations() (r map[string]interface{}, exists bool) {
	v := m.configurations
	if v == nil {
		return
	}
	return *v, true
}

// OldConfigurations returns the old "configurations" field's value of the Action entity.
// If the Action object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ActionMutation) OldConfigurations(ctx context.Context) (v map[string]interface{}, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldConfigurations is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldConfigurations requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldConfigurations: %w", err)
	}
	return oldValue.Configurations, nil
}

// ClearConfigurations clears the value of the "configurations" field.
func (m *ActionMutation) ClearConfigurations() {
	m.configurations = nil
	m.clearedFields[action.FieldConfigurations] = struct{}{}
}

// ConfigurationsCleared returns if the "configurations" field was cleared in this mutation.
func (m *ActionMutation) ConfigurationsCleared() bool {
	_, ok := m.clearedFields[action.FieldConfigurations]
	return ok
}

// ResetConfigurations resets all changes to the "configurations" field.
func (m *ActionMutation) ResetConfigurations() {
	m.configurations = nil
	delete(m.clearedFields, action.FieldConfigurations)
}

// AddRuleIDs adds the "rules" edge to the Rule entity by ids.
func (m *ActionMutation) AddRuleIDs(ids ...uuid.UUID) {
	if m.rules == nil {
		m.rules = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.rules[ids[i]] = struct{}{}
	}
}

// ClearRules clears the "rules" edge to the Rule entity.
func (m *ActionMutation) ClearRules() {
	m.clearedrules = true
}

// RulesCleared reports if the "rules" edge to the Rule entity was cleared.
func (m *ActionMutation) RulesCleared() bool {
	return m.clearedrules
}

// RemoveRuleIDs removes the "rules" edge to the Rule entity by IDs.
func (m *ActionMutation) RemoveRuleIDs(ids ...uuid.UUID) {
	if m.removedrules == nil {
		m.removedrules = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.rules, ids[i])
		m.removedrules[ids[i]] = struct{}{}
	}
}

// RemovedRules returns the removed IDs of the "rules" edge to the Rule entity.
func (m *ActionMutation) RemovedRulesIDs() (ids []uuid.UUID) {
	for id := range m.removedrules {
		ids = append(ids, id)
	}
	return
}

// RulesIDs returns the "rules" edge IDs in the mutation.
func (m *ActionMutation) RulesIDs() (ids []uuid.UUID) {
	for id := range m.rules {
		ids = append(ids, id)
	}
	return
}

// ResetRules resets all changes to the "rules" edge.
func (m *ActionMutation) ResetRules() {
	m.rules = nil
	m.clearedrules = false
	m.removedrules = nil
}

// AddReportIDs adds the "reports" edge to the Report entity by ids.
func (m *ActionMutation) AddReportIDs(ids ...uuid.UUID) {
	if m.reports == nil {
		m.reports = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.reports[ids[i]] = struct{}{}
	}
}

// ClearReports clears the "reports" edge to the Report entity.
func (m *ActionMutation) ClearReports() {
	m.clearedreports = true
}

// ReportsCleared reports if the "reports" edge to the Report entity was cleared.
func (m *ActionMutation) ReportsCleared() bool {
	return m.clearedreports
}

// RemoveReportIDs removes the "reports" edge to the Report entity by IDs.
func (m *ActionMutation) RemoveReportIDs(ids ...uuid.UUID) {
	if m.removedreports == nil {
		m.removedreports = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.reports, ids[i])
		m.removedreports[ids[i]] = struct{}{}
	}
}

// RemovedReports returns the removed IDs of the "reports" edge to the Report entity.
func (m *ActionMutation) RemovedReportsIDs() (ids []uuid.UUID) {
	for id := range m.removedreports {
		ids = append(ids, id)
	}
	return
}

// ReportsIDs returns the "reports" edge IDs in the mutation.
func (m *ActionMutation) ReportsIDs() (ids []uuid.UUID) {
	for id := range m.reports {
		ids = append(ids, id)
	}
	return
}

// ResetReports resets all changes to the "reports" edge.
func (m *ActionMutation) ResetReports() {
	m.reports = nil
	m.clearedreports = false
	m.removedreports = nil
}

// Where appends a list predicates to the ActionMutation builder.
func (m *ActionMutation) Where(ps ...predicate.Action) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the ActionMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *ActionMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Action, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *ActionMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *ActionMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Action).
func (m *ActionMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *ActionMutation) Fields() []string {
	fields := make([]string, 0, 3)
	if m.name != nil {
		fields = append(fields, action.FieldName)
	}
	if m._type != nil {
		fields = append(fields, action.FieldType)
	}
	if m.configurations != nil {
		fields = append(fields, action.FieldConfigurations)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *ActionMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case action.FieldName:
		return m.Name()
	case action.FieldType:
		return m.GetType()
	case action.FieldConfigurations:
		return m.Configurations()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *ActionMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case action.FieldName:
		return m.OldName(ctx)
	case action.FieldType:
		return m.OldType(ctx)
	case action.FieldConfigurations:
		return m.OldConfigurations(ctx)
	}
	return nil, fmt.Errorf("unknown Action field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *ActionMutation) SetField(name string, value ent.Value) error {
	switch name {
	case action.FieldName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetName(v)
		return nil
	case action.FieldType:
		v, ok := value.(action.Type)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetType(v)
		return nil
	case action.FieldConfigurations:
		v, ok := value.(map[string]interface{})
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetConfigurations(v)
		return nil
	}
	return fmt.Errorf("unknown Action field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *ActionMutation) AddedFields() []string {
	return nil
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *ActionMutation) AddedField(name string) (ent.Value, bool) {
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *ActionMutation) AddField(name string, value ent.Value) error {
	switch name {
	}
	return fmt.Errorf("unknown Action numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *ActionMutation) ClearedFields() []string {
	var fields []string
	if m.FieldCleared(action.FieldConfigurations) {
		fields = append(fields, action.FieldConfigurations)
	}
	return fields
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *ActionMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *ActionMutation) ClearField(name string) error {
	switch name {
	case action.FieldConfigurations:
		m.ClearConfigurations()
		return nil
	}
	return fmt.Errorf("unknown Action nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *ActionMutation) ResetField(name string) error {
	switch name {
	case action.FieldName:
		m.ResetName()
		return nil
	case action.FieldType:
		m.ResetType()
		return nil
	case action.FieldConfigurations:
		m.ResetConfigurations()
		return nil
	}
	return fmt.Errorf("unknown Action field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *ActionMutation) AddedEdges() []string {
	edges := make([]string, 0, 2)
	if m.rules != nil {
		edges = append(edges, action.EdgeRules)
	}
	if m.reports != nil {
		edges = append(edges, action.EdgeReports)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *ActionMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case action.EdgeRules:
		ids := make([]ent.Value, 0, len(m.rules))
		for id := range m.rules {
			ids = append(ids, id)
		}
		return ids
	case action.EdgeReports:
		ids := make([]ent.Value, 0, len(m.reports))
		for id := range m.reports {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *ActionMutation) RemovedEdges() []string {
	edges := make([]string, 0, 2)
	if m.removedrules != nil {
		edges = append(edges, action.EdgeRules)
	}
	if m.removedreports != nil {
		edges = append(edges, action.EdgeReports)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *ActionMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case action.EdgeRules:
		ids := make([]ent.Value, 0, len(m.removedrules))
		for id := range m.removedrules {
			ids = append(ids, id)
		}
		return ids
	case action.EdgeReports:
		ids := make([]ent.Value, 0, len(m.removedreports))
		for id := range m.removedreports {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *ActionMutation) ClearedEdges() []string {
	edges := make([]string, 0, 2)
	if m.clearedrules {
		edges = append(edges, action.EdgeRules)
	}
	if m.clearedreports {
		edges = append(edges, action.EdgeReports)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *ActionMutation) EdgeCleared(name string) bool {
	switch name {
	case action.EdgeRules:
		return m.clearedrules
	case action.EdgeReports:
		return m.clearedreports
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *ActionMutation) ClearEdge(name string) error {
	switch name {
	}
	return fmt.Errorf("unknown Action unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *ActionMutation) ResetEdge(name string) error {
	switch name {
	case action.EdgeRules:
		m.ResetRules()
		return nil
	case action.EdgeReports:
		m.ResetReports()
		return nil
	}
	return fmt.Errorf("unknown Action edge %s", name)
}

// DecisionMutation represents an operation that mutates the Decision nodes in the graph.
type DecisionMutation struct {
	config
	op               Op
	typ              string
	id               *uuid.UUID
	name             *string
	direction        *decision.Direction
	condition        *decision.Condition
	score            *float64
	addscore         *float64
	action           *decision.Action
	configurations   *map[string]interface{}
	clearedFields    map[string]struct{}
	defenders        map[uuid.UUID]struct{}
	removeddefenders map[uuid.UUID]struct{}
	cleareddefenders bool
	done             bool
	oldValue         func(context.Context) (*Decision, error)
	predicates       []predicate.Decision
}

var _ ent.Mutation = (*DecisionMutation)(nil)

// decisionOption allows management of the mutation configuration using functional options.
type decisionOption func(*DecisionMutation)

// newDecisionMutation creates new mutation for the Decision entity.
func newDecisionMutation(c config, op Op, opts ...decisionOption) *DecisionMutation {
	m := &DecisionMutation{
		config:        c,
		op:            op,
		typ:           TypeDecision,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withDecisionID sets the ID field of the mutation.
func withDecisionID(id uuid.UUID) decisionOption {
	return func(m *DecisionMutation) {
		var (
			err   error
			once  sync.Once
			value *Decision
		)
		m.oldValue = func(ctx context.Context) (*Decision, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Decision.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withDecision sets the old Decision of the mutation.
func withDecision(node *Decision) decisionOption {
	return func(m *DecisionMutation) {
		m.oldValue = func(context.Context) (*Decision, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m DecisionMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m DecisionMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of Decision entities.
func (m *DecisionMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *DecisionMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *DecisionMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Decision.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetName sets the "name" field.
func (m *DecisionMutation) SetName(s string) {
	m.name = &s
}

// Name returns the value of the "name" field in the mutation.
func (m *DecisionMutation) Name() (r string, exists bool) {
	v := m.name
	if v == nil {
		return
	}
	return *v, true
}

// OldName returns the old "name" field's value of the Decision entity.
// If the Decision object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *DecisionMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// ResetName resets all changes to the "name" field.
func (m *DecisionMutation) ResetName() {
	m.name = nil
}

// SetDirection sets the "direction" field.
func (m *DecisionMutation) SetDirection(d decision.Direction) {
	m.direction = &d
}

// Direction returns the value of the "direction" field in the mutation.
func (m *DecisionMutation) Direction() (r decision.Direction, exists bool) {
	v := m.direction
	if v == nil {
		return
	}
	return *v, true
}

// OldDirection returns the old "direction" field's value of the Decision entity.
// If the Decision object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *DecisionMutation) OldDirection(ctx context.Context) (v decision.Direction, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldDirection is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldDirection requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDirection: %w", err)
	}
	return oldValue.Direction, nil
}

// ResetDirection resets all changes to the "direction" field.
func (m *DecisionMutation) ResetDirection() {
	m.direction = nil
}

// SetCondition sets the "condition" field.
func (m *DecisionMutation) SetCondition(d decision.Condition) {
	m.condition = &d
}

// Condition returns the value of the "condition" field in the mutation.
func (m *DecisionMutation) Condition() (r decision.Condition, exists bool) {
	v := m.condition
	if v == nil {
		return
	}
	return *v, true
}

// OldCondition returns the old "condition" field's value of the Decision entity.
// If the Decision object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *DecisionMutation) OldCondition(ctx context.Context) (v decision.Condition, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldCondition is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldCondition requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldCondition: %w", err)
	}
	return oldValue.Condition, nil
}

// ResetCondition resets all changes to the "condition" field.
func (m *DecisionMutation) ResetCondition() {
	m.condition = nil
}

// SetScore sets the "score" field.
func (m *DecisionMutation) SetScore(f float64) {
	m.score = &f
	m.addscore = nil
}

// Score returns the value of the "score" field in the mutation.
func (m *DecisionMutation) Score() (r float64, exists bool) {
	v := m.score
	if v == nil {
		return
	}
	return *v, true
}

// OldScore returns the old "score" field's value of the Decision entity.
// If the Decision object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *DecisionMutation) OldScore(ctx context.Context) (v float64, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldScore is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldScore requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldScore: %w", err)
	}
	return oldValue.Score, nil
}

// AddScore adds f to the "score" field.
func (m *DecisionMutation) AddScore(f float64) {
	if m.addscore != nil {
		*m.addscore += f
	} else {
		m.addscore = &f
	}
}

// AddedScore returns the value that was added to the "score" field in this mutation.
func (m *DecisionMutation) AddedScore() (r float64, exists bool) {
	v := m.addscore
	if v == nil {
		return
	}
	return *v, true
}

// ResetScore resets all changes to the "score" field.
func (m *DecisionMutation) ResetScore() {
	m.score = nil
	m.addscore = nil
}

// SetAction sets the "action" field.
func (m *DecisionMutation) SetAction(d decision.Action) {
	m.action = &d
}

// Action returns the value of the "action" field in the mutation.
func (m *DecisionMutation) Action() (r decision.Action, exists bool) {
	v := m.action
	if v == nil {
		return
	}
	return *v, true
}

// OldAction returns the old "action" field's value of the Decision entity.
// If the Decision object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *DecisionMutation) OldAction(ctx context.Context) (v decision.Action, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldAction is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldAction requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldAction: %w", err)
	}
	return oldValue.Action, nil
}

// ResetAction resets all changes to the "action" field.
func (m *DecisionMutation) ResetAction() {
	m.action = nil
}

// SetConfigurations sets the "configurations" field.
func (m *DecisionMutation) SetConfigurations(value map[string]interface{}) {
	m.configurations = &value
}

// Configurations returns the value of the "configurations" field in the mutation.
func (m *DecisionMutation) Configurations() (r map[string]interface{}, exists bool) {
	v := m.configurations
	if v == nil {
		return
	}
	return *v, true
}

// OldConfigurations returns the old "configurations" field's value of the Decision entity.
// If the Decision object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *DecisionMutation) OldConfigurations(ctx context.Context) (v map[string]interface{}, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldConfigurations is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldConfigurations requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldConfigurations: %w", err)
	}
	return oldValue.Configurations, nil
}

// ClearConfigurations clears the value of the "configurations" field.
func (m *DecisionMutation) ClearConfigurations() {
	m.configurations = nil
	m.clearedFields[decision.FieldConfigurations] = struct{}{}
}

// ConfigurationsCleared returns if the "configurations" field was cleared in this mutation.
func (m *DecisionMutation) ConfigurationsCleared() bool {
	_, ok := m.clearedFields[decision.FieldConfigurations]
	return ok
}

// ResetConfigurations resets all changes to the "configurations" field.
func (m *DecisionMutation) ResetConfigurations() {
	m.configurations = nil
	delete(m.clearedFields, decision.FieldConfigurations)
}

// AddDefenderIDs adds the "defenders" edge to the Defender entity by ids.
func (m *DecisionMutation) AddDefenderIDs(ids ...uuid.UUID) {
	if m.defenders == nil {
		m.defenders = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.defenders[ids[i]] = struct{}{}
	}
}

// ClearDefenders clears the "defenders" edge to the Defender entity.
func (m *DecisionMutation) ClearDefenders() {
	m.cleareddefenders = true
}

// DefendersCleared reports if the "defenders" edge to the Defender entity was cleared.
func (m *DecisionMutation) DefendersCleared() bool {
	return m.cleareddefenders
}

// RemoveDefenderIDs removes the "defenders" edge to the Defender entity by IDs.
func (m *DecisionMutation) RemoveDefenderIDs(ids ...uuid.UUID) {
	if m.removeddefenders == nil {
		m.removeddefenders = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.defenders, ids[i])
		m.removeddefenders[ids[i]] = struct{}{}
	}
}

// RemovedDefenders returns the removed IDs of the "defenders" edge to the Defender entity.
func (m *DecisionMutation) RemovedDefendersIDs() (ids []uuid.UUID) {
	for id := range m.removeddefenders {
		ids = append(ids, id)
	}
	return
}

// DefendersIDs returns the "defenders" edge IDs in the mutation.
func (m *DecisionMutation) DefendersIDs() (ids []uuid.UUID) {
	for id := range m.defenders {
		ids = append(ids, id)
	}
	return
}

// ResetDefenders resets all changes to the "defenders" edge.
func (m *DecisionMutation) ResetDefenders() {
	m.defenders = nil
	m.cleareddefenders = false
	m.removeddefenders = nil
}

// Where appends a list predicates to the DecisionMutation builder.
func (m *DecisionMutation) Where(ps ...predicate.Decision) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the DecisionMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *DecisionMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Decision, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *DecisionMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *DecisionMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Decision).
func (m *DecisionMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *DecisionMutation) Fields() []string {
	fields := make([]string, 0, 6)
	if m.name != nil {
		fields = append(fields, decision.FieldName)
	}
	if m.direction != nil {
		fields = append(fields, decision.FieldDirection)
	}
	if m.condition != nil {
		fields = append(fields, decision.FieldCondition)
	}
	if m.score != nil {
		fields = append(fields, decision.FieldScore)
	}
	if m.action != nil {
		fields = append(fields, decision.FieldAction)
	}
	if m.configurations != nil {
		fields = append(fields, decision.FieldConfigurations)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *DecisionMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case decision.FieldName:
		return m.Name()
	case decision.FieldDirection:
		return m.Direction()
	case decision.FieldCondition:
		return m.Condition()
	case decision.FieldScore:
		return m.Score()
	case decision.FieldAction:
		return m.Action()
	case decision.FieldConfigurations:
		return m.Configurations()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *DecisionMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case decision.FieldName:
		return m.OldName(ctx)
	case decision.FieldDirection:
		return m.OldDirection(ctx)
	case decision.FieldCondition:
		return m.OldCondition(ctx)
	case decision.FieldScore:
		return m.OldScore(ctx)
	case decision.FieldAction:
		return m.OldAction(ctx)
	case decision.FieldConfigurations:
		return m.OldConfigurations(ctx)
	}
	return nil, fmt.Errorf("unknown Decision field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *DecisionMutation) SetField(name string, value ent.Value) error {
	switch name {
	case decision.FieldName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetName(v)
		return nil
	case decision.FieldDirection:
		v, ok := value.(decision.Direction)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetDirection(v)
		return nil
	case decision.FieldCondition:
		v, ok := value.(decision.Condition)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetCondition(v)
		return nil
	case decision.FieldScore:
		v, ok := value.(float64)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetScore(v)
		return nil
	case decision.FieldAction:
		v, ok := value.(decision.Action)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetAction(v)
		return nil
	case decision.FieldConfigurations:
		v, ok := value.(map[string]interface{})
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetConfigurations(v)
		return nil
	}
	return fmt.Errorf("unknown Decision field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *DecisionMutation) AddedFields() []string {
	var fields []string
	if m.addscore != nil {
		fields = append(fields, decision.FieldScore)
	}
	return fields
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *DecisionMutation) AddedField(name string) (ent.Value, bool) {
	switch name {
	case decision.FieldScore:
		return m.AddedScore()
	}
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *DecisionMutation) AddField(name string, value ent.Value) error {
	switch name {
	case decision.FieldScore:
		v, ok := value.(float64)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.AddScore(v)
		return nil
	}
	return fmt.Errorf("unknown Decision numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *DecisionMutation) ClearedFields() []string {
	var fields []string
	if m.FieldCleared(decision.FieldConfigurations) {
		fields = append(fields, decision.FieldConfigurations)
	}
	return fields
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *DecisionMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *DecisionMutation) ClearField(name string) error {
	switch name {
	case decision.FieldConfigurations:
		m.ClearConfigurations()
		return nil
	}
	return fmt.Errorf("unknown Decision nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *DecisionMutation) ResetField(name string) error {
	switch name {
	case decision.FieldName:
		m.ResetName()
		return nil
	case decision.FieldDirection:
		m.ResetDirection()
		return nil
	case decision.FieldCondition:
		m.ResetCondition()
		return nil
	case decision.FieldScore:
		m.ResetScore()
		return nil
	case decision.FieldAction:
		m.ResetAction()
		return nil
	case decision.FieldConfigurations:
		m.ResetConfigurations()
		return nil
	}
	return fmt.Errorf("unknown Decision field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *DecisionMutation) AddedEdges() []string {
	edges := make([]string, 0, 1)
	if m.defenders != nil {
		edges = append(edges, decision.EdgeDefenders)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *DecisionMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case decision.EdgeDefenders:
		ids := make([]ent.Value, 0, len(m.defenders))
		for id := range m.defenders {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *DecisionMutation) RemovedEdges() []string {
	edges := make([]string, 0, 1)
	if m.removeddefenders != nil {
		edges = append(edges, decision.EdgeDefenders)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *DecisionMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case decision.EdgeDefenders:
		ids := make([]ent.Value, 0, len(m.removeddefenders))
		for id := range m.removeddefenders {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *DecisionMutation) ClearedEdges() []string {
	edges := make([]string, 0, 1)
	if m.cleareddefenders {
		edges = append(edges, decision.EdgeDefenders)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *DecisionMutation) EdgeCleared(name string) bool {
	switch name {
	case decision.EdgeDefenders:
		return m.cleareddefenders
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *DecisionMutation) ClearEdge(name string) error {
	switch name {
	}
	return fmt.Errorf("unknown Decision unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *DecisionMutation) ResetEdge(name string) error {
	switch name {
	case decision.EdgeDefenders:
		m.ResetDefenders()
		return nil
	}
	return fmt.Errorf("unknown Decision edge %s", name)
}

// DefenderMutation represents an operation that mutates the Defender nodes in the graph.
type DefenderMutation struct {
	config
	op                Op
	typ               string
	id                *uuid.UUID
	name              *string
	status            *defender.Status
	details           *map[string]interface{}
	clearedFields     map[string]struct{}
	principles        map[uuid.UUID]struct{}
	removedprinciples map[uuid.UUID]struct{}
	clearedprinciples bool
	decisions         map[uuid.UUID]struct{}
	removeddecisions  map[uuid.UUID]struct{}
	cleareddecisions  bool
	reports           map[uuid.UUID]struct{}
	removedreports    map[uuid.UUID]struct{}
	clearedreports    bool
	done              bool
	oldValue          func(context.Context) (*Defender, error)
	predicates        []predicate.Defender
}

var _ ent.Mutation = (*DefenderMutation)(nil)

// defenderOption allows management of the mutation configuration using functional options.
type defenderOption func(*DefenderMutation)

// newDefenderMutation creates new mutation for the Defender entity.
func newDefenderMutation(c config, op Op, opts ...defenderOption) *DefenderMutation {
	m := &DefenderMutation{
		config:        c,
		op:            op,
		typ:           TypeDefender,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withDefenderID sets the ID field of the mutation.
func withDefenderID(id uuid.UUID) defenderOption {
	return func(m *DefenderMutation) {
		var (
			err   error
			once  sync.Once
			value *Defender
		)
		m.oldValue = func(ctx context.Context) (*Defender, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Defender.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withDefender sets the old Defender of the mutation.
func withDefender(node *Defender) defenderOption {
	return func(m *DefenderMutation) {
		m.oldValue = func(context.Context) (*Defender, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m DefenderMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m DefenderMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of Defender entities.
func (m *DefenderMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *DefenderMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *DefenderMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Defender.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetName sets the "name" field.
func (m *DefenderMutation) SetName(s string) {
	m.name = &s
}

// Name returns the value of the "name" field in the mutation.
func (m *DefenderMutation) Name() (r string, exists bool) {
	v := m.name
	if v == nil {
		return
	}
	return *v, true
}

// OldName returns the old "name" field's value of the Defender entity.
// If the Defender object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *DefenderMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// ResetName resets all changes to the "name" field.
func (m *DefenderMutation) ResetName() {
	m.name = nil
}

// SetStatus sets the "status" field.
func (m *DefenderMutation) SetStatus(d defender.Status) {
	m.status = &d
}

// Status returns the value of the "status" field in the mutation.
func (m *DefenderMutation) Status() (r defender.Status, exists bool) {
	v := m.status
	if v == nil {
		return
	}
	return *v, true
}

// OldStatus returns the old "status" field's value of the Defender entity.
// If the Defender object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *DefenderMutation) OldStatus(ctx context.Context) (v *defender.Status, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldStatus is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldStatus requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldStatus: %w", err)
	}
	return oldValue.Status, nil
}

// ClearStatus clears the value of the "status" field.
func (m *DefenderMutation) ClearStatus() {
	m.status = nil
	m.clearedFields[defender.FieldStatus] = struct{}{}
}

// StatusCleared returns if the "status" field was cleared in this mutation.
func (m *DefenderMutation) StatusCleared() bool {
	_, ok := m.clearedFields[defender.FieldStatus]
	return ok
}

// ResetStatus resets all changes to the "status" field.
func (m *DefenderMutation) ResetStatus() {
	m.status = nil
	delete(m.clearedFields, defender.FieldStatus)
}

// SetDetails sets the "details" field.
func (m *DefenderMutation) SetDetails(value map[string]interface{}) {
	m.details = &value
}

// Details returns the value of the "details" field in the mutation.
func (m *DefenderMutation) Details() (r map[string]interface{}, exists bool) {
	v := m.details
	if v == nil {
		return
	}
	return *v, true
}

// OldDetails returns the old "details" field's value of the Defender entity.
// If the Defender object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *DefenderMutation) OldDetails(ctx context.Context) (v map[string]interface{}, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldDetails is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldDetails requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDetails: %w", err)
	}
	return oldValue.Details, nil
}

// ClearDetails clears the value of the "details" field.
func (m *DefenderMutation) ClearDetails() {
	m.details = nil
	m.clearedFields[defender.FieldDetails] = struct{}{}
}

// DetailsCleared returns if the "details" field was cleared in this mutation.
func (m *DefenderMutation) DetailsCleared() bool {
	_, ok := m.clearedFields[defender.FieldDetails]
	return ok
}

// ResetDetails resets all changes to the "details" field.
func (m *DefenderMutation) ResetDetails() {
	m.details = nil
	delete(m.clearedFields, defender.FieldDetails)
}

// AddPrincipleIDs adds the "principles" edge to the Principle entity by ids.
func (m *DefenderMutation) AddPrincipleIDs(ids ...uuid.UUID) {
	if m.principles == nil {
		m.principles = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.principles[ids[i]] = struct{}{}
	}
}

// ClearPrinciples clears the "principles" edge to the Principle entity.
func (m *DefenderMutation) ClearPrinciples() {
	m.clearedprinciples = true
}

// PrinciplesCleared reports if the "principles" edge to the Principle entity was cleared.
func (m *DefenderMutation) PrinciplesCleared() bool {
	return m.clearedprinciples
}

// RemovePrincipleIDs removes the "principles" edge to the Principle entity by IDs.
func (m *DefenderMutation) RemovePrincipleIDs(ids ...uuid.UUID) {
	if m.removedprinciples == nil {
		m.removedprinciples = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.principles, ids[i])
		m.removedprinciples[ids[i]] = struct{}{}
	}
}

// RemovedPrinciples returns the removed IDs of the "principles" edge to the Principle entity.
func (m *DefenderMutation) RemovedPrinciplesIDs() (ids []uuid.UUID) {
	for id := range m.removedprinciples {
		ids = append(ids, id)
	}
	return
}

// PrinciplesIDs returns the "principles" edge IDs in the mutation.
func (m *DefenderMutation) PrinciplesIDs() (ids []uuid.UUID) {
	for id := range m.principles {
		ids = append(ids, id)
	}
	return
}

// ResetPrinciples resets all changes to the "principles" edge.
func (m *DefenderMutation) ResetPrinciples() {
	m.principles = nil
	m.clearedprinciples = false
	m.removedprinciples = nil
}

// AddDecisionIDs adds the "decisions" edge to the Decision entity by ids.
func (m *DefenderMutation) AddDecisionIDs(ids ...uuid.UUID) {
	if m.decisions == nil {
		m.decisions = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.decisions[ids[i]] = struct{}{}
	}
}

// ClearDecisions clears the "decisions" edge to the Decision entity.
func (m *DefenderMutation) ClearDecisions() {
	m.cleareddecisions = true
}

// DecisionsCleared reports if the "decisions" edge to the Decision entity was cleared.
func (m *DefenderMutation) DecisionsCleared() bool {
	return m.cleareddecisions
}

// RemoveDecisionIDs removes the "decisions" edge to the Decision entity by IDs.
func (m *DefenderMutation) RemoveDecisionIDs(ids ...uuid.UUID) {
	if m.removeddecisions == nil {
		m.removeddecisions = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.decisions, ids[i])
		m.removeddecisions[ids[i]] = struct{}{}
	}
}

// RemovedDecisions returns the removed IDs of the "decisions" edge to the Decision entity.
func (m *DefenderMutation) RemovedDecisionsIDs() (ids []uuid.UUID) {
	for id := range m.removeddecisions {
		ids = append(ids, id)
	}
	return
}

// DecisionsIDs returns the "decisions" edge IDs in the mutation.
func (m *DefenderMutation) DecisionsIDs() (ids []uuid.UUID) {
	for id := range m.decisions {
		ids = append(ids, id)
	}
	return
}

// ResetDecisions resets all changes to the "decisions" edge.
func (m *DefenderMutation) ResetDecisions() {
	m.decisions = nil
	m.cleareddecisions = false
	m.removeddecisions = nil
}

// AddReportIDs adds the "reports" edge to the Report entity by ids.
func (m *DefenderMutation) AddReportIDs(ids ...uuid.UUID) {
	if m.reports == nil {
		m.reports = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.reports[ids[i]] = struct{}{}
	}
}

// ClearReports clears the "reports" edge to the Report entity.
func (m *DefenderMutation) ClearReports() {
	m.clearedreports = true
}

// ReportsCleared reports if the "reports" edge to the Report entity was cleared.
func (m *DefenderMutation) ReportsCleared() bool {
	return m.clearedreports
}

// RemoveReportIDs removes the "reports" edge to the Report entity by IDs.
func (m *DefenderMutation) RemoveReportIDs(ids ...uuid.UUID) {
	if m.removedreports == nil {
		m.removedreports = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.reports, ids[i])
		m.removedreports[ids[i]] = struct{}{}
	}
}

// RemovedReports returns the removed IDs of the "reports" edge to the Report entity.
func (m *DefenderMutation) RemovedReportsIDs() (ids []uuid.UUID) {
	for id := range m.removedreports {
		ids = append(ids, id)
	}
	return
}

// ReportsIDs returns the "reports" edge IDs in the mutation.
func (m *DefenderMutation) ReportsIDs() (ids []uuid.UUID) {
	for id := range m.reports {
		ids = append(ids, id)
	}
	return
}

// ResetReports resets all changes to the "reports" edge.
func (m *DefenderMutation) ResetReports() {
	m.reports = nil
	m.clearedreports = false
	m.removedreports = nil
}

// Where appends a list predicates to the DefenderMutation builder.
func (m *DefenderMutation) Where(ps ...predicate.Defender) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the DefenderMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *DefenderMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Defender, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *DefenderMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *DefenderMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Defender).
func (m *DefenderMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *DefenderMutation) Fields() []string {
	fields := make([]string, 0, 3)
	if m.name != nil {
		fields = append(fields, defender.FieldName)
	}
	if m.status != nil {
		fields = append(fields, defender.FieldStatus)
	}
	if m.details != nil {
		fields = append(fields, defender.FieldDetails)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *DefenderMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case defender.FieldName:
		return m.Name()
	case defender.FieldStatus:
		return m.Status()
	case defender.FieldDetails:
		return m.Details()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *DefenderMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case defender.FieldName:
		return m.OldName(ctx)
	case defender.FieldStatus:
		return m.OldStatus(ctx)
	case defender.FieldDetails:
		return m.OldDetails(ctx)
	}
	return nil, fmt.Errorf("unknown Defender field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *DefenderMutation) SetField(name string, value ent.Value) error {
	switch name {
	case defender.FieldName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetName(v)
		return nil
	case defender.FieldStatus:
		v, ok := value.(defender.Status)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetStatus(v)
		return nil
	case defender.FieldDetails:
		v, ok := value.(map[string]interface{})
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetDetails(v)
		return nil
	}
	return fmt.Errorf("unknown Defender field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *DefenderMutation) AddedFields() []string {
	return nil
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *DefenderMutation) AddedField(name string) (ent.Value, bool) {
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *DefenderMutation) AddField(name string, value ent.Value) error {
	switch name {
	}
	return fmt.Errorf("unknown Defender numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *DefenderMutation) ClearedFields() []string {
	var fields []string
	if m.FieldCleared(defender.FieldStatus) {
		fields = append(fields, defender.FieldStatus)
	}
	if m.FieldCleared(defender.FieldDetails) {
		fields = append(fields, defender.FieldDetails)
	}
	return fields
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *DefenderMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *DefenderMutation) ClearField(name string) error {
	switch name {
	case defender.FieldStatus:
		m.ClearStatus()
		return nil
	case defender.FieldDetails:
		m.ClearDetails()
		return nil
	}
	return fmt.Errorf("unknown Defender nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *DefenderMutation) ResetField(name string) error {
	switch name {
	case defender.FieldName:
		m.ResetName()
		return nil
	case defender.FieldStatus:
		m.ResetStatus()
		return nil
	case defender.FieldDetails:
		m.ResetDetails()
		return nil
	}
	return fmt.Errorf("unknown Defender field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *DefenderMutation) AddedEdges() []string {
	edges := make([]string, 0, 3)
	if m.principles != nil {
		edges = append(edges, defender.EdgePrinciples)
	}
	if m.decisions != nil {
		edges = append(edges, defender.EdgeDecisions)
	}
	if m.reports != nil {
		edges = append(edges, defender.EdgeReports)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *DefenderMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case defender.EdgePrinciples:
		ids := make([]ent.Value, 0, len(m.principles))
		for id := range m.principles {
			ids = append(ids, id)
		}
		return ids
	case defender.EdgeDecisions:
		ids := make([]ent.Value, 0, len(m.decisions))
		for id := range m.decisions {
			ids = append(ids, id)
		}
		return ids
	case defender.EdgeReports:
		ids := make([]ent.Value, 0, len(m.reports))
		for id := range m.reports {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *DefenderMutation) RemovedEdges() []string {
	edges := make([]string, 0, 3)
	if m.removedprinciples != nil {
		edges = append(edges, defender.EdgePrinciples)
	}
	if m.removeddecisions != nil {
		edges = append(edges, defender.EdgeDecisions)
	}
	if m.removedreports != nil {
		edges = append(edges, defender.EdgeReports)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *DefenderMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case defender.EdgePrinciples:
		ids := make([]ent.Value, 0, len(m.removedprinciples))
		for id := range m.removedprinciples {
			ids = append(ids, id)
		}
		return ids
	case defender.EdgeDecisions:
		ids := make([]ent.Value, 0, len(m.removeddecisions))
		for id := range m.removeddecisions {
			ids = append(ids, id)
		}
		return ids
	case defender.EdgeReports:
		ids := make([]ent.Value, 0, len(m.removedreports))
		for id := range m.removedreports {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *DefenderMutation) ClearedEdges() []string {
	edges := make([]string, 0, 3)
	if m.clearedprinciples {
		edges = append(edges, defender.EdgePrinciples)
	}
	if m.cleareddecisions {
		edges = append(edges, defender.EdgeDecisions)
	}
	if m.clearedreports {
		edges = append(edges, defender.EdgeReports)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *DefenderMutation) EdgeCleared(name string) bool {
	switch name {
	case defender.EdgePrinciples:
		return m.clearedprinciples
	case defender.EdgeDecisions:
		return m.cleareddecisions
	case defender.EdgeReports:
		return m.clearedreports
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *DefenderMutation) ClearEdge(name string) error {
	switch name {
	}
	return fmt.Errorf("unknown Defender unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *DefenderMutation) ResetEdge(name string) error {
	switch name {
	case defender.EdgePrinciples:
		m.ResetPrinciples()
		return nil
	case defender.EdgeDecisions:
		m.ResetDecisions()
		return nil
	case defender.EdgeReports:
		m.ResetReports()
		return nil
	}
	return fmt.Errorf("unknown Defender edge %s", name)
}

// EngineMutation represents an operation that mutates the Engine nodes in the graph.
type EngineMutation struct {
	config
	op              Op
	typ             string
	id              *uuid.UUID
	name            *string
	input_datatype  *engine.InputDatatype
	_type           *string
	configurations  *map[string]interface{}
	output_datatype *engine.OutputDatatype
	clearedFields   map[string]struct{}
	targets         map[uuid.UUID]struct{}
	removedtargets  map[uuid.UUID]struct{}
	clearedtargets  bool
	done            bool
	oldValue        func(context.Context) (*Engine, error)
	predicates      []predicate.Engine
}

var _ ent.Mutation = (*EngineMutation)(nil)

// engineOption allows management of the mutation configuration using functional options.
type engineOption func(*EngineMutation)

// newEngineMutation creates new mutation for the Engine entity.
func newEngineMutation(c config, op Op, opts ...engineOption) *EngineMutation {
	m := &EngineMutation{
		config:        c,
		op:            op,
		typ:           TypeEngine,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withEngineID sets the ID field of the mutation.
func withEngineID(id uuid.UUID) engineOption {
	return func(m *EngineMutation) {
		var (
			err   error
			once  sync.Once
			value *Engine
		)
		m.oldValue = func(ctx context.Context) (*Engine, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Engine.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withEngine sets the old Engine of the mutation.
func withEngine(node *Engine) engineOption {
	return func(m *EngineMutation) {
		m.oldValue = func(context.Context) (*Engine, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m EngineMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m EngineMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of Engine entities.
func (m *EngineMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *EngineMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *EngineMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Engine.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetName sets the "name" field.
func (m *EngineMutation) SetName(s string) {
	m.name = &s
}

// Name returns the value of the "name" field in the mutation.
func (m *EngineMutation) Name() (r string, exists bool) {
	v := m.name
	if v == nil {
		return
	}
	return *v, true
}

// OldName returns the old "name" field's value of the Engine entity.
// If the Engine object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *EngineMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// ResetName resets all changes to the "name" field.
func (m *EngineMutation) ResetName() {
	m.name = nil
}

// SetInputDatatype sets the "input_datatype" field.
func (m *EngineMutation) SetInputDatatype(ed engine.InputDatatype) {
	m.input_datatype = &ed
}

// InputDatatype returns the value of the "input_datatype" field in the mutation.
func (m *EngineMutation) InputDatatype() (r engine.InputDatatype, exists bool) {
	v := m.input_datatype
	if v == nil {
		return
	}
	return *v, true
}

// OldInputDatatype returns the old "input_datatype" field's value of the Engine entity.
// If the Engine object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *EngineMutation) OldInputDatatype(ctx context.Context) (v engine.InputDatatype, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldInputDatatype is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldInputDatatype requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldInputDatatype: %w", err)
	}
	return oldValue.InputDatatype, nil
}

// ResetInputDatatype resets all changes to the "input_datatype" field.
func (m *EngineMutation) ResetInputDatatype() {
	m.input_datatype = nil
}

// SetType sets the "type" field.
func (m *EngineMutation) SetType(s string) {
	m._type = &s
}

// GetType returns the value of the "type" field in the mutation.
func (m *EngineMutation) GetType() (r string, exists bool) {
	v := m._type
	if v == nil {
		return
	}
	return *v, true
}

// OldType returns the old "type" field's value of the Engine entity.
// If the Engine object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *EngineMutation) OldType(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldType is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldType requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldType: %w", err)
	}
	return oldValue.Type, nil
}

// ResetType resets all changes to the "type" field.
func (m *EngineMutation) ResetType() {
	m._type = nil
}

// SetConfigurations sets the "configurations" field.
func (m *EngineMutation) SetConfigurations(value map[string]interface{}) {
	m.configurations = &value
}

// Configurations returns the value of the "configurations" field in the mutation.
func (m *EngineMutation) Configurations() (r map[string]interface{}, exists bool) {
	v := m.configurations
	if v == nil {
		return
	}
	return *v, true
}

// OldConfigurations returns the old "configurations" field's value of the Engine entity.
// If the Engine object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *EngineMutation) OldConfigurations(ctx context.Context) (v map[string]interface{}, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldConfigurations is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldConfigurations requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldConfigurations: %w", err)
	}
	return oldValue.Configurations, nil
}

// ClearConfigurations clears the value of the "configurations" field.
func (m *EngineMutation) ClearConfigurations() {
	m.configurations = nil
	m.clearedFields[engine.FieldConfigurations] = struct{}{}
}

// ConfigurationsCleared returns if the "configurations" field was cleared in this mutation.
func (m *EngineMutation) ConfigurationsCleared() bool {
	_, ok := m.clearedFields[engine.FieldConfigurations]
	return ok
}

// ResetConfigurations resets all changes to the "configurations" field.
func (m *EngineMutation) ResetConfigurations() {
	m.configurations = nil
	delete(m.clearedFields, engine.FieldConfigurations)
}

// SetOutputDatatype sets the "output_datatype" field.
func (m *EngineMutation) SetOutputDatatype(ed engine.OutputDatatype) {
	m.output_datatype = &ed
}

// OutputDatatype returns the value of the "output_datatype" field in the mutation.
func (m *EngineMutation) OutputDatatype() (r engine.OutputDatatype, exists bool) {
	v := m.output_datatype
	if v == nil {
		return
	}
	return *v, true
}

// OldOutputDatatype returns the old "output_datatype" field's value of the Engine entity.
// If the Engine object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *EngineMutation) OldOutputDatatype(ctx context.Context) (v engine.OutputDatatype, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldOutputDatatype is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldOutputDatatype requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldOutputDatatype: %w", err)
	}
	return oldValue.OutputDatatype, nil
}

// ResetOutputDatatype resets all changes to the "output_datatype" field.
func (m *EngineMutation) ResetOutputDatatype() {
	m.output_datatype = nil
}

// AddTargetIDs adds the "targets" edge to the Target entity by ids.
func (m *EngineMutation) AddTargetIDs(ids ...uuid.UUID) {
	if m.targets == nil {
		m.targets = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.targets[ids[i]] = struct{}{}
	}
}

// ClearTargets clears the "targets" edge to the Target entity.
func (m *EngineMutation) ClearTargets() {
	m.clearedtargets = true
}

// TargetsCleared reports if the "targets" edge to the Target entity was cleared.
func (m *EngineMutation) TargetsCleared() bool {
	return m.clearedtargets
}

// RemoveTargetIDs removes the "targets" edge to the Target entity by IDs.
func (m *EngineMutation) RemoveTargetIDs(ids ...uuid.UUID) {
	if m.removedtargets == nil {
		m.removedtargets = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.targets, ids[i])
		m.removedtargets[ids[i]] = struct{}{}
	}
}

// RemovedTargets returns the removed IDs of the "targets" edge to the Target entity.
func (m *EngineMutation) RemovedTargetsIDs() (ids []uuid.UUID) {
	for id := range m.removedtargets {
		ids = append(ids, id)
	}
	return
}

// TargetsIDs returns the "targets" edge IDs in the mutation.
func (m *EngineMutation) TargetsIDs() (ids []uuid.UUID) {
	for id := range m.targets {
		ids = append(ids, id)
	}
	return
}

// ResetTargets resets all changes to the "targets" edge.
func (m *EngineMutation) ResetTargets() {
	m.targets = nil
	m.clearedtargets = false
	m.removedtargets = nil
}

// Where appends a list predicates to the EngineMutation builder.
func (m *EngineMutation) Where(ps ...predicate.Engine) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the EngineMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *EngineMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Engine, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *EngineMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *EngineMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Engine).
func (m *EngineMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *EngineMutation) Fields() []string {
	fields := make([]string, 0, 5)
	if m.name != nil {
		fields = append(fields, engine.FieldName)
	}
	if m.input_datatype != nil {
		fields = append(fields, engine.FieldInputDatatype)
	}
	if m._type != nil {
		fields = append(fields, engine.FieldType)
	}
	if m.configurations != nil {
		fields = append(fields, engine.FieldConfigurations)
	}
	if m.output_datatype != nil {
		fields = append(fields, engine.FieldOutputDatatype)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *EngineMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case engine.FieldName:
		return m.Name()
	case engine.FieldInputDatatype:
		return m.InputDatatype()
	case engine.FieldType:
		return m.GetType()
	case engine.FieldConfigurations:
		return m.Configurations()
	case engine.FieldOutputDatatype:
		return m.OutputDatatype()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *EngineMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case engine.FieldName:
		return m.OldName(ctx)
	case engine.FieldInputDatatype:
		return m.OldInputDatatype(ctx)
	case engine.FieldType:
		return m.OldType(ctx)
	case engine.FieldConfigurations:
		return m.OldConfigurations(ctx)
	case engine.FieldOutputDatatype:
		return m.OldOutputDatatype(ctx)
	}
	return nil, fmt.Errorf("unknown Engine field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *EngineMutation) SetField(name string, value ent.Value) error {
	switch name {
	case engine.FieldName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetName(v)
		return nil
	case engine.FieldInputDatatype:
		v, ok := value.(engine.InputDatatype)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetInputDatatype(v)
		return nil
	case engine.FieldType:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetType(v)
		return nil
	case engine.FieldConfigurations:
		v, ok := value.(map[string]interface{})
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetConfigurations(v)
		return nil
	case engine.FieldOutputDatatype:
		v, ok := value.(engine.OutputDatatype)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetOutputDatatype(v)
		return nil
	}
	return fmt.Errorf("unknown Engine field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *EngineMutation) AddedFields() []string {
	return nil
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *EngineMutation) AddedField(name string) (ent.Value, bool) {
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *EngineMutation) AddField(name string, value ent.Value) error {
	switch name {
	}
	return fmt.Errorf("unknown Engine numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *EngineMutation) ClearedFields() []string {
	var fields []string
	if m.FieldCleared(engine.FieldConfigurations) {
		fields = append(fields, engine.FieldConfigurations)
	}
	return fields
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *EngineMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *EngineMutation) ClearField(name string) error {
	switch name {
	case engine.FieldConfigurations:
		m.ClearConfigurations()
		return nil
	}
	return fmt.Errorf("unknown Engine nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *EngineMutation) ResetField(name string) error {
	switch name {
	case engine.FieldName:
		m.ResetName()
		return nil
	case engine.FieldInputDatatype:
		m.ResetInputDatatype()
		return nil
	case engine.FieldType:
		m.ResetType()
		return nil
	case engine.FieldConfigurations:
		m.ResetConfigurations()
		return nil
	case engine.FieldOutputDatatype:
		m.ResetOutputDatatype()
		return nil
	}
	return fmt.Errorf("unknown Engine field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *EngineMutation) AddedEdges() []string {
	edges := make([]string, 0, 1)
	if m.targets != nil {
		edges = append(edges, engine.EdgeTargets)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *EngineMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case engine.EdgeTargets:
		ids := make([]ent.Value, 0, len(m.targets))
		for id := range m.targets {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *EngineMutation) RemovedEdges() []string {
	edges := make([]string, 0, 1)
	if m.removedtargets != nil {
		edges = append(edges, engine.EdgeTargets)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *EngineMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case engine.EdgeTargets:
		ids := make([]ent.Value, 0, len(m.removedtargets))
		for id := range m.removedtargets {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *EngineMutation) ClearedEdges() []string {
	edges := make([]string, 0, 1)
	if m.clearedtargets {
		edges = append(edges, engine.EdgeTargets)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *EngineMutation) EdgeCleared(name string) bool {
	switch name {
	case engine.EdgeTargets:
		return m.clearedtargets
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *EngineMutation) ClearEdge(name string) error {
	switch name {
	}
	return fmt.Errorf("unknown Engine unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *EngineMutation) ResetEdge(name string) error {
	switch name {
	case engine.EdgeTargets:
		m.ResetTargets()
		return nil
	}
	return fmt.Errorf("unknown Engine edge %s", name)
}

// GroupMutation represents an operation that mutates the Group nodes in the graph.
type GroupMutation struct {
	config
	op                 Op
	typ                string
	id                 *uuid.UUID
	name               *string
	clearedFields      map[string]struct{}
	users              map[uuid.UUID]struct{}
	removedusers       map[uuid.UUID]struct{}
	clearedusers       bool
	permissions        map[uuid.UUID]struct{}
	removedpermissions map[uuid.UUID]struct{}
	clearedpermissions bool
	done               bool
	oldValue           func(context.Context) (*Group, error)
	predicates         []predicate.Group
}

var _ ent.Mutation = (*GroupMutation)(nil)

// groupOption allows management of the mutation configuration using functional options.
type groupOption func(*GroupMutation)

// newGroupMutation creates new mutation for the Group entity.
func newGroupMutation(c config, op Op, opts ...groupOption) *GroupMutation {
	m := &GroupMutation{
		config:        c,
		op:            op,
		typ:           TypeGroup,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withGroupID sets the ID field of the mutation.
func withGroupID(id uuid.UUID) groupOption {
	return func(m *GroupMutation) {
		var (
			err   error
			once  sync.Once
			value *Group
		)
		m.oldValue = func(ctx context.Context) (*Group, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Group.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withGroup sets the old Group of the mutation.
func withGroup(node *Group) groupOption {
	return func(m *GroupMutation) {
		m.oldValue = func(context.Context) (*Group, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m GroupMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m GroupMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of Group entities.
func (m *GroupMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *GroupMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *GroupMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Group.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetName sets the "name" field.
func (m *GroupMutation) SetName(s string) {
	m.name = &s
}

// Name returns the value of the "name" field in the mutation.
func (m *GroupMutation) Name() (r string, exists bool) {
	v := m.name
	if v == nil {
		return
	}
	return *v, true
}

// OldName returns the old "name" field's value of the Group entity.
// If the Group object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *GroupMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// ResetName resets all changes to the "name" field.
func (m *GroupMutation) ResetName() {
	m.name = nil
}

// AddUserIDs adds the "users" edge to the User entity by ids.
func (m *GroupMutation) AddUserIDs(ids ...uuid.UUID) {
	if m.users == nil {
		m.users = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.users[ids[i]] = struct{}{}
	}
}

// ClearUsers clears the "users" edge to the User entity.
func (m *GroupMutation) ClearUsers() {
	m.clearedusers = true
}

// UsersCleared reports if the "users" edge to the User entity was cleared.
func (m *GroupMutation) UsersCleared() bool {
	return m.clearedusers
}

// RemoveUserIDs removes the "users" edge to the User entity by IDs.
func (m *GroupMutation) RemoveUserIDs(ids ...uuid.UUID) {
	if m.removedusers == nil {
		m.removedusers = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.users, ids[i])
		m.removedusers[ids[i]] = struct{}{}
	}
}

// RemovedUsers returns the removed IDs of the "users" edge to the User entity.
func (m *GroupMutation) RemovedUsersIDs() (ids []uuid.UUID) {
	for id := range m.removedusers {
		ids = append(ids, id)
	}
	return
}

// UsersIDs returns the "users" edge IDs in the mutation.
func (m *GroupMutation) UsersIDs() (ids []uuid.UUID) {
	for id := range m.users {
		ids = append(ids, id)
	}
	return
}

// ResetUsers resets all changes to the "users" edge.
func (m *GroupMutation) ResetUsers() {
	m.users = nil
	m.clearedusers = false
	m.removedusers = nil
}

// AddPermissionIDs adds the "permissions" edge to the Permission entity by ids.
func (m *GroupMutation) AddPermissionIDs(ids ...uuid.UUID) {
	if m.permissions == nil {
		m.permissions = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.permissions[ids[i]] = struct{}{}
	}
}

// ClearPermissions clears the "permissions" edge to the Permission entity.
func (m *GroupMutation) ClearPermissions() {
	m.clearedpermissions = true
}

// PermissionsCleared reports if the "permissions" edge to the Permission entity was cleared.
func (m *GroupMutation) PermissionsCleared() bool {
	return m.clearedpermissions
}

// RemovePermissionIDs removes the "permissions" edge to the Permission entity by IDs.
func (m *GroupMutation) RemovePermissionIDs(ids ...uuid.UUID) {
	if m.removedpermissions == nil {
		m.removedpermissions = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.permissions, ids[i])
		m.removedpermissions[ids[i]] = struct{}{}
	}
}

// RemovedPermissions returns the removed IDs of the "permissions" edge to the Permission entity.
func (m *GroupMutation) RemovedPermissionsIDs() (ids []uuid.UUID) {
	for id := range m.removedpermissions {
		ids = append(ids, id)
	}
	return
}

// PermissionsIDs returns the "permissions" edge IDs in the mutation.
func (m *GroupMutation) PermissionsIDs() (ids []uuid.UUID) {
	for id := range m.permissions {
		ids = append(ids, id)
	}
	return
}

// ResetPermissions resets all changes to the "permissions" edge.
func (m *GroupMutation) ResetPermissions() {
	m.permissions = nil
	m.clearedpermissions = false
	m.removedpermissions = nil
}

// Where appends a list predicates to the GroupMutation builder.
func (m *GroupMutation) Where(ps ...predicate.Group) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the GroupMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *GroupMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Group, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *GroupMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *GroupMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Group).
func (m *GroupMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *GroupMutation) Fields() []string {
	fields := make([]string, 0, 1)
	if m.name != nil {
		fields = append(fields, group.FieldName)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *GroupMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case group.FieldName:
		return m.Name()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *GroupMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case group.FieldName:
		return m.OldName(ctx)
	}
	return nil, fmt.Errorf("unknown Group field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *GroupMutation) SetField(name string, value ent.Value) error {
	switch name {
	case group.FieldName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetName(v)
		return nil
	}
	return fmt.Errorf("unknown Group field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *GroupMutation) AddedFields() []string {
	return nil
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *GroupMutation) AddedField(name string) (ent.Value, bool) {
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *GroupMutation) AddField(name string, value ent.Value) error {
	switch name {
	}
	return fmt.Errorf("unknown Group numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *GroupMutation) ClearedFields() []string {
	return nil
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *GroupMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *GroupMutation) ClearField(name string) error {
	return fmt.Errorf("unknown Group nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *GroupMutation) ResetField(name string) error {
	switch name {
	case group.FieldName:
		m.ResetName()
		return nil
	}
	return fmt.Errorf("unknown Group field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *GroupMutation) AddedEdges() []string {
	edges := make([]string, 0, 2)
	if m.users != nil {
		edges = append(edges, group.EdgeUsers)
	}
	if m.permissions != nil {
		edges = append(edges, group.EdgePermissions)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *GroupMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case group.EdgeUsers:
		ids := make([]ent.Value, 0, len(m.users))
		for id := range m.users {
			ids = append(ids, id)
		}
		return ids
	case group.EdgePermissions:
		ids := make([]ent.Value, 0, len(m.permissions))
		for id := range m.permissions {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *GroupMutation) RemovedEdges() []string {
	edges := make([]string, 0, 2)
	if m.removedusers != nil {
		edges = append(edges, group.EdgeUsers)
	}
	if m.removedpermissions != nil {
		edges = append(edges, group.EdgePermissions)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *GroupMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case group.EdgeUsers:
		ids := make([]ent.Value, 0, len(m.removedusers))
		for id := range m.removedusers {
			ids = append(ids, id)
		}
		return ids
	case group.EdgePermissions:
		ids := make([]ent.Value, 0, len(m.removedpermissions))
		for id := range m.removedpermissions {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *GroupMutation) ClearedEdges() []string {
	edges := make([]string, 0, 2)
	if m.clearedusers {
		edges = append(edges, group.EdgeUsers)
	}
	if m.clearedpermissions {
		edges = append(edges, group.EdgePermissions)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *GroupMutation) EdgeCleared(name string) bool {
	switch name {
	case group.EdgeUsers:
		return m.clearedusers
	case group.EdgePermissions:
		return m.clearedpermissions
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *GroupMutation) ClearEdge(name string) error {
	switch name {
	}
	return fmt.Errorf("unknown Group unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *GroupMutation) ResetEdge(name string) error {
	switch name {
	case group.EdgeUsers:
		m.ResetUsers()
		return nil
	case group.EdgePermissions:
		m.ResetPermissions()
		return nil
	}
	return fmt.Errorf("unknown Group edge %s", name)
}

// PatternMutation represents an operation that mutates the Pattern nodes in the graph.
type PatternMutation struct {
	config
	op             Op
	typ            string
	id             *uuid.UUID
	name           *string
	phase          *int
	addphase       *int
	_type          *pattern.Type
	datatype       *pattern.Datatype
	clearedFields  map[string]struct{}
	targets        map[uuid.UUID]struct{}
	removedtargets map[uuid.UUID]struct{}
	clearedtargets bool
	done           bool
	oldValue       func(context.Context) (*Pattern, error)
	predicates     []predicate.Pattern
}

var _ ent.Mutation = (*PatternMutation)(nil)

// patternOption allows management of the mutation configuration using functional options.
type patternOption func(*PatternMutation)

// newPatternMutation creates new mutation for the Pattern entity.
func newPatternMutation(c config, op Op, opts ...patternOption) *PatternMutation {
	m := &PatternMutation{
		config:        c,
		op:            op,
		typ:           TypePattern,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withPatternID sets the ID field of the mutation.
func withPatternID(id uuid.UUID) patternOption {
	return func(m *PatternMutation) {
		var (
			err   error
			once  sync.Once
			value *Pattern
		)
		m.oldValue = func(ctx context.Context) (*Pattern, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Pattern.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withPattern sets the old Pattern of the mutation.
func withPattern(node *Pattern) patternOption {
	return func(m *PatternMutation) {
		m.oldValue = func(context.Context) (*Pattern, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m PatternMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m PatternMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of Pattern entities.
func (m *PatternMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *PatternMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *PatternMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Pattern.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetName sets the "name" field.
func (m *PatternMutation) SetName(s string) {
	m.name = &s
}

// Name returns the value of the "name" field in the mutation.
func (m *PatternMutation) Name() (r string, exists bool) {
	v := m.name
	if v == nil {
		return
	}
	return *v, true
}

// OldName returns the old "name" field's value of the Pattern entity.
// If the Pattern object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *PatternMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// ResetName resets all changes to the "name" field.
func (m *PatternMutation) ResetName() {
	m.name = nil
}

// SetPhase sets the "phase" field.
func (m *PatternMutation) SetPhase(i int) {
	m.phase = &i
	m.addphase = nil
}

// Phase returns the value of the "phase" field in the mutation.
func (m *PatternMutation) Phase() (r int, exists bool) {
	v := m.phase
	if v == nil {
		return
	}
	return *v, true
}

// OldPhase returns the old "phase" field's value of the Pattern entity.
// If the Pattern object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *PatternMutation) OldPhase(ctx context.Context) (v int, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldPhase is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldPhase requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldPhase: %w", err)
	}
	return oldValue.Phase, nil
}

// AddPhase adds i to the "phase" field.
func (m *PatternMutation) AddPhase(i int) {
	if m.addphase != nil {
		*m.addphase += i
	} else {
		m.addphase = &i
	}
}

// AddedPhase returns the value that was added to the "phase" field in this mutation.
func (m *PatternMutation) AddedPhase() (r int, exists bool) {
	v := m.addphase
	if v == nil {
		return
	}
	return *v, true
}

// ResetPhase resets all changes to the "phase" field.
func (m *PatternMutation) ResetPhase() {
	m.phase = nil
	m.addphase = nil
}

// SetType sets the "type" field.
func (m *PatternMutation) SetType(pa pattern.Type) {
	m._type = &pa
}

// GetType returns the value of the "type" field in the mutation.
func (m *PatternMutation) GetType() (r pattern.Type, exists bool) {
	v := m._type
	if v == nil {
		return
	}
	return *v, true
}

// OldType returns the old "type" field's value of the Pattern entity.
// If the Pattern object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *PatternMutation) OldType(ctx context.Context) (v pattern.Type, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldType is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldType requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldType: %w", err)
	}
	return oldValue.Type, nil
}

// ResetType resets all changes to the "type" field.
func (m *PatternMutation) ResetType() {
	m._type = nil
}

// SetDatatype sets the "datatype" field.
func (m *PatternMutation) SetDatatype(pa pattern.Datatype) {
	m.datatype = &pa
}

// Datatype returns the value of the "datatype" field in the mutation.
func (m *PatternMutation) Datatype() (r pattern.Datatype, exists bool) {
	v := m.datatype
	if v == nil {
		return
	}
	return *v, true
}

// OldDatatype returns the old "datatype" field's value of the Pattern entity.
// If the Pattern object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *PatternMutation) OldDatatype(ctx context.Context) (v pattern.Datatype, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldDatatype is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldDatatype requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDatatype: %w", err)
	}
	return oldValue.Datatype, nil
}

// ResetDatatype resets all changes to the "datatype" field.
func (m *PatternMutation) ResetDatatype() {
	m.datatype = nil
}

// AddTargetIDs adds the "targets" edge to the Target entity by ids.
func (m *PatternMutation) AddTargetIDs(ids ...uuid.UUID) {
	if m.targets == nil {
		m.targets = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.targets[ids[i]] = struct{}{}
	}
}

// ClearTargets clears the "targets" edge to the Target entity.
func (m *PatternMutation) ClearTargets() {
	m.clearedtargets = true
}

// TargetsCleared reports if the "targets" edge to the Target entity was cleared.
func (m *PatternMutation) TargetsCleared() bool {
	return m.clearedtargets
}

// RemoveTargetIDs removes the "targets" edge to the Target entity by IDs.
func (m *PatternMutation) RemoveTargetIDs(ids ...uuid.UUID) {
	if m.removedtargets == nil {
		m.removedtargets = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.targets, ids[i])
		m.removedtargets[ids[i]] = struct{}{}
	}
}

// RemovedTargets returns the removed IDs of the "targets" edge to the Target entity.
func (m *PatternMutation) RemovedTargetsIDs() (ids []uuid.UUID) {
	for id := range m.removedtargets {
		ids = append(ids, id)
	}
	return
}

// TargetsIDs returns the "targets" edge IDs in the mutation.
func (m *PatternMutation) TargetsIDs() (ids []uuid.UUID) {
	for id := range m.targets {
		ids = append(ids, id)
	}
	return
}

// ResetTargets resets all changes to the "targets" edge.
func (m *PatternMutation) ResetTargets() {
	m.targets = nil
	m.clearedtargets = false
	m.removedtargets = nil
}

// Where appends a list predicates to the PatternMutation builder.
func (m *PatternMutation) Where(ps ...predicate.Pattern) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the PatternMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *PatternMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Pattern, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *PatternMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *PatternMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Pattern).
func (m *PatternMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *PatternMutation) Fields() []string {
	fields := make([]string, 0, 4)
	if m.name != nil {
		fields = append(fields, pattern.FieldName)
	}
	if m.phase != nil {
		fields = append(fields, pattern.FieldPhase)
	}
	if m._type != nil {
		fields = append(fields, pattern.FieldType)
	}
	if m.datatype != nil {
		fields = append(fields, pattern.FieldDatatype)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *PatternMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case pattern.FieldName:
		return m.Name()
	case pattern.FieldPhase:
		return m.Phase()
	case pattern.FieldType:
		return m.GetType()
	case pattern.FieldDatatype:
		return m.Datatype()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *PatternMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case pattern.FieldName:
		return m.OldName(ctx)
	case pattern.FieldPhase:
		return m.OldPhase(ctx)
	case pattern.FieldType:
		return m.OldType(ctx)
	case pattern.FieldDatatype:
		return m.OldDatatype(ctx)
	}
	return nil, fmt.Errorf("unknown Pattern field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *PatternMutation) SetField(name string, value ent.Value) error {
	switch name {
	case pattern.FieldName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetName(v)
		return nil
	case pattern.FieldPhase:
		v, ok := value.(int)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetPhase(v)
		return nil
	case pattern.FieldType:
		v, ok := value.(pattern.Type)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetType(v)
		return nil
	case pattern.FieldDatatype:
		v, ok := value.(pattern.Datatype)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetDatatype(v)
		return nil
	}
	return fmt.Errorf("unknown Pattern field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *PatternMutation) AddedFields() []string {
	var fields []string
	if m.addphase != nil {
		fields = append(fields, pattern.FieldPhase)
	}
	return fields
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *PatternMutation) AddedField(name string) (ent.Value, bool) {
	switch name {
	case pattern.FieldPhase:
		return m.AddedPhase()
	}
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *PatternMutation) AddField(name string, value ent.Value) error {
	switch name {
	case pattern.FieldPhase:
		v, ok := value.(int)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.AddPhase(v)
		return nil
	}
	return fmt.Errorf("unknown Pattern numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *PatternMutation) ClearedFields() []string {
	return nil
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *PatternMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *PatternMutation) ClearField(name string) error {
	return fmt.Errorf("unknown Pattern nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *PatternMutation) ResetField(name string) error {
	switch name {
	case pattern.FieldName:
		m.ResetName()
		return nil
	case pattern.FieldPhase:
		m.ResetPhase()
		return nil
	case pattern.FieldType:
		m.ResetType()
		return nil
	case pattern.FieldDatatype:
		m.ResetDatatype()
		return nil
	}
	return fmt.Errorf("unknown Pattern field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *PatternMutation) AddedEdges() []string {
	edges := make([]string, 0, 1)
	if m.targets != nil {
		edges = append(edges, pattern.EdgeTargets)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *PatternMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case pattern.EdgeTargets:
		ids := make([]ent.Value, 0, len(m.targets))
		for id := range m.targets {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *PatternMutation) RemovedEdges() []string {
	edges := make([]string, 0, 1)
	if m.removedtargets != nil {
		edges = append(edges, pattern.EdgeTargets)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *PatternMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case pattern.EdgeTargets:
		ids := make([]ent.Value, 0, len(m.removedtargets))
		for id := range m.removedtargets {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *PatternMutation) ClearedEdges() []string {
	edges := make([]string, 0, 1)
	if m.clearedtargets {
		edges = append(edges, pattern.EdgeTargets)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *PatternMutation) EdgeCleared(name string) bool {
	switch name {
	case pattern.EdgeTargets:
		return m.clearedtargets
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *PatternMutation) ClearEdge(name string) error {
	switch name {
	}
	return fmt.Errorf("unknown Pattern unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *PatternMutation) ResetEdge(name string) error {
	switch name {
	case pattern.EdgeTargets:
		m.ResetTargets()
		return nil
	}
	return fmt.Errorf("unknown Pattern edge %s", name)
}

// PermissionMutation represents an operation that mutates the Permission nodes in the graph.
type PermissionMutation struct {
	config
	op            Op
	typ           string
	id            *uuid.UUID
	applied_for   *string
	action        *string
	clearedFields map[string]struct{}
	users         map[uuid.UUID]struct{}
	removedusers  map[uuid.UUID]struct{}
	clearedusers  bool
	groups        map[uuid.UUID]struct{}
	removedgroups map[uuid.UUID]struct{}
	clearedgroups bool
	done          bool
	oldValue      func(context.Context) (*Permission, error)
	predicates    []predicate.Permission
}

var _ ent.Mutation = (*PermissionMutation)(nil)

// permissionOption allows management of the mutation configuration using functional options.
type permissionOption func(*PermissionMutation)

// newPermissionMutation creates new mutation for the Permission entity.
func newPermissionMutation(c config, op Op, opts ...permissionOption) *PermissionMutation {
	m := &PermissionMutation{
		config:        c,
		op:            op,
		typ:           TypePermission,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withPermissionID sets the ID field of the mutation.
func withPermissionID(id uuid.UUID) permissionOption {
	return func(m *PermissionMutation) {
		var (
			err   error
			once  sync.Once
			value *Permission
		)
		m.oldValue = func(ctx context.Context) (*Permission, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Permission.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withPermission sets the old Permission of the mutation.
func withPermission(node *Permission) permissionOption {
	return func(m *PermissionMutation) {
		m.oldValue = func(context.Context) (*Permission, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m PermissionMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m PermissionMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of Permission entities.
func (m *PermissionMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *PermissionMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *PermissionMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Permission.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetAppliedFor sets the "applied_for" field.
func (m *PermissionMutation) SetAppliedFor(s string) {
	m.applied_for = &s
}

// AppliedFor returns the value of the "applied_for" field in the mutation.
func (m *PermissionMutation) AppliedFor() (r string, exists bool) {
	v := m.applied_for
	if v == nil {
		return
	}
	return *v, true
}

// OldAppliedFor returns the old "applied_for" field's value of the Permission entity.
// If the Permission object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *PermissionMutation) OldAppliedFor(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldAppliedFor is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldAppliedFor requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldAppliedFor: %w", err)
	}
	return oldValue.AppliedFor, nil
}

// ResetAppliedFor resets all changes to the "applied_for" field.
func (m *PermissionMutation) ResetAppliedFor() {
	m.applied_for = nil
}

// SetAction sets the "action" field.
func (m *PermissionMutation) SetAction(s string) {
	m.action = &s
}

// Action returns the value of the "action" field in the mutation.
func (m *PermissionMutation) Action() (r string, exists bool) {
	v := m.action
	if v == nil {
		return
	}
	return *v, true
}

// OldAction returns the old "action" field's value of the Permission entity.
// If the Permission object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *PermissionMutation) OldAction(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldAction is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldAction requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldAction: %w", err)
	}
	return oldValue.Action, nil
}

// ResetAction resets all changes to the "action" field.
func (m *PermissionMutation) ResetAction() {
	m.action = nil
}

// AddUserIDs adds the "users" edge to the User entity by ids.
func (m *PermissionMutation) AddUserIDs(ids ...uuid.UUID) {
	if m.users == nil {
		m.users = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.users[ids[i]] = struct{}{}
	}
}

// ClearUsers clears the "users" edge to the User entity.
func (m *PermissionMutation) ClearUsers() {
	m.clearedusers = true
}

// UsersCleared reports if the "users" edge to the User entity was cleared.
func (m *PermissionMutation) UsersCleared() bool {
	return m.clearedusers
}

// RemoveUserIDs removes the "users" edge to the User entity by IDs.
func (m *PermissionMutation) RemoveUserIDs(ids ...uuid.UUID) {
	if m.removedusers == nil {
		m.removedusers = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.users, ids[i])
		m.removedusers[ids[i]] = struct{}{}
	}
}

// RemovedUsers returns the removed IDs of the "users" edge to the User entity.
func (m *PermissionMutation) RemovedUsersIDs() (ids []uuid.UUID) {
	for id := range m.removedusers {
		ids = append(ids, id)
	}
	return
}

// UsersIDs returns the "users" edge IDs in the mutation.
func (m *PermissionMutation) UsersIDs() (ids []uuid.UUID) {
	for id := range m.users {
		ids = append(ids, id)
	}
	return
}

// ResetUsers resets all changes to the "users" edge.
func (m *PermissionMutation) ResetUsers() {
	m.users = nil
	m.clearedusers = false
	m.removedusers = nil
}

// AddGroupIDs adds the "groups" edge to the Group entity by ids.
func (m *PermissionMutation) AddGroupIDs(ids ...uuid.UUID) {
	if m.groups == nil {
		m.groups = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.groups[ids[i]] = struct{}{}
	}
}

// ClearGroups clears the "groups" edge to the Group entity.
func (m *PermissionMutation) ClearGroups() {
	m.clearedgroups = true
}

// GroupsCleared reports if the "groups" edge to the Group entity was cleared.
func (m *PermissionMutation) GroupsCleared() bool {
	return m.clearedgroups
}

// RemoveGroupIDs removes the "groups" edge to the Group entity by IDs.
func (m *PermissionMutation) RemoveGroupIDs(ids ...uuid.UUID) {
	if m.removedgroups == nil {
		m.removedgroups = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.groups, ids[i])
		m.removedgroups[ids[i]] = struct{}{}
	}
}

// RemovedGroups returns the removed IDs of the "groups" edge to the Group entity.
func (m *PermissionMutation) RemovedGroupsIDs() (ids []uuid.UUID) {
	for id := range m.removedgroups {
		ids = append(ids, id)
	}
	return
}

// GroupsIDs returns the "groups" edge IDs in the mutation.
func (m *PermissionMutation) GroupsIDs() (ids []uuid.UUID) {
	for id := range m.groups {
		ids = append(ids, id)
	}
	return
}

// ResetGroups resets all changes to the "groups" edge.
func (m *PermissionMutation) ResetGroups() {
	m.groups = nil
	m.clearedgroups = false
	m.removedgroups = nil
}

// Where appends a list predicates to the PermissionMutation builder.
func (m *PermissionMutation) Where(ps ...predicate.Permission) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the PermissionMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *PermissionMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Permission, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *PermissionMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *PermissionMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Permission).
func (m *PermissionMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *PermissionMutation) Fields() []string {
	fields := make([]string, 0, 2)
	if m.applied_for != nil {
		fields = append(fields, permission.FieldAppliedFor)
	}
	if m.action != nil {
		fields = append(fields, permission.FieldAction)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *PermissionMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case permission.FieldAppliedFor:
		return m.AppliedFor()
	case permission.FieldAction:
		return m.Action()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *PermissionMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case permission.FieldAppliedFor:
		return m.OldAppliedFor(ctx)
	case permission.FieldAction:
		return m.OldAction(ctx)
	}
	return nil, fmt.Errorf("unknown Permission field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *PermissionMutation) SetField(name string, value ent.Value) error {
	switch name {
	case permission.FieldAppliedFor:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetAppliedFor(v)
		return nil
	case permission.FieldAction:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetAction(v)
		return nil
	}
	return fmt.Errorf("unknown Permission field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *PermissionMutation) AddedFields() []string {
	return nil
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *PermissionMutation) AddedField(name string) (ent.Value, bool) {
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *PermissionMutation) AddField(name string, value ent.Value) error {
	switch name {
	}
	return fmt.Errorf("unknown Permission numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *PermissionMutation) ClearedFields() []string {
	return nil
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *PermissionMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *PermissionMutation) ClearField(name string) error {
	return fmt.Errorf("unknown Permission nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *PermissionMutation) ResetField(name string) error {
	switch name {
	case permission.FieldAppliedFor:
		m.ResetAppliedFor()
		return nil
	case permission.FieldAction:
		m.ResetAction()
		return nil
	}
	return fmt.Errorf("unknown Permission field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *PermissionMutation) AddedEdges() []string {
	edges := make([]string, 0, 2)
	if m.users != nil {
		edges = append(edges, permission.EdgeUsers)
	}
	if m.groups != nil {
		edges = append(edges, permission.EdgeGroups)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *PermissionMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case permission.EdgeUsers:
		ids := make([]ent.Value, 0, len(m.users))
		for id := range m.users {
			ids = append(ids, id)
		}
		return ids
	case permission.EdgeGroups:
		ids := make([]ent.Value, 0, len(m.groups))
		for id := range m.groups {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *PermissionMutation) RemovedEdges() []string {
	edges := make([]string, 0, 2)
	if m.removedusers != nil {
		edges = append(edges, permission.EdgeUsers)
	}
	if m.removedgroups != nil {
		edges = append(edges, permission.EdgeGroups)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *PermissionMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case permission.EdgeUsers:
		ids := make([]ent.Value, 0, len(m.removedusers))
		for id := range m.removedusers {
			ids = append(ids, id)
		}
		return ids
	case permission.EdgeGroups:
		ids := make([]ent.Value, 0, len(m.removedgroups))
		for id := range m.removedgroups {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *PermissionMutation) ClearedEdges() []string {
	edges := make([]string, 0, 2)
	if m.clearedusers {
		edges = append(edges, permission.EdgeUsers)
	}
	if m.clearedgroups {
		edges = append(edges, permission.EdgeGroups)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *PermissionMutation) EdgeCleared(name string) bool {
	switch name {
	case permission.EdgeUsers:
		return m.clearedusers
	case permission.EdgeGroups:
		return m.clearedgroups
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *PermissionMutation) ClearEdge(name string) error {
	switch name {
	}
	return fmt.Errorf("unknown Permission unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *PermissionMutation) ResetEdge(name string) error {
	switch name {
	case permission.EdgeUsers:
		m.ResetUsers()
		return nil
	case permission.EdgeGroups:
		m.ResetGroups()
		return nil
	}
	return fmt.Errorf("unknown Permission edge %s", name)
}

// PrincipleMutation represents an operation that mutates the Principle nodes in the graph.
type PrincipleMutation struct {
	config
	op               Op
	typ              string
	id               *uuid.UUID
	name             *string
	level            *uint64
	addlevel         *int64
	phase            *int
	addphase         *int
	clearedFields    map[string]struct{}
	rules            map[uuid.UUID]struct{}
	removedrules     map[uuid.UUID]struct{}
	clearedrules     bool
	defenders        map[uuid.UUID]struct{}
	removeddefenders map[uuid.UUID]struct{}
	cleareddefenders bool
	done             bool
	oldValue         func(context.Context) (*Principle, error)
	predicates       []predicate.Principle
}

var _ ent.Mutation = (*PrincipleMutation)(nil)

// principleOption allows management of the mutation configuration using functional options.
type principleOption func(*PrincipleMutation)

// newPrincipleMutation creates new mutation for the Principle entity.
func newPrincipleMutation(c config, op Op, opts ...principleOption) *PrincipleMutation {
	m := &PrincipleMutation{
		config:        c,
		op:            op,
		typ:           TypePrinciple,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withPrincipleID sets the ID field of the mutation.
func withPrincipleID(id uuid.UUID) principleOption {
	return func(m *PrincipleMutation) {
		var (
			err   error
			once  sync.Once
			value *Principle
		)
		m.oldValue = func(ctx context.Context) (*Principle, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Principle.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withPrinciple sets the old Principle of the mutation.
func withPrinciple(node *Principle) principleOption {
	return func(m *PrincipleMutation) {
		m.oldValue = func(context.Context) (*Principle, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m PrincipleMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m PrincipleMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of Principle entities.
func (m *PrincipleMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *PrincipleMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *PrincipleMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Principle.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetName sets the "name" field.
func (m *PrincipleMutation) SetName(s string) {
	m.name = &s
}

// Name returns the value of the "name" field in the mutation.
func (m *PrincipleMutation) Name() (r string, exists bool) {
	v := m.name
	if v == nil {
		return
	}
	return *v, true
}

// OldName returns the old "name" field's value of the Principle entity.
// If the Principle object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *PrincipleMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// ResetName resets all changes to the "name" field.
func (m *PrincipleMutation) ResetName() {
	m.name = nil
}

// SetLevel sets the "level" field.
func (m *PrincipleMutation) SetLevel(u uint64) {
	m.level = &u
	m.addlevel = nil
}

// Level returns the value of the "level" field in the mutation.
func (m *PrincipleMutation) Level() (r uint64, exists bool) {
	v := m.level
	if v == nil {
		return
	}
	return *v, true
}

// OldLevel returns the old "level" field's value of the Principle entity.
// If the Principle object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *PrincipleMutation) OldLevel(ctx context.Context) (v uint64, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldLevel is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldLevel requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldLevel: %w", err)
	}
	return oldValue.Level, nil
}

// AddLevel adds u to the "level" field.
func (m *PrincipleMutation) AddLevel(u int64) {
	if m.addlevel != nil {
		*m.addlevel += u
	} else {
		m.addlevel = &u
	}
}

// AddedLevel returns the value that was added to the "level" field in this mutation.
func (m *PrincipleMutation) AddedLevel() (r int64, exists bool) {
	v := m.addlevel
	if v == nil {
		return
	}
	return *v, true
}

// ResetLevel resets all changes to the "level" field.
func (m *PrincipleMutation) ResetLevel() {
	m.level = nil
	m.addlevel = nil
}

// SetPhase sets the "phase" field.
func (m *PrincipleMutation) SetPhase(i int) {
	m.phase = &i
	m.addphase = nil
}

// Phase returns the value of the "phase" field in the mutation.
func (m *PrincipleMutation) Phase() (r int, exists bool) {
	v := m.phase
	if v == nil {
		return
	}
	return *v, true
}

// OldPhase returns the old "phase" field's value of the Principle entity.
// If the Principle object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *PrincipleMutation) OldPhase(ctx context.Context) (v int, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldPhase is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldPhase requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldPhase: %w", err)
	}
	return oldValue.Phase, nil
}

// AddPhase adds i to the "phase" field.
func (m *PrincipleMutation) AddPhase(i int) {
	if m.addphase != nil {
		*m.addphase += i
	} else {
		m.addphase = &i
	}
}

// AddedPhase returns the value that was added to the "phase" field in this mutation.
func (m *PrincipleMutation) AddedPhase() (r int, exists bool) {
	v := m.addphase
	if v == nil {
		return
	}
	return *v, true
}

// ResetPhase resets all changes to the "phase" field.
func (m *PrincipleMutation) ResetPhase() {
	m.phase = nil
	m.addphase = nil
}

// AddRuleIDs adds the "rules" edge to the Rule entity by ids.
func (m *PrincipleMutation) AddRuleIDs(ids ...uuid.UUID) {
	if m.rules == nil {
		m.rules = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.rules[ids[i]] = struct{}{}
	}
}

// ClearRules clears the "rules" edge to the Rule entity.
func (m *PrincipleMutation) ClearRules() {
	m.clearedrules = true
}

// RulesCleared reports if the "rules" edge to the Rule entity was cleared.
func (m *PrincipleMutation) RulesCleared() bool {
	return m.clearedrules
}

// RemoveRuleIDs removes the "rules" edge to the Rule entity by IDs.
func (m *PrincipleMutation) RemoveRuleIDs(ids ...uuid.UUID) {
	if m.removedrules == nil {
		m.removedrules = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.rules, ids[i])
		m.removedrules[ids[i]] = struct{}{}
	}
}

// RemovedRules returns the removed IDs of the "rules" edge to the Rule entity.
func (m *PrincipleMutation) RemovedRulesIDs() (ids []uuid.UUID) {
	for id := range m.removedrules {
		ids = append(ids, id)
	}
	return
}

// RulesIDs returns the "rules" edge IDs in the mutation.
func (m *PrincipleMutation) RulesIDs() (ids []uuid.UUID) {
	for id := range m.rules {
		ids = append(ids, id)
	}
	return
}

// ResetRules resets all changes to the "rules" edge.
func (m *PrincipleMutation) ResetRules() {
	m.rules = nil
	m.clearedrules = false
	m.removedrules = nil
}

// AddDefenderIDs adds the "defenders" edge to the Defender entity by ids.
func (m *PrincipleMutation) AddDefenderIDs(ids ...uuid.UUID) {
	if m.defenders == nil {
		m.defenders = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.defenders[ids[i]] = struct{}{}
	}
}

// ClearDefenders clears the "defenders" edge to the Defender entity.
func (m *PrincipleMutation) ClearDefenders() {
	m.cleareddefenders = true
}

// DefendersCleared reports if the "defenders" edge to the Defender entity was cleared.
func (m *PrincipleMutation) DefendersCleared() bool {
	return m.cleareddefenders
}

// RemoveDefenderIDs removes the "defenders" edge to the Defender entity by IDs.
func (m *PrincipleMutation) RemoveDefenderIDs(ids ...uuid.UUID) {
	if m.removeddefenders == nil {
		m.removeddefenders = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.defenders, ids[i])
		m.removeddefenders[ids[i]] = struct{}{}
	}
}

// RemovedDefenders returns the removed IDs of the "defenders" edge to the Defender entity.
func (m *PrincipleMutation) RemovedDefendersIDs() (ids []uuid.UUID) {
	for id := range m.removeddefenders {
		ids = append(ids, id)
	}
	return
}

// DefendersIDs returns the "defenders" edge IDs in the mutation.
func (m *PrincipleMutation) DefendersIDs() (ids []uuid.UUID) {
	for id := range m.defenders {
		ids = append(ids, id)
	}
	return
}

// ResetDefenders resets all changes to the "defenders" edge.
func (m *PrincipleMutation) ResetDefenders() {
	m.defenders = nil
	m.cleareddefenders = false
	m.removeddefenders = nil
}

// Where appends a list predicates to the PrincipleMutation builder.
func (m *PrincipleMutation) Where(ps ...predicate.Principle) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the PrincipleMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *PrincipleMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Principle, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *PrincipleMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *PrincipleMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Principle).
func (m *PrincipleMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *PrincipleMutation) Fields() []string {
	fields := make([]string, 0, 3)
	if m.name != nil {
		fields = append(fields, principle.FieldName)
	}
	if m.level != nil {
		fields = append(fields, principle.FieldLevel)
	}
	if m.phase != nil {
		fields = append(fields, principle.FieldPhase)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *PrincipleMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case principle.FieldName:
		return m.Name()
	case principle.FieldLevel:
		return m.Level()
	case principle.FieldPhase:
		return m.Phase()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *PrincipleMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case principle.FieldName:
		return m.OldName(ctx)
	case principle.FieldLevel:
		return m.OldLevel(ctx)
	case principle.FieldPhase:
		return m.OldPhase(ctx)
	}
	return nil, fmt.Errorf("unknown Principle field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *PrincipleMutation) SetField(name string, value ent.Value) error {
	switch name {
	case principle.FieldName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetName(v)
		return nil
	case principle.FieldLevel:
		v, ok := value.(uint64)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetLevel(v)
		return nil
	case principle.FieldPhase:
		v, ok := value.(int)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetPhase(v)
		return nil
	}
	return fmt.Errorf("unknown Principle field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *PrincipleMutation) AddedFields() []string {
	var fields []string
	if m.addlevel != nil {
		fields = append(fields, principle.FieldLevel)
	}
	if m.addphase != nil {
		fields = append(fields, principle.FieldPhase)
	}
	return fields
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *PrincipleMutation) AddedField(name string) (ent.Value, bool) {
	switch name {
	case principle.FieldLevel:
		return m.AddedLevel()
	case principle.FieldPhase:
		return m.AddedPhase()
	}
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *PrincipleMutation) AddField(name string, value ent.Value) error {
	switch name {
	case principle.FieldLevel:
		v, ok := value.(int64)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.AddLevel(v)
		return nil
	case principle.FieldPhase:
		v, ok := value.(int)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.AddPhase(v)
		return nil
	}
	return fmt.Errorf("unknown Principle numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *PrincipleMutation) ClearedFields() []string {
	return nil
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *PrincipleMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *PrincipleMutation) ClearField(name string) error {
	return fmt.Errorf("unknown Principle nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *PrincipleMutation) ResetField(name string) error {
	switch name {
	case principle.FieldName:
		m.ResetName()
		return nil
	case principle.FieldLevel:
		m.ResetLevel()
		return nil
	case principle.FieldPhase:
		m.ResetPhase()
		return nil
	}
	return fmt.Errorf("unknown Principle field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *PrincipleMutation) AddedEdges() []string {
	edges := make([]string, 0, 2)
	if m.rules != nil {
		edges = append(edges, principle.EdgeRules)
	}
	if m.defenders != nil {
		edges = append(edges, principle.EdgeDefenders)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *PrincipleMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case principle.EdgeRules:
		ids := make([]ent.Value, 0, len(m.rules))
		for id := range m.rules {
			ids = append(ids, id)
		}
		return ids
	case principle.EdgeDefenders:
		ids := make([]ent.Value, 0, len(m.defenders))
		for id := range m.defenders {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *PrincipleMutation) RemovedEdges() []string {
	edges := make([]string, 0, 2)
	if m.removedrules != nil {
		edges = append(edges, principle.EdgeRules)
	}
	if m.removeddefenders != nil {
		edges = append(edges, principle.EdgeDefenders)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *PrincipleMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case principle.EdgeRules:
		ids := make([]ent.Value, 0, len(m.removedrules))
		for id := range m.removedrules {
			ids = append(ids, id)
		}
		return ids
	case principle.EdgeDefenders:
		ids := make([]ent.Value, 0, len(m.removeddefenders))
		for id := range m.removeddefenders {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *PrincipleMutation) ClearedEdges() []string {
	edges := make([]string, 0, 2)
	if m.clearedrules {
		edges = append(edges, principle.EdgeRules)
	}
	if m.cleareddefenders {
		edges = append(edges, principle.EdgeDefenders)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *PrincipleMutation) EdgeCleared(name string) bool {
	switch name {
	case principle.EdgeRules:
		return m.clearedrules
	case principle.EdgeDefenders:
		return m.cleareddefenders
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *PrincipleMutation) ClearEdge(name string) error {
	switch name {
	}
	return fmt.Errorf("unknown Principle unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *PrincipleMutation) ResetEdge(name string) error {
	switch name {
	case principle.EdgeRules:
		m.ResetRules()
		return nil
	case principle.EdgeDefenders:
		m.ResetDefenders()
		return nil
	}
	return fmt.Errorf("unknown Principle edge %s", name)
}

// ReportMutation represents an operation that mutates the Report nodes in the graph.
type ReportMutation struct {
	config
	op                         Op
	typ                        string
	id                         *uuid.UUID
	metas                      *map[string]interface{}
	request_headers            *[]map[string]string
	appendrequest_headers      []map[string]string
	request_body               *map[string]interface{}
	response_headers           *[]map[string]string
	appendresponse_headers     []map[string]string
	response_body              *map[string]interface{}
	rule_details               *map[string]interface{}
	created_at                 *time.Time
	updated_at                 *time.Time
	clearedFields              map[string]struct{}
	triggered_by_action        *uuid.UUID
	clearedtriggered_by_action bool
	created_by_defender        *uuid.UUID
	clearedcreated_by_defender bool
	done                       bool
	oldValue                   func(context.Context) (*Report, error)
	predicates                 []predicate.Report
}

var _ ent.Mutation = (*ReportMutation)(nil)

// reportOption allows management of the mutation configuration using functional options.
type reportOption func(*ReportMutation)

// newReportMutation creates new mutation for the Report entity.
func newReportMutation(c config, op Op, opts ...reportOption) *ReportMutation {
	m := &ReportMutation{
		config:        c,
		op:            op,
		typ:           TypeReport,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withReportID sets the ID field of the mutation.
func withReportID(id uuid.UUID) reportOption {
	return func(m *ReportMutation) {
		var (
			err   error
			once  sync.Once
			value *Report
		)
		m.oldValue = func(ctx context.Context) (*Report, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Report.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withReport sets the old Report of the mutation.
func withReport(node *Report) reportOption {
	return func(m *ReportMutation) {
		m.oldValue = func(context.Context) (*Report, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m ReportMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m ReportMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of Report entities.
func (m *ReportMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *ReportMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *ReportMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Report.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetMetas sets the "metas" field.
func (m *ReportMutation) SetMetas(value map[string]interface{}) {
	m.metas = &value
}

// Metas returns the value of the "metas" field in the mutation.
func (m *ReportMutation) Metas() (r map[string]interface{}, exists bool) {
	v := m.metas
	if v == nil {
		return
	}
	return *v, true
}

// OldMetas returns the old "metas" field's value of the Report entity.
// If the Report object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ReportMutation) OldMetas(ctx context.Context) (v map[string]interface{}, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldMetas is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldMetas requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldMetas: %w", err)
	}
	return oldValue.Metas, nil
}

// ClearMetas clears the value of the "metas" field.
func (m *ReportMutation) ClearMetas() {
	m.metas = nil
	m.clearedFields[report.FieldMetas] = struct{}{}
}

// MetasCleared returns if the "metas" field was cleared in this mutation.
func (m *ReportMutation) MetasCleared() bool {
	_, ok := m.clearedFields[report.FieldMetas]
	return ok
}

// ResetMetas resets all changes to the "metas" field.
func (m *ReportMutation) ResetMetas() {
	m.metas = nil
	delete(m.clearedFields, report.FieldMetas)
}

// SetRequestHeaders sets the "request_headers" field.
func (m *ReportMutation) SetRequestHeaders(value []map[string]string) {
	m.request_headers = &value
	m.appendrequest_headers = nil
}

// RequestHeaders returns the value of the "request_headers" field in the mutation.
func (m *ReportMutation) RequestHeaders() (r []map[string]string, exists bool) {
	v := m.request_headers
	if v == nil {
		return
	}
	return *v, true
}

// OldRequestHeaders returns the old "request_headers" field's value of the Report entity.
// If the Report object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ReportMutation) OldRequestHeaders(ctx context.Context) (v []map[string]string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldRequestHeaders is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldRequestHeaders requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldRequestHeaders: %w", err)
	}
	return oldValue.RequestHeaders, nil
}

// AppendRequestHeaders adds value to the "request_headers" field.
func (m *ReportMutation) AppendRequestHeaders(value []map[string]string) {
	m.appendrequest_headers = append(m.appendrequest_headers, value...)
}

// AppendedRequestHeaders returns the list of values that were appended to the "request_headers" field in this mutation.
func (m *ReportMutation) AppendedRequestHeaders() ([]map[string]string, bool) {
	if len(m.appendrequest_headers) == 0 {
		return nil, false
	}
	return m.appendrequest_headers, true
}

// ClearRequestHeaders clears the value of the "request_headers" field.
func (m *ReportMutation) ClearRequestHeaders() {
	m.request_headers = nil
	m.appendrequest_headers = nil
	m.clearedFields[report.FieldRequestHeaders] = struct{}{}
}

// RequestHeadersCleared returns if the "request_headers" field was cleared in this mutation.
func (m *ReportMutation) RequestHeadersCleared() bool {
	_, ok := m.clearedFields[report.FieldRequestHeaders]
	return ok
}

// ResetRequestHeaders resets all changes to the "request_headers" field.
func (m *ReportMutation) ResetRequestHeaders() {
	m.request_headers = nil
	m.appendrequest_headers = nil
	delete(m.clearedFields, report.FieldRequestHeaders)
}

// SetRequestBody sets the "request_body" field.
func (m *ReportMutation) SetRequestBody(value map[string]interface{}) {
	m.request_body = &value
}

// RequestBody returns the value of the "request_body" field in the mutation.
func (m *ReportMutation) RequestBody() (r map[string]interface{}, exists bool) {
	v := m.request_body
	if v == nil {
		return
	}
	return *v, true
}

// OldRequestBody returns the old "request_body" field's value of the Report entity.
// If the Report object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ReportMutation) OldRequestBody(ctx context.Context) (v map[string]interface{}, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldRequestBody is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldRequestBody requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldRequestBody: %w", err)
	}
	return oldValue.RequestBody, nil
}

// ClearRequestBody clears the value of the "request_body" field.
func (m *ReportMutation) ClearRequestBody() {
	m.request_body = nil
	m.clearedFields[report.FieldRequestBody] = struct{}{}
}

// RequestBodyCleared returns if the "request_body" field was cleared in this mutation.
func (m *ReportMutation) RequestBodyCleared() bool {
	_, ok := m.clearedFields[report.FieldRequestBody]
	return ok
}

// ResetRequestBody resets all changes to the "request_body" field.
func (m *ReportMutation) ResetRequestBody() {
	m.request_body = nil
	delete(m.clearedFields, report.FieldRequestBody)
}

// SetResponseHeaders sets the "response_headers" field.
func (m *ReportMutation) SetResponseHeaders(value []map[string]string) {
	m.response_headers = &value
	m.appendresponse_headers = nil
}

// ResponseHeaders returns the value of the "response_headers" field in the mutation.
func (m *ReportMutation) ResponseHeaders() (r []map[string]string, exists bool) {
	v := m.response_headers
	if v == nil {
		return
	}
	return *v, true
}

// OldResponseHeaders returns the old "response_headers" field's value of the Report entity.
// If the Report object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ReportMutation) OldResponseHeaders(ctx context.Context) (v []map[string]string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldResponseHeaders is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldResponseHeaders requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldResponseHeaders: %w", err)
	}
	return oldValue.ResponseHeaders, nil
}

// AppendResponseHeaders adds value to the "response_headers" field.
func (m *ReportMutation) AppendResponseHeaders(value []map[string]string) {
	m.appendresponse_headers = append(m.appendresponse_headers, value...)
}

// AppendedResponseHeaders returns the list of values that were appended to the "response_headers" field in this mutation.
func (m *ReportMutation) AppendedResponseHeaders() ([]map[string]string, bool) {
	if len(m.appendresponse_headers) == 0 {
		return nil, false
	}
	return m.appendresponse_headers, true
}

// ClearResponseHeaders clears the value of the "response_headers" field.
func (m *ReportMutation) ClearResponseHeaders() {
	m.response_headers = nil
	m.appendresponse_headers = nil
	m.clearedFields[report.FieldResponseHeaders] = struct{}{}
}

// ResponseHeadersCleared returns if the "response_headers" field was cleared in this mutation.
func (m *ReportMutation) ResponseHeadersCleared() bool {
	_, ok := m.clearedFields[report.FieldResponseHeaders]
	return ok
}

// ResetResponseHeaders resets all changes to the "response_headers" field.
func (m *ReportMutation) ResetResponseHeaders() {
	m.response_headers = nil
	m.appendresponse_headers = nil
	delete(m.clearedFields, report.FieldResponseHeaders)
}

// SetResponseBody sets the "response_body" field.
func (m *ReportMutation) SetResponseBody(value map[string]interface{}) {
	m.response_body = &value
}

// ResponseBody returns the value of the "response_body" field in the mutation.
func (m *ReportMutation) ResponseBody() (r map[string]interface{}, exists bool) {
	v := m.response_body
	if v == nil {
		return
	}
	return *v, true
}

// OldResponseBody returns the old "response_body" field's value of the Report entity.
// If the Report object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ReportMutation) OldResponseBody(ctx context.Context) (v map[string]interface{}, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldResponseBody is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldResponseBody requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldResponseBody: %w", err)
	}
	return oldValue.ResponseBody, nil
}

// ClearResponseBody clears the value of the "response_body" field.
func (m *ReportMutation) ClearResponseBody() {
	m.response_body = nil
	m.clearedFields[report.FieldResponseBody] = struct{}{}
}

// ResponseBodyCleared returns if the "response_body" field was cleared in this mutation.
func (m *ReportMutation) ResponseBodyCleared() bool {
	_, ok := m.clearedFields[report.FieldResponseBody]
	return ok
}

// ResetResponseBody resets all changes to the "response_body" field.
func (m *ReportMutation) ResetResponseBody() {
	m.response_body = nil
	delete(m.clearedFields, report.FieldResponseBody)
}

// SetRuleDetails sets the "rule_details" field.
func (m *ReportMutation) SetRuleDetails(value map[string]interface{}) {
	m.rule_details = &value
}

// RuleDetails returns the value of the "rule_details" field in the mutation.
func (m *ReportMutation) RuleDetails() (r map[string]interface{}, exists bool) {
	v := m.rule_details
	if v == nil {
		return
	}
	return *v, true
}

// OldRuleDetails returns the old "rule_details" field's value of the Report entity.
// If the Report object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ReportMutation) OldRuleDetails(ctx context.Context) (v map[string]interface{}, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldRuleDetails is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldRuleDetails requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldRuleDetails: %w", err)
	}
	return oldValue.RuleDetails, nil
}

// ClearRuleDetails clears the value of the "rule_details" field.
func (m *ReportMutation) ClearRuleDetails() {
	m.rule_details = nil
	m.clearedFields[report.FieldRuleDetails] = struct{}{}
}

// RuleDetailsCleared returns if the "rule_details" field was cleared in this mutation.
func (m *ReportMutation) RuleDetailsCleared() bool {
	_, ok := m.clearedFields[report.FieldRuleDetails]
	return ok
}

// ResetRuleDetails resets all changes to the "rule_details" field.
func (m *ReportMutation) ResetRuleDetails() {
	m.rule_details = nil
	delete(m.clearedFields, report.FieldRuleDetails)
}

// SetTriggeredBy sets the "triggered_by" field.
func (m *ReportMutation) SetTriggeredBy(u uuid.UUID) {
	m.triggered_by_action = &u
}

// TriggeredBy returns the value of the "triggered_by" field in the mutation.
func (m *ReportMutation) TriggeredBy() (r uuid.UUID, exists bool) {
	v := m.triggered_by_action
	if v == nil {
		return
	}
	return *v, true
}

// OldTriggeredBy returns the old "triggered_by" field's value of the Report entity.
// If the Report object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ReportMutation) OldTriggeredBy(ctx context.Context) (v *uuid.UUID, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldTriggeredBy is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldTriggeredBy requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldTriggeredBy: %w", err)
	}
	return oldValue.TriggeredBy, nil
}

// ClearTriggeredBy clears the value of the "triggered_by" field.
func (m *ReportMutation) ClearTriggeredBy() {
	m.triggered_by_action = nil
	m.clearedFields[report.FieldTriggeredBy] = struct{}{}
}

// TriggeredByCleared returns if the "triggered_by" field was cleared in this mutation.
func (m *ReportMutation) TriggeredByCleared() bool {
	_, ok := m.clearedFields[report.FieldTriggeredBy]
	return ok
}

// ResetTriggeredBy resets all changes to the "triggered_by" field.
func (m *ReportMutation) ResetTriggeredBy() {
	m.triggered_by_action = nil
	delete(m.clearedFields, report.FieldTriggeredBy)
}

// SetCreatedBy sets the "created_by" field.
func (m *ReportMutation) SetCreatedBy(u uuid.UUID) {
	m.created_by_defender = &u
}

// CreatedBy returns the value of the "created_by" field in the mutation.
func (m *ReportMutation) CreatedBy() (r uuid.UUID, exists bool) {
	v := m.created_by_defender
	if v == nil {
		return
	}
	return *v, true
}

// OldCreatedBy returns the old "created_by" field's value of the Report entity.
// If the Report object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ReportMutation) OldCreatedBy(ctx context.Context) (v *uuid.UUID, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldCreatedBy is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldCreatedBy requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldCreatedBy: %w", err)
	}
	return oldValue.CreatedBy, nil
}

// ClearCreatedBy clears the value of the "created_by" field.
func (m *ReportMutation) ClearCreatedBy() {
	m.created_by_defender = nil
	m.clearedFields[report.FieldCreatedBy] = struct{}{}
}

// CreatedByCleared returns if the "created_by" field was cleared in this mutation.
func (m *ReportMutation) CreatedByCleared() bool {
	_, ok := m.clearedFields[report.FieldCreatedBy]
	return ok
}

// ResetCreatedBy resets all changes to the "created_by" field.
func (m *ReportMutation) ResetCreatedBy() {
	m.created_by_defender = nil
	delete(m.clearedFields, report.FieldCreatedBy)
}

// SetCreatedAt sets the "created_at" field.
func (m *ReportMutation) SetCreatedAt(t time.Time) {
	m.created_at = &t
}

// CreatedAt returns the value of the "created_at" field in the mutation.
func (m *ReportMutation) CreatedAt() (r time.Time, exists bool) {
	v := m.created_at
	if v == nil {
		return
	}
	return *v, true
}

// OldCreatedAt returns the old "created_at" field's value of the Report entity.
// If the Report object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ReportMutation) OldCreatedAt(ctx context.Context) (v time.Time, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldCreatedAt is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldCreatedAt requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldCreatedAt: %w", err)
	}
	return oldValue.CreatedAt, nil
}

// ResetCreatedAt resets all changes to the "created_at" field.
func (m *ReportMutation) ResetCreatedAt() {
	m.created_at = nil
}

// SetUpdatedAt sets the "updated_at" field.
func (m *ReportMutation) SetUpdatedAt(t time.Time) {
	m.updated_at = &t
}

// UpdatedAt returns the value of the "updated_at" field in the mutation.
func (m *ReportMutation) UpdatedAt() (r time.Time, exists bool) {
	v := m.updated_at
	if v == nil {
		return
	}
	return *v, true
}

// OldUpdatedAt returns the old "updated_at" field's value of the Report entity.
// If the Report object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ReportMutation) OldUpdatedAt(ctx context.Context) (v time.Time, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldUpdatedAt is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldUpdatedAt requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldUpdatedAt: %w", err)
	}
	return oldValue.UpdatedAt, nil
}

// ResetUpdatedAt resets all changes to the "updated_at" field.
func (m *ReportMutation) ResetUpdatedAt() {
	m.updated_at = nil
}

// SetTriggeredByActionID sets the "triggered_by_action" edge to the Action entity by id.
func (m *ReportMutation) SetTriggeredByActionID(id uuid.UUID) {
	m.triggered_by_action = &id
}

// ClearTriggeredByAction clears the "triggered_by_action" edge to the Action entity.
func (m *ReportMutation) ClearTriggeredByAction() {
	m.clearedtriggered_by_action = true
	m.clearedFields[report.FieldTriggeredBy] = struct{}{}
}

// TriggeredByActionCleared reports if the "triggered_by_action" edge to the Action entity was cleared.
func (m *ReportMutation) TriggeredByActionCleared() bool {
	return m.TriggeredByCleared() || m.clearedtriggered_by_action
}

// TriggeredByActionID returns the "triggered_by_action" edge ID in the mutation.
func (m *ReportMutation) TriggeredByActionID() (id uuid.UUID, exists bool) {
	if m.triggered_by_action != nil {
		return *m.triggered_by_action, true
	}
	return
}

// TriggeredByActionIDs returns the "triggered_by_action" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// TriggeredByActionID instead. It exists only for internal usage by the builders.
func (m *ReportMutation) TriggeredByActionIDs() (ids []uuid.UUID) {
	if id := m.triggered_by_action; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetTriggeredByAction resets all changes to the "triggered_by_action" edge.
func (m *ReportMutation) ResetTriggeredByAction() {
	m.triggered_by_action = nil
	m.clearedtriggered_by_action = false
}

// SetCreatedByDefenderID sets the "created_by_defender" edge to the Defender entity by id.
func (m *ReportMutation) SetCreatedByDefenderID(id uuid.UUID) {
	m.created_by_defender = &id
}

// ClearCreatedByDefender clears the "created_by_defender" edge to the Defender entity.
func (m *ReportMutation) ClearCreatedByDefender() {
	m.clearedcreated_by_defender = true
	m.clearedFields[report.FieldCreatedBy] = struct{}{}
}

// CreatedByDefenderCleared reports if the "created_by_defender" edge to the Defender entity was cleared.
func (m *ReportMutation) CreatedByDefenderCleared() bool {
	return m.CreatedByCleared() || m.clearedcreated_by_defender
}

// CreatedByDefenderID returns the "created_by_defender" edge ID in the mutation.
func (m *ReportMutation) CreatedByDefenderID() (id uuid.UUID, exists bool) {
	if m.created_by_defender != nil {
		return *m.created_by_defender, true
	}
	return
}

// CreatedByDefenderIDs returns the "created_by_defender" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// CreatedByDefenderID instead. It exists only for internal usage by the builders.
func (m *ReportMutation) CreatedByDefenderIDs() (ids []uuid.UUID) {
	if id := m.created_by_defender; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetCreatedByDefender resets all changes to the "created_by_defender" edge.
func (m *ReportMutation) ResetCreatedByDefender() {
	m.created_by_defender = nil
	m.clearedcreated_by_defender = false
}

// Where appends a list predicates to the ReportMutation builder.
func (m *ReportMutation) Where(ps ...predicate.Report) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the ReportMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *ReportMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Report, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *ReportMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *ReportMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Report).
func (m *ReportMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *ReportMutation) Fields() []string {
	fields := make([]string, 0, 10)
	if m.metas != nil {
		fields = append(fields, report.FieldMetas)
	}
	if m.request_headers != nil {
		fields = append(fields, report.FieldRequestHeaders)
	}
	if m.request_body != nil {
		fields = append(fields, report.FieldRequestBody)
	}
	if m.response_headers != nil {
		fields = append(fields, report.FieldResponseHeaders)
	}
	if m.response_body != nil {
		fields = append(fields, report.FieldResponseBody)
	}
	if m.rule_details != nil {
		fields = append(fields, report.FieldRuleDetails)
	}
	if m.triggered_by_action != nil {
		fields = append(fields, report.FieldTriggeredBy)
	}
	if m.created_by_defender != nil {
		fields = append(fields, report.FieldCreatedBy)
	}
	if m.created_at != nil {
		fields = append(fields, report.FieldCreatedAt)
	}
	if m.updated_at != nil {
		fields = append(fields, report.FieldUpdatedAt)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *ReportMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case report.FieldMetas:
		return m.Metas()
	case report.FieldRequestHeaders:
		return m.RequestHeaders()
	case report.FieldRequestBody:
		return m.RequestBody()
	case report.FieldResponseHeaders:
		return m.ResponseHeaders()
	case report.FieldResponseBody:
		return m.ResponseBody()
	case report.FieldRuleDetails:
		return m.RuleDetails()
	case report.FieldTriggeredBy:
		return m.TriggeredBy()
	case report.FieldCreatedBy:
		return m.CreatedBy()
	case report.FieldCreatedAt:
		return m.CreatedAt()
	case report.FieldUpdatedAt:
		return m.UpdatedAt()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *ReportMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case report.FieldMetas:
		return m.OldMetas(ctx)
	case report.FieldRequestHeaders:
		return m.OldRequestHeaders(ctx)
	case report.FieldRequestBody:
		return m.OldRequestBody(ctx)
	case report.FieldResponseHeaders:
		return m.OldResponseHeaders(ctx)
	case report.FieldResponseBody:
		return m.OldResponseBody(ctx)
	case report.FieldRuleDetails:
		return m.OldRuleDetails(ctx)
	case report.FieldTriggeredBy:
		return m.OldTriggeredBy(ctx)
	case report.FieldCreatedBy:
		return m.OldCreatedBy(ctx)
	case report.FieldCreatedAt:
		return m.OldCreatedAt(ctx)
	case report.FieldUpdatedAt:
		return m.OldUpdatedAt(ctx)
	}
	return nil, fmt.Errorf("unknown Report field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *ReportMutation) SetField(name string, value ent.Value) error {
	switch name {
	case report.FieldMetas:
		v, ok := value.(map[string]interface{})
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetMetas(v)
		return nil
	case report.FieldRequestHeaders:
		v, ok := value.([]map[string]string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetRequestHeaders(v)
		return nil
	case report.FieldRequestBody:
		v, ok := value.(map[string]interface{})
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetRequestBody(v)
		return nil
	case report.FieldResponseHeaders:
		v, ok := value.([]map[string]string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetResponseHeaders(v)
		return nil
	case report.FieldResponseBody:
		v, ok := value.(map[string]interface{})
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetResponseBody(v)
		return nil
	case report.FieldRuleDetails:
		v, ok := value.(map[string]interface{})
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetRuleDetails(v)
		return nil
	case report.FieldTriggeredBy:
		v, ok := value.(uuid.UUID)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetTriggeredBy(v)
		return nil
	case report.FieldCreatedBy:
		v, ok := value.(uuid.UUID)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetCreatedBy(v)
		return nil
	case report.FieldCreatedAt:
		v, ok := value.(time.Time)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetCreatedAt(v)
		return nil
	case report.FieldUpdatedAt:
		v, ok := value.(time.Time)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetUpdatedAt(v)
		return nil
	}
	return fmt.Errorf("unknown Report field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *ReportMutation) AddedFields() []string {
	return nil
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *ReportMutation) AddedField(name string) (ent.Value, bool) {
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *ReportMutation) AddField(name string, value ent.Value) error {
	switch name {
	}
	return fmt.Errorf("unknown Report numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *ReportMutation) ClearedFields() []string {
	var fields []string
	if m.FieldCleared(report.FieldMetas) {
		fields = append(fields, report.FieldMetas)
	}
	if m.FieldCleared(report.FieldRequestHeaders) {
		fields = append(fields, report.FieldRequestHeaders)
	}
	if m.FieldCleared(report.FieldRequestBody) {
		fields = append(fields, report.FieldRequestBody)
	}
	if m.FieldCleared(report.FieldResponseHeaders) {
		fields = append(fields, report.FieldResponseHeaders)
	}
	if m.FieldCleared(report.FieldResponseBody) {
		fields = append(fields, report.FieldResponseBody)
	}
	if m.FieldCleared(report.FieldRuleDetails) {
		fields = append(fields, report.FieldRuleDetails)
	}
	if m.FieldCleared(report.FieldTriggeredBy) {
		fields = append(fields, report.FieldTriggeredBy)
	}
	if m.FieldCleared(report.FieldCreatedBy) {
		fields = append(fields, report.FieldCreatedBy)
	}
	return fields
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *ReportMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *ReportMutation) ClearField(name string) error {
	switch name {
	case report.FieldMetas:
		m.ClearMetas()
		return nil
	case report.FieldRequestHeaders:
		m.ClearRequestHeaders()
		return nil
	case report.FieldRequestBody:
		m.ClearRequestBody()
		return nil
	case report.FieldResponseHeaders:
		m.ClearResponseHeaders()
		return nil
	case report.FieldResponseBody:
		m.ClearResponseBody()
		return nil
	case report.FieldRuleDetails:
		m.ClearRuleDetails()
		return nil
	case report.FieldTriggeredBy:
		m.ClearTriggeredBy()
		return nil
	case report.FieldCreatedBy:
		m.ClearCreatedBy()
		return nil
	}
	return fmt.Errorf("unknown Report nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *ReportMutation) ResetField(name string) error {
	switch name {
	case report.FieldMetas:
		m.ResetMetas()
		return nil
	case report.FieldRequestHeaders:
		m.ResetRequestHeaders()
		return nil
	case report.FieldRequestBody:
		m.ResetRequestBody()
		return nil
	case report.FieldResponseHeaders:
		m.ResetResponseHeaders()
		return nil
	case report.FieldResponseBody:
		m.ResetResponseBody()
		return nil
	case report.FieldRuleDetails:
		m.ResetRuleDetails()
		return nil
	case report.FieldTriggeredBy:
		m.ResetTriggeredBy()
		return nil
	case report.FieldCreatedBy:
		m.ResetCreatedBy()
		return nil
	case report.FieldCreatedAt:
		m.ResetCreatedAt()
		return nil
	case report.FieldUpdatedAt:
		m.ResetUpdatedAt()
		return nil
	}
	return fmt.Errorf("unknown Report field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *ReportMutation) AddedEdges() []string {
	edges := make([]string, 0, 2)
	if m.triggered_by_action != nil {
		edges = append(edges, report.EdgeTriggeredByAction)
	}
	if m.created_by_defender != nil {
		edges = append(edges, report.EdgeCreatedByDefender)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *ReportMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case report.EdgeTriggeredByAction:
		if id := m.triggered_by_action; id != nil {
			return []ent.Value{*id}
		}
	case report.EdgeCreatedByDefender:
		if id := m.created_by_defender; id != nil {
			return []ent.Value{*id}
		}
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *ReportMutation) RemovedEdges() []string {
	edges := make([]string, 0, 2)
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *ReportMutation) RemovedIDs(name string) []ent.Value {
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *ReportMutation) ClearedEdges() []string {
	edges := make([]string, 0, 2)
	if m.clearedtriggered_by_action {
		edges = append(edges, report.EdgeTriggeredByAction)
	}
	if m.clearedcreated_by_defender {
		edges = append(edges, report.EdgeCreatedByDefender)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *ReportMutation) EdgeCleared(name string) bool {
	switch name {
	case report.EdgeTriggeredByAction:
		return m.clearedtriggered_by_action
	case report.EdgeCreatedByDefender:
		return m.clearedcreated_by_defender
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *ReportMutation) ClearEdge(name string) error {
	switch name {
	case report.EdgeTriggeredByAction:
		m.ClearTriggeredByAction()
		return nil
	case report.EdgeCreatedByDefender:
		m.ClearCreatedByDefender()
		return nil
	}
	return fmt.Errorf("unknown Report unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *ReportMutation) ResetEdge(name string) error {
	switch name {
	case report.EdgeTriggeredByAction:
		m.ResetTriggeredByAction()
		return nil
	case report.EdgeCreatedByDefender:
		m.ResetCreatedByDefender()
		return nil
	}
	return fmt.Errorf("unknown Report edge %s", name)
}

// RuleMutation represents an operation that mutates the Rule nodes in the graph.
type RuleMutation struct {
	config
	op                Op
	typ               string
	id                *uuid.UUID
	name              *string
	phase             *int
	addphase          *int
	comparator        *string
	is_inversed       *bool
	configurations    *map[string]interface{}
	clearedFields     map[string]struct{}
	target            *uuid.UUID
	clearedtarget     bool
	wordlist          *uuid.UUID
	clearedwordlist   bool
	actions           map[uuid.UUID]struct{}
	removedactions    map[uuid.UUID]struct{}
	clearedactions    bool
	principles        map[uuid.UUID]struct{}
	removedprinciples map[uuid.UUID]struct{}
	clearedprinciples bool
	done              bool
	oldValue          func(context.Context) (*Rule, error)
	predicates        []predicate.Rule
}

var _ ent.Mutation = (*RuleMutation)(nil)

// ruleOption allows management of the mutation configuration using functional options.
type ruleOption func(*RuleMutation)

// newRuleMutation creates new mutation for the Rule entity.
func newRuleMutation(c config, op Op, opts ...ruleOption) *RuleMutation {
	m := &RuleMutation{
		config:        c,
		op:            op,
		typ:           TypeRule,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withRuleID sets the ID field of the mutation.
func withRuleID(id uuid.UUID) ruleOption {
	return func(m *RuleMutation) {
		var (
			err   error
			once  sync.Once
			value *Rule
		)
		m.oldValue = func(ctx context.Context) (*Rule, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Rule.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withRule sets the old Rule of the mutation.
func withRule(node *Rule) ruleOption {
	return func(m *RuleMutation) {
		m.oldValue = func(context.Context) (*Rule, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m RuleMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m RuleMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of Rule entities.
func (m *RuleMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *RuleMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *RuleMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Rule.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetName sets the "name" field.
func (m *RuleMutation) SetName(s string) {
	m.name = &s
}

// Name returns the value of the "name" field in the mutation.
func (m *RuleMutation) Name() (r string, exists bool) {
	v := m.name
	if v == nil {
		return
	}
	return *v, true
}

// OldName returns the old "name" field's value of the Rule entity.
// If the Rule object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *RuleMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// ResetName resets all changes to the "name" field.
func (m *RuleMutation) ResetName() {
	m.name = nil
}

// SetPhase sets the "phase" field.
func (m *RuleMutation) SetPhase(i int) {
	m.phase = &i
	m.addphase = nil
}

// Phase returns the value of the "phase" field in the mutation.
func (m *RuleMutation) Phase() (r int, exists bool) {
	v := m.phase
	if v == nil {
		return
	}
	return *v, true
}

// OldPhase returns the old "phase" field's value of the Rule entity.
// If the Rule object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *RuleMutation) OldPhase(ctx context.Context) (v int, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldPhase is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldPhase requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldPhase: %w", err)
	}
	return oldValue.Phase, nil
}

// AddPhase adds i to the "phase" field.
func (m *RuleMutation) AddPhase(i int) {
	if m.addphase != nil {
		*m.addphase += i
	} else {
		m.addphase = &i
	}
}

// AddedPhase returns the value that was added to the "phase" field in this mutation.
func (m *RuleMutation) AddedPhase() (r int, exists bool) {
	v := m.addphase
	if v == nil {
		return
	}
	return *v, true
}

// ResetPhase resets all changes to the "phase" field.
func (m *RuleMutation) ResetPhase() {
	m.phase = nil
	m.addphase = nil
}

// SetTargetID sets the "target_id" field.
func (m *RuleMutation) SetTargetID(u uuid.UUID) {
	m.target = &u
}

// TargetID returns the value of the "target_id" field in the mutation.
func (m *RuleMutation) TargetID() (r uuid.UUID, exists bool) {
	v := m.target
	if v == nil {
		return
	}
	return *v, true
}

// OldTargetID returns the old "target_id" field's value of the Rule entity.
// If the Rule object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *RuleMutation) OldTargetID(ctx context.Context) (v *uuid.UUID, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldTargetID is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldTargetID requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldTargetID: %w", err)
	}
	return oldValue.TargetID, nil
}

// ClearTargetID clears the value of the "target_id" field.
func (m *RuleMutation) ClearTargetID() {
	m.target = nil
	m.clearedFields[rule.FieldTargetID] = struct{}{}
}

// TargetIDCleared returns if the "target_id" field was cleared in this mutation.
func (m *RuleMutation) TargetIDCleared() bool {
	_, ok := m.clearedFields[rule.FieldTargetID]
	return ok
}

// ResetTargetID resets all changes to the "target_id" field.
func (m *RuleMutation) ResetTargetID() {
	m.target = nil
	delete(m.clearedFields, rule.FieldTargetID)
}

// SetComparator sets the "comparator" field.
func (m *RuleMutation) SetComparator(s string) {
	m.comparator = &s
}

// Comparator returns the value of the "comparator" field in the mutation.
func (m *RuleMutation) Comparator() (r string, exists bool) {
	v := m.comparator
	if v == nil {
		return
	}
	return *v, true
}

// OldComparator returns the old "comparator" field's value of the Rule entity.
// If the Rule object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *RuleMutation) OldComparator(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldComparator is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldComparator requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldComparator: %w", err)
	}
	return oldValue.Comparator, nil
}

// ResetComparator resets all changes to the "comparator" field.
func (m *RuleMutation) ResetComparator() {
	m.comparator = nil
}

// SetIsInversed sets the "is_inversed" field.
func (m *RuleMutation) SetIsInversed(b bool) {
	m.is_inversed = &b
}

// IsInversed returns the value of the "is_inversed" field in the mutation.
func (m *RuleMutation) IsInversed() (r bool, exists bool) {
	v := m.is_inversed
	if v == nil {
		return
	}
	return *v, true
}

// OldIsInversed returns the old "is_inversed" field's value of the Rule entity.
// If the Rule object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *RuleMutation) OldIsInversed(ctx context.Context) (v bool, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldIsInversed is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldIsInversed requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldIsInversed: %w", err)
	}
	return oldValue.IsInversed, nil
}

// ResetIsInversed resets all changes to the "is_inversed" field.
func (m *RuleMutation) ResetIsInversed() {
	m.is_inversed = nil
}

// SetConfigurations sets the "configurations" field.
func (m *RuleMutation) SetConfigurations(value map[string]interface{}) {
	m.configurations = &value
}

// Configurations returns the value of the "configurations" field in the mutation.
func (m *RuleMutation) Configurations() (r map[string]interface{}, exists bool) {
	v := m.configurations
	if v == nil {
		return
	}
	return *v, true
}

// OldConfigurations returns the old "configurations" field's value of the Rule entity.
// If the Rule object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *RuleMutation) OldConfigurations(ctx context.Context) (v map[string]interface{}, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldConfigurations is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldConfigurations requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldConfigurations: %w", err)
	}
	return oldValue.Configurations, nil
}

// ClearConfigurations clears the value of the "configurations" field.
func (m *RuleMutation) ClearConfigurations() {
	m.configurations = nil
	m.clearedFields[rule.FieldConfigurations] = struct{}{}
}

// ConfigurationsCleared returns if the "configurations" field was cleared in this mutation.
func (m *RuleMutation) ConfigurationsCleared() bool {
	_, ok := m.clearedFields[rule.FieldConfigurations]
	return ok
}

// ResetConfigurations resets all changes to the "configurations" field.
func (m *RuleMutation) ResetConfigurations() {
	m.configurations = nil
	delete(m.clearedFields, rule.FieldConfigurations)
}

// SetWordlistID sets the "wordlist_id" field.
func (m *RuleMutation) SetWordlistID(u uuid.UUID) {
	m.wordlist = &u
}

// WordlistID returns the value of the "wordlist_id" field in the mutation.
func (m *RuleMutation) WordlistID() (r uuid.UUID, exists bool) {
	v := m.wordlist
	if v == nil {
		return
	}
	return *v, true
}

// OldWordlistID returns the old "wordlist_id" field's value of the Rule entity.
// If the Rule object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *RuleMutation) OldWordlistID(ctx context.Context) (v *uuid.UUID, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldWordlistID is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldWordlistID requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldWordlistID: %w", err)
	}
	return oldValue.WordlistID, nil
}

// ClearWordlistID clears the value of the "wordlist_id" field.
func (m *RuleMutation) ClearWordlistID() {
	m.wordlist = nil
	m.clearedFields[rule.FieldWordlistID] = struct{}{}
}

// WordlistIDCleared returns if the "wordlist_id" field was cleared in this mutation.
func (m *RuleMutation) WordlistIDCleared() bool {
	_, ok := m.clearedFields[rule.FieldWordlistID]
	return ok
}

// ResetWordlistID resets all changes to the "wordlist_id" field.
func (m *RuleMutation) ResetWordlistID() {
	m.wordlist = nil
	delete(m.clearedFields, rule.FieldWordlistID)
}

// ClearTarget clears the "target" edge to the Target entity.
func (m *RuleMutation) ClearTarget() {
	m.clearedtarget = true
	m.clearedFields[rule.FieldTargetID] = struct{}{}
}

// TargetCleared reports if the "target" edge to the Target entity was cleared.
func (m *RuleMutation) TargetCleared() bool {
	return m.TargetIDCleared() || m.clearedtarget
}

// TargetIDs returns the "target" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// TargetID instead. It exists only for internal usage by the builders.
func (m *RuleMutation) TargetIDs() (ids []uuid.UUID) {
	if id := m.target; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetTarget resets all changes to the "target" edge.
func (m *RuleMutation) ResetTarget() {
	m.target = nil
	m.clearedtarget = false
}

// ClearWordlist clears the "wordlist" edge to the Wordlist entity.
func (m *RuleMutation) ClearWordlist() {
	m.clearedwordlist = true
	m.clearedFields[rule.FieldWordlistID] = struct{}{}
}

// WordlistCleared reports if the "wordlist" edge to the Wordlist entity was cleared.
func (m *RuleMutation) WordlistCleared() bool {
	return m.WordlistIDCleared() || m.clearedwordlist
}

// WordlistIDs returns the "wordlist" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// WordlistID instead. It exists only for internal usage by the builders.
func (m *RuleMutation) WordlistIDs() (ids []uuid.UUID) {
	if id := m.wordlist; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetWordlist resets all changes to the "wordlist" edge.
func (m *RuleMutation) ResetWordlist() {
	m.wordlist = nil
	m.clearedwordlist = false
}

// AddActionIDs adds the "actions" edge to the Action entity by ids.
func (m *RuleMutation) AddActionIDs(ids ...uuid.UUID) {
	if m.actions == nil {
		m.actions = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.actions[ids[i]] = struct{}{}
	}
}

// ClearActions clears the "actions" edge to the Action entity.
func (m *RuleMutation) ClearActions() {
	m.clearedactions = true
}

// ActionsCleared reports if the "actions" edge to the Action entity was cleared.
func (m *RuleMutation) ActionsCleared() bool {
	return m.clearedactions
}

// RemoveActionIDs removes the "actions" edge to the Action entity by IDs.
func (m *RuleMutation) RemoveActionIDs(ids ...uuid.UUID) {
	if m.removedactions == nil {
		m.removedactions = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.actions, ids[i])
		m.removedactions[ids[i]] = struct{}{}
	}
}

// RemovedActions returns the removed IDs of the "actions" edge to the Action entity.
func (m *RuleMutation) RemovedActionsIDs() (ids []uuid.UUID) {
	for id := range m.removedactions {
		ids = append(ids, id)
	}
	return
}

// ActionsIDs returns the "actions" edge IDs in the mutation.
func (m *RuleMutation) ActionsIDs() (ids []uuid.UUID) {
	for id := range m.actions {
		ids = append(ids, id)
	}
	return
}

// ResetActions resets all changes to the "actions" edge.
func (m *RuleMutation) ResetActions() {
	m.actions = nil
	m.clearedactions = false
	m.removedactions = nil
}

// AddPrincipleIDs adds the "principles" edge to the Principle entity by ids.
func (m *RuleMutation) AddPrincipleIDs(ids ...uuid.UUID) {
	if m.principles == nil {
		m.principles = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.principles[ids[i]] = struct{}{}
	}
}

// ClearPrinciples clears the "principles" edge to the Principle entity.
func (m *RuleMutation) ClearPrinciples() {
	m.clearedprinciples = true
}

// PrinciplesCleared reports if the "principles" edge to the Principle entity was cleared.
func (m *RuleMutation) PrinciplesCleared() bool {
	return m.clearedprinciples
}

// RemovePrincipleIDs removes the "principles" edge to the Principle entity by IDs.
func (m *RuleMutation) RemovePrincipleIDs(ids ...uuid.UUID) {
	if m.removedprinciples == nil {
		m.removedprinciples = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.principles, ids[i])
		m.removedprinciples[ids[i]] = struct{}{}
	}
}

// RemovedPrinciples returns the removed IDs of the "principles" edge to the Principle entity.
func (m *RuleMutation) RemovedPrinciplesIDs() (ids []uuid.UUID) {
	for id := range m.removedprinciples {
		ids = append(ids, id)
	}
	return
}

// PrinciplesIDs returns the "principles" edge IDs in the mutation.
func (m *RuleMutation) PrinciplesIDs() (ids []uuid.UUID) {
	for id := range m.principles {
		ids = append(ids, id)
	}
	return
}

// ResetPrinciples resets all changes to the "principles" edge.
func (m *RuleMutation) ResetPrinciples() {
	m.principles = nil
	m.clearedprinciples = false
	m.removedprinciples = nil
}

// Where appends a list predicates to the RuleMutation builder.
func (m *RuleMutation) Where(ps ...predicate.Rule) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the RuleMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *RuleMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Rule, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *RuleMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *RuleMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Rule).
func (m *RuleMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *RuleMutation) Fields() []string {
	fields := make([]string, 0, 7)
	if m.name != nil {
		fields = append(fields, rule.FieldName)
	}
	if m.phase != nil {
		fields = append(fields, rule.FieldPhase)
	}
	if m.target != nil {
		fields = append(fields, rule.FieldTargetID)
	}
	if m.comparator != nil {
		fields = append(fields, rule.FieldComparator)
	}
	if m.is_inversed != nil {
		fields = append(fields, rule.FieldIsInversed)
	}
	if m.configurations != nil {
		fields = append(fields, rule.FieldConfigurations)
	}
	if m.wordlist != nil {
		fields = append(fields, rule.FieldWordlistID)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *RuleMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case rule.FieldName:
		return m.Name()
	case rule.FieldPhase:
		return m.Phase()
	case rule.FieldTargetID:
		return m.TargetID()
	case rule.FieldComparator:
		return m.Comparator()
	case rule.FieldIsInversed:
		return m.IsInversed()
	case rule.FieldConfigurations:
		return m.Configurations()
	case rule.FieldWordlistID:
		return m.WordlistID()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *RuleMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case rule.FieldName:
		return m.OldName(ctx)
	case rule.FieldPhase:
		return m.OldPhase(ctx)
	case rule.FieldTargetID:
		return m.OldTargetID(ctx)
	case rule.FieldComparator:
		return m.OldComparator(ctx)
	case rule.FieldIsInversed:
		return m.OldIsInversed(ctx)
	case rule.FieldConfigurations:
		return m.OldConfigurations(ctx)
	case rule.FieldWordlistID:
		return m.OldWordlistID(ctx)
	}
	return nil, fmt.Errorf("unknown Rule field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *RuleMutation) SetField(name string, value ent.Value) error {
	switch name {
	case rule.FieldName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetName(v)
		return nil
	case rule.FieldPhase:
		v, ok := value.(int)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetPhase(v)
		return nil
	case rule.FieldTargetID:
		v, ok := value.(uuid.UUID)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetTargetID(v)
		return nil
	case rule.FieldComparator:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetComparator(v)
		return nil
	case rule.FieldIsInversed:
		v, ok := value.(bool)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetIsInversed(v)
		return nil
	case rule.FieldConfigurations:
		v, ok := value.(map[string]interface{})
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetConfigurations(v)
		return nil
	case rule.FieldWordlistID:
		v, ok := value.(uuid.UUID)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetWordlistID(v)
		return nil
	}
	return fmt.Errorf("unknown Rule field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *RuleMutation) AddedFields() []string {
	var fields []string
	if m.addphase != nil {
		fields = append(fields, rule.FieldPhase)
	}
	return fields
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *RuleMutation) AddedField(name string) (ent.Value, bool) {
	switch name {
	case rule.FieldPhase:
		return m.AddedPhase()
	}
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *RuleMutation) AddField(name string, value ent.Value) error {
	switch name {
	case rule.FieldPhase:
		v, ok := value.(int)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.AddPhase(v)
		return nil
	}
	return fmt.Errorf("unknown Rule numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *RuleMutation) ClearedFields() []string {
	var fields []string
	if m.FieldCleared(rule.FieldTargetID) {
		fields = append(fields, rule.FieldTargetID)
	}
	if m.FieldCleared(rule.FieldConfigurations) {
		fields = append(fields, rule.FieldConfigurations)
	}
	if m.FieldCleared(rule.FieldWordlistID) {
		fields = append(fields, rule.FieldWordlistID)
	}
	return fields
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *RuleMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *RuleMutation) ClearField(name string) error {
	switch name {
	case rule.FieldTargetID:
		m.ClearTargetID()
		return nil
	case rule.FieldConfigurations:
		m.ClearConfigurations()
		return nil
	case rule.FieldWordlistID:
		m.ClearWordlistID()
		return nil
	}
	return fmt.Errorf("unknown Rule nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *RuleMutation) ResetField(name string) error {
	switch name {
	case rule.FieldName:
		m.ResetName()
		return nil
	case rule.FieldPhase:
		m.ResetPhase()
		return nil
	case rule.FieldTargetID:
		m.ResetTargetID()
		return nil
	case rule.FieldComparator:
		m.ResetComparator()
		return nil
	case rule.FieldIsInversed:
		m.ResetIsInversed()
		return nil
	case rule.FieldConfigurations:
		m.ResetConfigurations()
		return nil
	case rule.FieldWordlistID:
		m.ResetWordlistID()
		return nil
	}
	return fmt.Errorf("unknown Rule field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *RuleMutation) AddedEdges() []string {
	edges := make([]string, 0, 4)
	if m.target != nil {
		edges = append(edges, rule.EdgeTarget)
	}
	if m.wordlist != nil {
		edges = append(edges, rule.EdgeWordlist)
	}
	if m.actions != nil {
		edges = append(edges, rule.EdgeActions)
	}
	if m.principles != nil {
		edges = append(edges, rule.EdgePrinciples)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *RuleMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case rule.EdgeTarget:
		if id := m.target; id != nil {
			return []ent.Value{*id}
		}
	case rule.EdgeWordlist:
		if id := m.wordlist; id != nil {
			return []ent.Value{*id}
		}
	case rule.EdgeActions:
		ids := make([]ent.Value, 0, len(m.actions))
		for id := range m.actions {
			ids = append(ids, id)
		}
		return ids
	case rule.EdgePrinciples:
		ids := make([]ent.Value, 0, len(m.principles))
		for id := range m.principles {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *RuleMutation) RemovedEdges() []string {
	edges := make([]string, 0, 4)
	if m.removedactions != nil {
		edges = append(edges, rule.EdgeActions)
	}
	if m.removedprinciples != nil {
		edges = append(edges, rule.EdgePrinciples)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *RuleMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case rule.EdgeActions:
		ids := make([]ent.Value, 0, len(m.removedactions))
		for id := range m.removedactions {
			ids = append(ids, id)
		}
		return ids
	case rule.EdgePrinciples:
		ids := make([]ent.Value, 0, len(m.removedprinciples))
		for id := range m.removedprinciples {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *RuleMutation) ClearedEdges() []string {
	edges := make([]string, 0, 4)
	if m.clearedtarget {
		edges = append(edges, rule.EdgeTarget)
	}
	if m.clearedwordlist {
		edges = append(edges, rule.EdgeWordlist)
	}
	if m.clearedactions {
		edges = append(edges, rule.EdgeActions)
	}
	if m.clearedprinciples {
		edges = append(edges, rule.EdgePrinciples)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *RuleMutation) EdgeCleared(name string) bool {
	switch name {
	case rule.EdgeTarget:
		return m.clearedtarget
	case rule.EdgeWordlist:
		return m.clearedwordlist
	case rule.EdgeActions:
		return m.clearedactions
	case rule.EdgePrinciples:
		return m.clearedprinciples
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *RuleMutation) ClearEdge(name string) error {
	switch name {
	case rule.EdgeTarget:
		m.ClearTarget()
		return nil
	case rule.EdgeWordlist:
		m.ClearWordlist()
		return nil
	}
	return fmt.Errorf("unknown Rule unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *RuleMutation) ResetEdge(name string) error {
	switch name {
	case rule.EdgeTarget:
		m.ResetTarget()
		return nil
	case rule.EdgeWordlist:
		m.ResetWordlist()
		return nil
	case rule.EdgeActions:
		m.ResetActions()
		return nil
	case rule.EdgePrinciples:
		m.ResetPrinciples()
		return nil
	}
	return fmt.Errorf("unknown Rule edge %s", name)
}

// TargetMutation represents an operation that mutates the Target nodes in the graph.
type TargetMutation struct {
	config
	op              Op
	typ             string
	id              *uuid.UUID
	name            *string
	phase           *int
	addphase        *int
	_type           *target.Type
	datatype        *target.Datatype
	clearedFields   map[string]struct{}
	pattern         *uuid.UUID
	clearedpattern  bool
	wordlist        *uuid.UUID
	clearedwordlist bool
	engines         map[uuid.UUID]struct{}
	removedengines  map[uuid.UUID]struct{}
	clearedengines  bool
	rules           map[uuid.UUID]struct{}
	removedrules    map[uuid.UUID]struct{}
	clearedrules    bool
	done            bool
	oldValue        func(context.Context) (*Target, error)
	predicates      []predicate.Target
}

var _ ent.Mutation = (*TargetMutation)(nil)

// targetOption allows management of the mutation configuration using functional options.
type targetOption func(*TargetMutation)

// newTargetMutation creates new mutation for the Target entity.
func newTargetMutation(c config, op Op, opts ...targetOption) *TargetMutation {
	m := &TargetMutation{
		config:        c,
		op:            op,
		typ:           TypeTarget,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withTargetID sets the ID field of the mutation.
func withTargetID(id uuid.UUID) targetOption {
	return func(m *TargetMutation) {
		var (
			err   error
			once  sync.Once
			value *Target
		)
		m.oldValue = func(ctx context.Context) (*Target, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Target.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withTarget sets the old Target of the mutation.
func withTarget(node *Target) targetOption {
	return func(m *TargetMutation) {
		m.oldValue = func(context.Context) (*Target, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m TargetMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m TargetMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of Target entities.
func (m *TargetMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *TargetMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *TargetMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Target.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetName sets the "name" field.
func (m *TargetMutation) SetName(s string) {
	m.name = &s
}

// Name returns the value of the "name" field in the mutation.
func (m *TargetMutation) Name() (r string, exists bool) {
	v := m.name
	if v == nil {
		return
	}
	return *v, true
}

// OldName returns the old "name" field's value of the Target entity.
// If the Target object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TargetMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// ResetName resets all changes to the "name" field.
func (m *TargetMutation) ResetName() {
	m.name = nil
}

// SetPhase sets the "phase" field.
func (m *TargetMutation) SetPhase(i int) {
	m.phase = &i
	m.addphase = nil
}

// Phase returns the value of the "phase" field in the mutation.
func (m *TargetMutation) Phase() (r int, exists bool) {
	v := m.phase
	if v == nil {
		return
	}
	return *v, true
}

// OldPhase returns the old "phase" field's value of the Target entity.
// If the Target object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TargetMutation) OldPhase(ctx context.Context) (v int, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldPhase is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldPhase requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldPhase: %w", err)
	}
	return oldValue.Phase, nil
}

// AddPhase adds i to the "phase" field.
func (m *TargetMutation) AddPhase(i int) {
	if m.addphase != nil {
		*m.addphase += i
	} else {
		m.addphase = &i
	}
}

// AddedPhase returns the value that was added to the "phase" field in this mutation.
func (m *TargetMutation) AddedPhase() (r int, exists bool) {
	v := m.addphase
	if v == nil {
		return
	}
	return *v, true
}

// ResetPhase resets all changes to the "phase" field.
func (m *TargetMutation) ResetPhase() {
	m.phase = nil
	m.addphase = nil
}

// SetType sets the "type" field.
func (m *TargetMutation) SetType(t target.Type) {
	m._type = &t
}

// GetType returns the value of the "type" field in the mutation.
func (m *TargetMutation) GetType() (r target.Type, exists bool) {
	v := m._type
	if v == nil {
		return
	}
	return *v, true
}

// OldType returns the old "type" field's value of the Target entity.
// If the Target object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TargetMutation) OldType(ctx context.Context) (v target.Type, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldType is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldType requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldType: %w", err)
	}
	return oldValue.Type, nil
}

// ResetType resets all changes to the "type" field.
func (m *TargetMutation) ResetType() {
	m._type = nil
}

// SetDatatype sets the "datatype" field.
func (m *TargetMutation) SetDatatype(t target.Datatype) {
	m.datatype = &t
}

// Datatype returns the value of the "datatype" field in the mutation.
func (m *TargetMutation) Datatype() (r target.Datatype, exists bool) {
	v := m.datatype
	if v == nil {
		return
	}
	return *v, true
}

// OldDatatype returns the old "datatype" field's value of the Target entity.
// If the Target object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TargetMutation) OldDatatype(ctx context.Context) (v target.Datatype, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldDatatype is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldDatatype requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldDatatype: %w", err)
	}
	return oldValue.Datatype, nil
}

// ResetDatatype resets all changes to the "datatype" field.
func (m *TargetMutation) ResetDatatype() {
	m.datatype = nil
}

// SetPatternID sets the "pattern_id" field.
func (m *TargetMutation) SetPatternID(u uuid.UUID) {
	m.pattern = &u
}

// PatternID returns the value of the "pattern_id" field in the mutation.
func (m *TargetMutation) PatternID() (r uuid.UUID, exists bool) {
	v := m.pattern
	if v == nil {
		return
	}
	return *v, true
}

// OldPatternID returns the old "pattern_id" field's value of the Target entity.
// If the Target object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TargetMutation) OldPatternID(ctx context.Context) (v *uuid.UUID, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldPatternID is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldPatternID requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldPatternID: %w", err)
	}
	return oldValue.PatternID, nil
}

// ClearPatternID clears the value of the "pattern_id" field.
func (m *TargetMutation) ClearPatternID() {
	m.pattern = nil
	m.clearedFields[target.FieldPatternID] = struct{}{}
}

// PatternIDCleared returns if the "pattern_id" field was cleared in this mutation.
func (m *TargetMutation) PatternIDCleared() bool {
	_, ok := m.clearedFields[target.FieldPatternID]
	return ok
}

// ResetPatternID resets all changes to the "pattern_id" field.
func (m *TargetMutation) ResetPatternID() {
	m.pattern = nil
	delete(m.clearedFields, target.FieldPatternID)
}

// SetWordlistID sets the "wordlist_id" field.
func (m *TargetMutation) SetWordlistID(u uuid.UUID) {
	m.wordlist = &u
}

// WordlistID returns the value of the "wordlist_id" field in the mutation.
func (m *TargetMutation) WordlistID() (r uuid.UUID, exists bool) {
	v := m.wordlist
	if v == nil {
		return
	}
	return *v, true
}

// OldWordlistID returns the old "wordlist_id" field's value of the Target entity.
// If the Target object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *TargetMutation) OldWordlistID(ctx context.Context) (v *uuid.UUID, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldWordlistID is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldWordlistID requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldWordlistID: %w", err)
	}
	return oldValue.WordlistID, nil
}

// ClearWordlistID clears the value of the "wordlist_id" field.
func (m *TargetMutation) ClearWordlistID() {
	m.wordlist = nil
	m.clearedFields[target.FieldWordlistID] = struct{}{}
}

// WordlistIDCleared returns if the "wordlist_id" field was cleared in this mutation.
func (m *TargetMutation) WordlistIDCleared() bool {
	_, ok := m.clearedFields[target.FieldWordlistID]
	return ok
}

// ResetWordlistID resets all changes to the "wordlist_id" field.
func (m *TargetMutation) ResetWordlistID() {
	m.wordlist = nil
	delete(m.clearedFields, target.FieldWordlistID)
}

// ClearPattern clears the "pattern" edge to the Pattern entity.
func (m *TargetMutation) ClearPattern() {
	m.clearedpattern = true
	m.clearedFields[target.FieldPatternID] = struct{}{}
}

// PatternCleared reports if the "pattern" edge to the Pattern entity was cleared.
func (m *TargetMutation) PatternCleared() bool {
	return m.PatternIDCleared() || m.clearedpattern
}

// PatternIDs returns the "pattern" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// PatternID instead. It exists only for internal usage by the builders.
func (m *TargetMutation) PatternIDs() (ids []uuid.UUID) {
	if id := m.pattern; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetPattern resets all changes to the "pattern" edge.
func (m *TargetMutation) ResetPattern() {
	m.pattern = nil
	m.clearedpattern = false
}

// ClearWordlist clears the "wordlist" edge to the Wordlist entity.
func (m *TargetMutation) ClearWordlist() {
	m.clearedwordlist = true
	m.clearedFields[target.FieldWordlistID] = struct{}{}
}

// WordlistCleared reports if the "wordlist" edge to the Wordlist entity was cleared.
func (m *TargetMutation) WordlistCleared() bool {
	return m.WordlistIDCleared() || m.clearedwordlist
}

// WordlistIDs returns the "wordlist" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// WordlistID instead. It exists only for internal usage by the builders.
func (m *TargetMutation) WordlistIDs() (ids []uuid.UUID) {
	if id := m.wordlist; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetWordlist resets all changes to the "wordlist" edge.
func (m *TargetMutation) ResetWordlist() {
	m.wordlist = nil
	m.clearedwordlist = false
}

// AddEngineIDs adds the "engines" edge to the Engine entity by ids.
func (m *TargetMutation) AddEngineIDs(ids ...uuid.UUID) {
	if m.engines == nil {
		m.engines = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.engines[ids[i]] = struct{}{}
	}
}

// ClearEngines clears the "engines" edge to the Engine entity.
func (m *TargetMutation) ClearEngines() {
	m.clearedengines = true
}

// EnginesCleared reports if the "engines" edge to the Engine entity was cleared.
func (m *TargetMutation) EnginesCleared() bool {
	return m.clearedengines
}

// RemoveEngineIDs removes the "engines" edge to the Engine entity by IDs.
func (m *TargetMutation) RemoveEngineIDs(ids ...uuid.UUID) {
	if m.removedengines == nil {
		m.removedengines = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.engines, ids[i])
		m.removedengines[ids[i]] = struct{}{}
	}
}

// RemovedEngines returns the removed IDs of the "engines" edge to the Engine entity.
func (m *TargetMutation) RemovedEnginesIDs() (ids []uuid.UUID) {
	for id := range m.removedengines {
		ids = append(ids, id)
	}
	return
}

// EnginesIDs returns the "engines" edge IDs in the mutation.
func (m *TargetMutation) EnginesIDs() (ids []uuid.UUID) {
	for id := range m.engines {
		ids = append(ids, id)
	}
	return
}

// ResetEngines resets all changes to the "engines" edge.
func (m *TargetMutation) ResetEngines() {
	m.engines = nil
	m.clearedengines = false
	m.removedengines = nil
}

// AddRuleIDs adds the "rules" edge to the Rule entity by ids.
func (m *TargetMutation) AddRuleIDs(ids ...uuid.UUID) {
	if m.rules == nil {
		m.rules = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.rules[ids[i]] = struct{}{}
	}
}

// ClearRules clears the "rules" edge to the Rule entity.
func (m *TargetMutation) ClearRules() {
	m.clearedrules = true
}

// RulesCleared reports if the "rules" edge to the Rule entity was cleared.
func (m *TargetMutation) RulesCleared() bool {
	return m.clearedrules
}

// RemoveRuleIDs removes the "rules" edge to the Rule entity by IDs.
func (m *TargetMutation) RemoveRuleIDs(ids ...uuid.UUID) {
	if m.removedrules == nil {
		m.removedrules = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.rules, ids[i])
		m.removedrules[ids[i]] = struct{}{}
	}
}

// RemovedRules returns the removed IDs of the "rules" edge to the Rule entity.
func (m *TargetMutation) RemovedRulesIDs() (ids []uuid.UUID) {
	for id := range m.removedrules {
		ids = append(ids, id)
	}
	return
}

// RulesIDs returns the "rules" edge IDs in the mutation.
func (m *TargetMutation) RulesIDs() (ids []uuid.UUID) {
	for id := range m.rules {
		ids = append(ids, id)
	}
	return
}

// ResetRules resets all changes to the "rules" edge.
func (m *TargetMutation) ResetRules() {
	m.rules = nil
	m.clearedrules = false
	m.removedrules = nil
}

// Where appends a list predicates to the TargetMutation builder.
func (m *TargetMutation) Where(ps ...predicate.Target) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the TargetMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *TargetMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Target, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *TargetMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *TargetMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Target).
func (m *TargetMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *TargetMutation) Fields() []string {
	fields := make([]string, 0, 6)
	if m.name != nil {
		fields = append(fields, target.FieldName)
	}
	if m.phase != nil {
		fields = append(fields, target.FieldPhase)
	}
	if m._type != nil {
		fields = append(fields, target.FieldType)
	}
	if m.datatype != nil {
		fields = append(fields, target.FieldDatatype)
	}
	if m.pattern != nil {
		fields = append(fields, target.FieldPatternID)
	}
	if m.wordlist != nil {
		fields = append(fields, target.FieldWordlistID)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *TargetMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case target.FieldName:
		return m.Name()
	case target.FieldPhase:
		return m.Phase()
	case target.FieldType:
		return m.GetType()
	case target.FieldDatatype:
		return m.Datatype()
	case target.FieldPatternID:
		return m.PatternID()
	case target.FieldWordlistID:
		return m.WordlistID()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *TargetMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case target.FieldName:
		return m.OldName(ctx)
	case target.FieldPhase:
		return m.OldPhase(ctx)
	case target.FieldType:
		return m.OldType(ctx)
	case target.FieldDatatype:
		return m.OldDatatype(ctx)
	case target.FieldPatternID:
		return m.OldPatternID(ctx)
	case target.FieldWordlistID:
		return m.OldWordlistID(ctx)
	}
	return nil, fmt.Errorf("unknown Target field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *TargetMutation) SetField(name string, value ent.Value) error {
	switch name {
	case target.FieldName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetName(v)
		return nil
	case target.FieldPhase:
		v, ok := value.(int)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetPhase(v)
		return nil
	case target.FieldType:
		v, ok := value.(target.Type)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetType(v)
		return nil
	case target.FieldDatatype:
		v, ok := value.(target.Datatype)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetDatatype(v)
		return nil
	case target.FieldPatternID:
		v, ok := value.(uuid.UUID)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetPatternID(v)
		return nil
	case target.FieldWordlistID:
		v, ok := value.(uuid.UUID)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetWordlistID(v)
		return nil
	}
	return fmt.Errorf("unknown Target field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *TargetMutation) AddedFields() []string {
	var fields []string
	if m.addphase != nil {
		fields = append(fields, target.FieldPhase)
	}
	return fields
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *TargetMutation) AddedField(name string) (ent.Value, bool) {
	switch name {
	case target.FieldPhase:
		return m.AddedPhase()
	}
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *TargetMutation) AddField(name string, value ent.Value) error {
	switch name {
	case target.FieldPhase:
		v, ok := value.(int)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.AddPhase(v)
		return nil
	}
	return fmt.Errorf("unknown Target numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *TargetMutation) ClearedFields() []string {
	var fields []string
	if m.FieldCleared(target.FieldPatternID) {
		fields = append(fields, target.FieldPatternID)
	}
	if m.FieldCleared(target.FieldWordlistID) {
		fields = append(fields, target.FieldWordlistID)
	}
	return fields
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *TargetMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *TargetMutation) ClearField(name string) error {
	switch name {
	case target.FieldPatternID:
		m.ClearPatternID()
		return nil
	case target.FieldWordlistID:
		m.ClearWordlistID()
		return nil
	}
	return fmt.Errorf("unknown Target nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *TargetMutation) ResetField(name string) error {
	switch name {
	case target.FieldName:
		m.ResetName()
		return nil
	case target.FieldPhase:
		m.ResetPhase()
		return nil
	case target.FieldType:
		m.ResetType()
		return nil
	case target.FieldDatatype:
		m.ResetDatatype()
		return nil
	case target.FieldPatternID:
		m.ResetPatternID()
		return nil
	case target.FieldWordlistID:
		m.ResetWordlistID()
		return nil
	}
	return fmt.Errorf("unknown Target field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *TargetMutation) AddedEdges() []string {
	edges := make([]string, 0, 4)
	if m.pattern != nil {
		edges = append(edges, target.EdgePattern)
	}
	if m.wordlist != nil {
		edges = append(edges, target.EdgeWordlist)
	}
	if m.engines != nil {
		edges = append(edges, target.EdgeEngines)
	}
	if m.rules != nil {
		edges = append(edges, target.EdgeRules)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *TargetMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case target.EdgePattern:
		if id := m.pattern; id != nil {
			return []ent.Value{*id}
		}
	case target.EdgeWordlist:
		if id := m.wordlist; id != nil {
			return []ent.Value{*id}
		}
	case target.EdgeEngines:
		ids := make([]ent.Value, 0, len(m.engines))
		for id := range m.engines {
			ids = append(ids, id)
		}
		return ids
	case target.EdgeRules:
		ids := make([]ent.Value, 0, len(m.rules))
		for id := range m.rules {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *TargetMutation) RemovedEdges() []string {
	edges := make([]string, 0, 4)
	if m.removedengines != nil {
		edges = append(edges, target.EdgeEngines)
	}
	if m.removedrules != nil {
		edges = append(edges, target.EdgeRules)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *TargetMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case target.EdgeEngines:
		ids := make([]ent.Value, 0, len(m.removedengines))
		for id := range m.removedengines {
			ids = append(ids, id)
		}
		return ids
	case target.EdgeRules:
		ids := make([]ent.Value, 0, len(m.removedrules))
		for id := range m.removedrules {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *TargetMutation) ClearedEdges() []string {
	edges := make([]string, 0, 4)
	if m.clearedpattern {
		edges = append(edges, target.EdgePattern)
	}
	if m.clearedwordlist {
		edges = append(edges, target.EdgeWordlist)
	}
	if m.clearedengines {
		edges = append(edges, target.EdgeEngines)
	}
	if m.clearedrules {
		edges = append(edges, target.EdgeRules)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *TargetMutation) EdgeCleared(name string) bool {
	switch name {
	case target.EdgePattern:
		return m.clearedpattern
	case target.EdgeWordlist:
		return m.clearedwordlist
	case target.EdgeEngines:
		return m.clearedengines
	case target.EdgeRules:
		return m.clearedrules
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *TargetMutation) ClearEdge(name string) error {
	switch name {
	case target.EdgePattern:
		m.ClearPattern()
		return nil
	case target.EdgeWordlist:
		m.ClearWordlist()
		return nil
	}
	return fmt.Errorf("unknown Target unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *TargetMutation) ResetEdge(name string) error {
	switch name {
	case target.EdgePattern:
		m.ResetPattern()
		return nil
	case target.EdgeWordlist:
		m.ResetWordlist()
		return nil
	case target.EdgeEngines:
		m.ResetEngines()
		return nil
	case target.EdgeRules:
		m.ResetRules()
		return nil
	}
	return fmt.Errorf("unknown Target edge %s", name)
}

// UserMutation represents an operation that mutates the User nodes in the graph.
type UserMutation struct {
	config
	op                 Op
	typ                string
	id                 *uuid.UUID
	email              *string
	is_verified        *bool
	is_activated       *bool
	is_root            *bool
	clearedFields      map[string]struct{}
	groups             map[uuid.UUID]struct{}
	removedgroups      map[uuid.UUID]struct{}
	clearedgroups      bool
	permissions        map[uuid.UUID]struct{}
	removedpermissions map[uuid.UUID]struct{}
	clearedpermissions bool
	done               bool
	oldValue           func(context.Context) (*User, error)
	predicates         []predicate.User
}

var _ ent.Mutation = (*UserMutation)(nil)

// userOption allows management of the mutation configuration using functional options.
type userOption func(*UserMutation)

// newUserMutation creates new mutation for the User entity.
func newUserMutation(c config, op Op, opts ...userOption) *UserMutation {
	m := &UserMutation{
		config:        c,
		op:            op,
		typ:           TypeUser,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withUserID sets the ID field of the mutation.
func withUserID(id uuid.UUID) userOption {
	return func(m *UserMutation) {
		var (
			err   error
			once  sync.Once
			value *User
		)
		m.oldValue = func(ctx context.Context) (*User, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().User.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withUser sets the old User of the mutation.
func withUser(node *User) userOption {
	return func(m *UserMutation) {
		m.oldValue = func(context.Context) (*User, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m UserMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m UserMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of User entities.
func (m *UserMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *UserMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *UserMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().User.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetEmail sets the "email" field.
func (m *UserMutation) SetEmail(s string) {
	m.email = &s
}

// Email returns the value of the "email" field in the mutation.
func (m *UserMutation) Email() (r string, exists bool) {
	v := m.email
	if v == nil {
		return
	}
	return *v, true
}

// OldEmail returns the old "email" field's value of the User entity.
// If the User object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *UserMutation) OldEmail(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldEmail is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldEmail requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldEmail: %w", err)
	}
	return oldValue.Email, nil
}

// ResetEmail resets all changes to the "email" field.
func (m *UserMutation) ResetEmail() {
	m.email = nil
}

// SetIsVerified sets the "is_verified" field.
func (m *UserMutation) SetIsVerified(b bool) {
	m.is_verified = &b
}

// IsVerified returns the value of the "is_verified" field in the mutation.
func (m *UserMutation) IsVerified() (r bool, exists bool) {
	v := m.is_verified
	if v == nil {
		return
	}
	return *v, true
}

// OldIsVerified returns the old "is_verified" field's value of the User entity.
// If the User object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *UserMutation) OldIsVerified(ctx context.Context) (v bool, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldIsVerified is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldIsVerified requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldIsVerified: %w", err)
	}
	return oldValue.IsVerified, nil
}

// ResetIsVerified resets all changes to the "is_verified" field.
func (m *UserMutation) ResetIsVerified() {
	m.is_verified = nil
}

// SetIsActivated sets the "is_activated" field.
func (m *UserMutation) SetIsActivated(b bool) {
	m.is_activated = &b
}

// IsActivated returns the value of the "is_activated" field in the mutation.
func (m *UserMutation) IsActivated() (r bool, exists bool) {
	v := m.is_activated
	if v == nil {
		return
	}
	return *v, true
}

// OldIsActivated returns the old "is_activated" field's value of the User entity.
// If the User object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *UserMutation) OldIsActivated(ctx context.Context) (v bool, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldIsActivated is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldIsActivated requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldIsActivated: %w", err)
	}
	return oldValue.IsActivated, nil
}

// ResetIsActivated resets all changes to the "is_activated" field.
func (m *UserMutation) ResetIsActivated() {
	m.is_activated = nil
}

// SetIsRoot sets the "is_root" field.
func (m *UserMutation) SetIsRoot(b bool) {
	m.is_root = &b
}

// IsRoot returns the value of the "is_root" field in the mutation.
func (m *UserMutation) IsRoot() (r bool, exists bool) {
	v := m.is_root
	if v == nil {
		return
	}
	return *v, true
}

// OldIsRoot returns the old "is_root" field's value of the User entity.
// If the User object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *UserMutation) OldIsRoot(ctx context.Context) (v bool, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldIsRoot is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldIsRoot requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldIsRoot: %w", err)
	}
	return oldValue.IsRoot, nil
}

// ResetIsRoot resets all changes to the "is_root" field.
func (m *UserMutation) ResetIsRoot() {
	m.is_root = nil
}

// AddGroupIDs adds the "groups" edge to the Group entity by ids.
func (m *UserMutation) AddGroupIDs(ids ...uuid.UUID) {
	if m.groups == nil {
		m.groups = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.groups[ids[i]] = struct{}{}
	}
}

// ClearGroups clears the "groups" edge to the Group entity.
func (m *UserMutation) ClearGroups() {
	m.clearedgroups = true
}

// GroupsCleared reports if the "groups" edge to the Group entity was cleared.
func (m *UserMutation) GroupsCleared() bool {
	return m.clearedgroups
}

// RemoveGroupIDs removes the "groups" edge to the Group entity by IDs.
func (m *UserMutation) RemoveGroupIDs(ids ...uuid.UUID) {
	if m.removedgroups == nil {
		m.removedgroups = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.groups, ids[i])
		m.removedgroups[ids[i]] = struct{}{}
	}
}

// RemovedGroups returns the removed IDs of the "groups" edge to the Group entity.
func (m *UserMutation) RemovedGroupsIDs() (ids []uuid.UUID) {
	for id := range m.removedgroups {
		ids = append(ids, id)
	}
	return
}

// GroupsIDs returns the "groups" edge IDs in the mutation.
func (m *UserMutation) GroupsIDs() (ids []uuid.UUID) {
	for id := range m.groups {
		ids = append(ids, id)
	}
	return
}

// ResetGroups resets all changes to the "groups" edge.
func (m *UserMutation) ResetGroups() {
	m.groups = nil
	m.clearedgroups = false
	m.removedgroups = nil
}

// AddPermissionIDs adds the "permissions" edge to the Permission entity by ids.
func (m *UserMutation) AddPermissionIDs(ids ...uuid.UUID) {
	if m.permissions == nil {
		m.permissions = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.permissions[ids[i]] = struct{}{}
	}
}

// ClearPermissions clears the "permissions" edge to the Permission entity.
func (m *UserMutation) ClearPermissions() {
	m.clearedpermissions = true
}

// PermissionsCleared reports if the "permissions" edge to the Permission entity was cleared.
func (m *UserMutation) PermissionsCleared() bool {
	return m.clearedpermissions
}

// RemovePermissionIDs removes the "permissions" edge to the Permission entity by IDs.
func (m *UserMutation) RemovePermissionIDs(ids ...uuid.UUID) {
	if m.removedpermissions == nil {
		m.removedpermissions = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.permissions, ids[i])
		m.removedpermissions[ids[i]] = struct{}{}
	}
}

// RemovedPermissions returns the removed IDs of the "permissions" edge to the Permission entity.
func (m *UserMutation) RemovedPermissionsIDs() (ids []uuid.UUID) {
	for id := range m.removedpermissions {
		ids = append(ids, id)
	}
	return
}

// PermissionsIDs returns the "permissions" edge IDs in the mutation.
func (m *UserMutation) PermissionsIDs() (ids []uuid.UUID) {
	for id := range m.permissions {
		ids = append(ids, id)
	}
	return
}

// ResetPermissions resets all changes to the "permissions" edge.
func (m *UserMutation) ResetPermissions() {
	m.permissions = nil
	m.clearedpermissions = false
	m.removedpermissions = nil
}

// Where appends a list predicates to the UserMutation builder.
func (m *UserMutation) Where(ps ...predicate.User) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the UserMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *UserMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.User, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *UserMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *UserMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (User).
func (m *UserMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *UserMutation) Fields() []string {
	fields := make([]string, 0, 4)
	if m.email != nil {
		fields = append(fields, user.FieldEmail)
	}
	if m.is_verified != nil {
		fields = append(fields, user.FieldIsVerified)
	}
	if m.is_activated != nil {
		fields = append(fields, user.FieldIsActivated)
	}
	if m.is_root != nil {
		fields = append(fields, user.FieldIsRoot)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *UserMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case user.FieldEmail:
		return m.Email()
	case user.FieldIsVerified:
		return m.IsVerified()
	case user.FieldIsActivated:
		return m.IsActivated()
	case user.FieldIsRoot:
		return m.IsRoot()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *UserMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case user.FieldEmail:
		return m.OldEmail(ctx)
	case user.FieldIsVerified:
		return m.OldIsVerified(ctx)
	case user.FieldIsActivated:
		return m.OldIsActivated(ctx)
	case user.FieldIsRoot:
		return m.OldIsRoot(ctx)
	}
	return nil, fmt.Errorf("unknown User field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *UserMutation) SetField(name string, value ent.Value) error {
	switch name {
	case user.FieldEmail:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetEmail(v)
		return nil
	case user.FieldIsVerified:
		v, ok := value.(bool)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetIsVerified(v)
		return nil
	case user.FieldIsActivated:
		v, ok := value.(bool)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetIsActivated(v)
		return nil
	case user.FieldIsRoot:
		v, ok := value.(bool)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetIsRoot(v)
		return nil
	}
	return fmt.Errorf("unknown User field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *UserMutation) AddedFields() []string {
	return nil
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *UserMutation) AddedField(name string) (ent.Value, bool) {
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *UserMutation) AddField(name string, value ent.Value) error {
	switch name {
	}
	return fmt.Errorf("unknown User numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *UserMutation) ClearedFields() []string {
	return nil
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *UserMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *UserMutation) ClearField(name string) error {
	return fmt.Errorf("unknown User nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *UserMutation) ResetField(name string) error {
	switch name {
	case user.FieldEmail:
		m.ResetEmail()
		return nil
	case user.FieldIsVerified:
		m.ResetIsVerified()
		return nil
	case user.FieldIsActivated:
		m.ResetIsActivated()
		return nil
	case user.FieldIsRoot:
		m.ResetIsRoot()
		return nil
	}
	return fmt.Errorf("unknown User field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *UserMutation) AddedEdges() []string {
	edges := make([]string, 0, 2)
	if m.groups != nil {
		edges = append(edges, user.EdgeGroups)
	}
	if m.permissions != nil {
		edges = append(edges, user.EdgePermissions)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *UserMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case user.EdgeGroups:
		ids := make([]ent.Value, 0, len(m.groups))
		for id := range m.groups {
			ids = append(ids, id)
		}
		return ids
	case user.EdgePermissions:
		ids := make([]ent.Value, 0, len(m.permissions))
		for id := range m.permissions {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *UserMutation) RemovedEdges() []string {
	edges := make([]string, 0, 2)
	if m.removedgroups != nil {
		edges = append(edges, user.EdgeGroups)
	}
	if m.removedpermissions != nil {
		edges = append(edges, user.EdgePermissions)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *UserMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case user.EdgeGroups:
		ids := make([]ent.Value, 0, len(m.removedgroups))
		for id := range m.removedgroups {
			ids = append(ids, id)
		}
		return ids
	case user.EdgePermissions:
		ids := make([]ent.Value, 0, len(m.removedpermissions))
		for id := range m.removedpermissions {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *UserMutation) ClearedEdges() []string {
	edges := make([]string, 0, 2)
	if m.clearedgroups {
		edges = append(edges, user.EdgeGroups)
	}
	if m.clearedpermissions {
		edges = append(edges, user.EdgePermissions)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *UserMutation) EdgeCleared(name string) bool {
	switch name {
	case user.EdgeGroups:
		return m.clearedgroups
	case user.EdgePermissions:
		return m.clearedpermissions
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *UserMutation) ClearEdge(name string) error {
	switch name {
	}
	return fmt.Errorf("unknown User unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *UserMutation) ResetEdge(name string) error {
	switch name {
	case user.EdgeGroups:
		m.ResetGroups()
		return nil
	case user.EdgePermissions:
		m.ResetPermissions()
		return nil
	}
	return fmt.Errorf("unknown User edge %s", name)
}

// WordlistMutation represents an operation that mutates the Wordlist nodes in the graph.
type WordlistMutation struct {
	config
	op              Op
	typ             string
	id              *uuid.UUID
	name            *string
	_type           *wordlist.Type
	word_file       *string
	word_json       *[]types.WordlistItem
	appendword_json []types.WordlistItem
	word_count      *int
	addword_count   *int
	clearedFields   map[string]struct{}
	targets         map[uuid.UUID]struct{}
	removedtargets  map[uuid.UUID]struct{}
	clearedtargets  bool
	rules           map[uuid.UUID]struct{}
	removedrules    map[uuid.UUID]struct{}
	clearedrules    bool
	done            bool
	oldValue        func(context.Context) (*Wordlist, error)
	predicates      []predicate.Wordlist
}

var _ ent.Mutation = (*WordlistMutation)(nil)

// wordlistOption allows management of the mutation configuration using functional options.
type wordlistOption func(*WordlistMutation)

// newWordlistMutation creates new mutation for the Wordlist entity.
func newWordlistMutation(c config, op Op, opts ...wordlistOption) *WordlistMutation {
	m := &WordlistMutation{
		config:        c,
		op:            op,
		typ:           TypeWordlist,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withWordlistID sets the ID field of the mutation.
func withWordlistID(id uuid.UUID) wordlistOption {
	return func(m *WordlistMutation) {
		var (
			err   error
			once  sync.Once
			value *Wordlist
		)
		m.oldValue = func(ctx context.Context) (*Wordlist, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Wordlist.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withWordlist sets the old Wordlist of the mutation.
func withWordlist(node *Wordlist) wordlistOption {
	return func(m *WordlistMutation) {
		m.oldValue = func(context.Context) (*Wordlist, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m WordlistMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m WordlistMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// SetID sets the value of the id field. Note that this
// operation is only accepted on creation of Wordlist entities.
func (m *WordlistMutation) SetID(id uuid.UUID) {
	m.id = &id
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *WordlistMutation) ID() (id uuid.UUID, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *WordlistMutation) IDs(ctx context.Context) ([]uuid.UUID, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []uuid.UUID{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Wordlist.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetName sets the "name" field.
func (m *WordlistMutation) SetName(s string) {
	m.name = &s
}

// Name returns the value of the "name" field in the mutation.
func (m *WordlistMutation) Name() (r string, exists bool) {
	v := m.name
	if v == nil {
		return
	}
	return *v, true
}

// OldName returns the old "name" field's value of the Wordlist entity.
// If the Wordlist object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *WordlistMutation) OldName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldName: %w", err)
	}
	return oldValue.Name, nil
}

// ResetName resets all changes to the "name" field.
func (m *WordlistMutation) ResetName() {
	m.name = nil
}

// SetType sets the "type" field.
func (m *WordlistMutation) SetType(w wordlist.Type) {
	m._type = &w
}

// GetType returns the value of the "type" field in the mutation.
func (m *WordlistMutation) GetType() (r wordlist.Type, exists bool) {
	v := m._type
	if v == nil {
		return
	}
	return *v, true
}

// OldType returns the old "type" field's value of the Wordlist entity.
// If the Wordlist object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *WordlistMutation) OldType(ctx context.Context) (v wordlist.Type, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldType is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldType requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldType: %w", err)
	}
	return oldValue.Type, nil
}

// ResetType resets all changes to the "type" field.
func (m *WordlistMutation) ResetType() {
	m._type = nil
}

// SetWordFile sets the "word_file" field.
func (m *WordlistMutation) SetWordFile(s string) {
	m.word_file = &s
}

// WordFile returns the value of the "word_file" field in the mutation.
func (m *WordlistMutation) WordFile() (r string, exists bool) {
	v := m.word_file
	if v == nil {
		return
	}
	return *v, true
}

// OldWordFile returns the old "word_file" field's value of the Wordlist entity.
// If the Wordlist object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *WordlistMutation) OldWordFile(ctx context.Context) (v *string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldWordFile is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldWordFile requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldWordFile: %w", err)
	}
	return oldValue.WordFile, nil
}

// ClearWordFile clears the value of the "word_file" field.
func (m *WordlistMutation) ClearWordFile() {
	m.word_file = nil
	m.clearedFields[wordlist.FieldWordFile] = struct{}{}
}

// WordFileCleared returns if the "word_file" field was cleared in this mutation.
func (m *WordlistMutation) WordFileCleared() bool {
	_, ok := m.clearedFields[wordlist.FieldWordFile]
	return ok
}

// ResetWordFile resets all changes to the "word_file" field.
func (m *WordlistMutation) ResetWordFile() {
	m.word_file = nil
	delete(m.clearedFields, wordlist.FieldWordFile)
}

// SetWordJSON sets the "word_json" field.
func (m *WordlistMutation) SetWordJSON(ti []types.WordlistItem) {
	m.word_json = &ti
	m.appendword_json = nil
}

// WordJSON returns the value of the "word_json" field in the mutation.
func (m *WordlistMutation) WordJSON() (r []types.WordlistItem, exists bool) {
	v := m.word_json
	if v == nil {
		return
	}
	return *v, true
}

// OldWordJSON returns the old "word_json" field's value of the Wordlist entity.
// If the Wordlist object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *WordlistMutation) OldWordJSON(ctx context.Context) (v []types.WordlistItem, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldWordJSON is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldWordJSON requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldWordJSON: %w", err)
	}
	return oldValue.WordJSON, nil
}

// AppendWordJSON adds ti to the "word_json" field.
func (m *WordlistMutation) AppendWordJSON(ti []types.WordlistItem) {
	m.appendword_json = append(m.appendword_json, ti...)
}

// AppendedWordJSON returns the list of values that were appended to the "word_json" field in this mutation.
func (m *WordlistMutation) AppendedWordJSON() ([]types.WordlistItem, bool) {
	if len(m.appendword_json) == 0 {
		return nil, false
	}
	return m.appendword_json, true
}

// ClearWordJSON clears the value of the "word_json" field.
func (m *WordlistMutation) ClearWordJSON() {
	m.word_json = nil
	m.appendword_json = nil
	m.clearedFields[wordlist.FieldWordJSON] = struct{}{}
}

// WordJSONCleared returns if the "word_json" field was cleared in this mutation.
func (m *WordlistMutation) WordJSONCleared() bool {
	_, ok := m.clearedFields[wordlist.FieldWordJSON]
	return ok
}

// ResetWordJSON resets all changes to the "word_json" field.
func (m *WordlistMutation) ResetWordJSON() {
	m.word_json = nil
	m.appendword_json = nil
	delete(m.clearedFields, wordlist.FieldWordJSON)
}

// SetWordCount sets the "word_count" field.
func (m *WordlistMutation) SetWordCount(i int) {
	m.word_count = &i
	m.addword_count = nil
}

// WordCount returns the value of the "word_count" field in the mutation.
func (m *WordlistMutation) WordCount() (r int, exists bool) {
	v := m.word_count
	if v == nil {
		return
	}
	return *v, true
}

// OldWordCount returns the old "word_count" field's value of the Wordlist entity.
// If the Wordlist object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *WordlistMutation) OldWordCount(ctx context.Context) (v *int, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldWordCount is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldWordCount requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldWordCount: %w", err)
	}
	return oldValue.WordCount, nil
}

// AddWordCount adds i to the "word_count" field.
func (m *WordlistMutation) AddWordCount(i int) {
	if m.addword_count != nil {
		*m.addword_count += i
	} else {
		m.addword_count = &i
	}
}

// AddedWordCount returns the value that was added to the "word_count" field in this mutation.
func (m *WordlistMutation) AddedWordCount() (r int, exists bool) {
	v := m.addword_count
	if v == nil {
		return
	}
	return *v, true
}

// ClearWordCount clears the value of the "word_count" field.
func (m *WordlistMutation) ClearWordCount() {
	m.word_count = nil
	m.addword_count = nil
	m.clearedFields[wordlist.FieldWordCount] = struct{}{}
}

// WordCountCleared returns if the "word_count" field was cleared in this mutation.
func (m *WordlistMutation) WordCountCleared() bool {
	_, ok := m.clearedFields[wordlist.FieldWordCount]
	return ok
}

// ResetWordCount resets all changes to the "word_count" field.
func (m *WordlistMutation) ResetWordCount() {
	m.word_count = nil
	m.addword_count = nil
	delete(m.clearedFields, wordlist.FieldWordCount)
}

// AddTargetIDs adds the "targets" edge to the Target entity by ids.
func (m *WordlistMutation) AddTargetIDs(ids ...uuid.UUID) {
	if m.targets == nil {
		m.targets = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.targets[ids[i]] = struct{}{}
	}
}

// ClearTargets clears the "targets" edge to the Target entity.
func (m *WordlistMutation) ClearTargets() {
	m.clearedtargets = true
}

// TargetsCleared reports if the "targets" edge to the Target entity was cleared.
func (m *WordlistMutation) TargetsCleared() bool {
	return m.clearedtargets
}

// RemoveTargetIDs removes the "targets" edge to the Target entity by IDs.
func (m *WordlistMutation) RemoveTargetIDs(ids ...uuid.UUID) {
	if m.removedtargets == nil {
		m.removedtargets = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.targets, ids[i])
		m.removedtargets[ids[i]] = struct{}{}
	}
}

// RemovedTargets returns the removed IDs of the "targets" edge to the Target entity.
func (m *WordlistMutation) RemovedTargetsIDs() (ids []uuid.UUID) {
	for id := range m.removedtargets {
		ids = append(ids, id)
	}
	return
}

// TargetsIDs returns the "targets" edge IDs in the mutation.
func (m *WordlistMutation) TargetsIDs() (ids []uuid.UUID) {
	for id := range m.targets {
		ids = append(ids, id)
	}
	return
}

// ResetTargets resets all changes to the "targets" edge.
func (m *WordlistMutation) ResetTargets() {
	m.targets = nil
	m.clearedtargets = false
	m.removedtargets = nil
}

// AddRuleIDs adds the "rules" edge to the Rule entity by ids.
func (m *WordlistMutation) AddRuleIDs(ids ...uuid.UUID) {
	if m.rules == nil {
		m.rules = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		m.rules[ids[i]] = struct{}{}
	}
}

// ClearRules clears the "rules" edge to the Rule entity.
func (m *WordlistMutation) ClearRules() {
	m.clearedrules = true
}

// RulesCleared reports if the "rules" edge to the Rule entity was cleared.
func (m *WordlistMutation) RulesCleared() bool {
	return m.clearedrules
}

// RemoveRuleIDs removes the "rules" edge to the Rule entity by IDs.
func (m *WordlistMutation) RemoveRuleIDs(ids ...uuid.UUID) {
	if m.removedrules == nil {
		m.removedrules = make(map[uuid.UUID]struct{})
	}
	for i := range ids {
		delete(m.rules, ids[i])
		m.removedrules[ids[i]] = struct{}{}
	}
}

// RemovedRules returns the removed IDs of the "rules" edge to the Rule entity.
func (m *WordlistMutation) RemovedRulesIDs() (ids []uuid.UUID) {
	for id := range m.removedrules {
		ids = append(ids, id)
	}
	return
}

// RulesIDs returns the "rules" edge IDs in the mutation.
func (m *WordlistMutation) RulesIDs() (ids []uuid.UUID) {
	for id := range m.rules {
		ids = append(ids, id)
	}
	return
}

// ResetRules resets all changes to the "rules" edge.
func (m *WordlistMutation) ResetRules() {
	m.rules = nil
	m.clearedrules = false
	m.removedrules = nil
}

// Where appends a list predicates to the WordlistMutation builder.
func (m *WordlistMutation) Where(ps ...predicate.Wordlist) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the WordlistMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *WordlistMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Wordlist, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *WordlistMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *WordlistMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Wordlist).
func (m *WordlistMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *WordlistMutation) Fields() []string {
	fields := make([]string, 0, 5)
	if m.name != nil {
		fields = append(fields, wordlist.FieldName)
	}
	if m._type != nil {
		fields = append(fields, wordlist.FieldType)
	}
	if m.word_file != nil {
		fields = append(fields, wordlist.FieldWordFile)
	}
	if m.word_json != nil {
		fields = append(fields, wordlist.FieldWordJSON)
	}
	if m.word_count != nil {
		fields = append(fields, wordlist.FieldWordCount)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *WordlistMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case wordlist.FieldName:
		return m.Name()
	case wordlist.FieldType:
		return m.GetType()
	case wordlist.FieldWordFile:
		return m.WordFile()
	case wordlist.FieldWordJSON:
		return m.WordJSON()
	case wordlist.FieldWordCount:
		return m.WordCount()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *WordlistMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case wordlist.FieldName:
		return m.OldName(ctx)
	case wordlist.FieldType:
		return m.OldType(ctx)
	case wordlist.FieldWordFile:
		return m.OldWordFile(ctx)
	case wordlist.FieldWordJSON:
		return m.OldWordJSON(ctx)
	case wordlist.FieldWordCount:
		return m.OldWordCount(ctx)
	}
	return nil, fmt.Errorf("unknown Wordlist field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *WordlistMutation) SetField(name string, value ent.Value) error {
	switch name {
	case wordlist.FieldName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetName(v)
		return nil
	case wordlist.FieldType:
		v, ok := value.(wordlist.Type)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetType(v)
		return nil
	case wordlist.FieldWordFile:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetWordFile(v)
		return nil
	case wordlist.FieldWordJSON:
		v, ok := value.([]types.WordlistItem)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetWordJSON(v)
		return nil
	case wordlist.FieldWordCount:
		v, ok := value.(int)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetWordCount(v)
		return nil
	}
	return fmt.Errorf("unknown Wordlist field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *WordlistMutation) AddedFields() []string {
	var fields []string
	if m.addword_count != nil {
		fields = append(fields, wordlist.FieldWordCount)
	}
	return fields
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *WordlistMutation) AddedField(name string) (ent.Value, bool) {
	switch name {
	case wordlist.FieldWordCount:
		return m.AddedWordCount()
	}
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *WordlistMutation) AddField(name string, value ent.Value) error {
	switch name {
	case wordlist.FieldWordCount:
		v, ok := value.(int)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.AddWordCount(v)
		return nil
	}
	return fmt.Errorf("unknown Wordlist numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *WordlistMutation) ClearedFields() []string {
	var fields []string
	if m.FieldCleared(wordlist.FieldWordFile) {
		fields = append(fields, wordlist.FieldWordFile)
	}
	if m.FieldCleared(wordlist.FieldWordJSON) {
		fields = append(fields, wordlist.FieldWordJSON)
	}
	if m.FieldCleared(wordlist.FieldWordCount) {
		fields = append(fields, wordlist.FieldWordCount)
	}
	return fields
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *WordlistMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *WordlistMutation) ClearField(name string) error {
	switch name {
	case wordlist.FieldWordFile:
		m.ClearWordFile()
		return nil
	case wordlist.FieldWordJSON:
		m.ClearWordJSON()
		return nil
	case wordlist.FieldWordCount:
		m.ClearWordCount()
		return nil
	}
	return fmt.Errorf("unknown Wordlist nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *WordlistMutation) ResetField(name string) error {
	switch name {
	case wordlist.FieldName:
		m.ResetName()
		return nil
	case wordlist.FieldType:
		m.ResetType()
		return nil
	case wordlist.FieldWordFile:
		m.ResetWordFile()
		return nil
	case wordlist.FieldWordJSON:
		m.ResetWordJSON()
		return nil
	case wordlist.FieldWordCount:
		m.ResetWordCount()
		return nil
	}
	return fmt.Errorf("unknown Wordlist field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *WordlistMutation) AddedEdges() []string {
	edges := make([]string, 0, 2)
	if m.targets != nil {
		edges = append(edges, wordlist.EdgeTargets)
	}
	if m.rules != nil {
		edges = append(edges, wordlist.EdgeRules)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *WordlistMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case wordlist.EdgeTargets:
		ids := make([]ent.Value, 0, len(m.targets))
		for id := range m.targets {
			ids = append(ids, id)
		}
		return ids
	case wordlist.EdgeRules:
		ids := make([]ent.Value, 0, len(m.rules))
		for id := range m.rules {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *WordlistMutation) RemovedEdges() []string {
	edges := make([]string, 0, 2)
	if m.removedtargets != nil {
		edges = append(edges, wordlist.EdgeTargets)
	}
	if m.removedrules != nil {
		edges = append(edges, wordlist.EdgeRules)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *WordlistMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case wordlist.EdgeTargets:
		ids := make([]ent.Value, 0, len(m.removedtargets))
		for id := range m.removedtargets {
			ids = append(ids, id)
		}
		return ids
	case wordlist.EdgeRules:
		ids := make([]ent.Value, 0, len(m.removedrules))
		for id := range m.removedrules {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *WordlistMutation) ClearedEdges() []string {
	edges := make([]string, 0, 2)
	if m.clearedtargets {
		edges = append(edges, wordlist.EdgeTargets)
	}
	if m.clearedrules {
		edges = append(edges, wordlist.EdgeRules)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *WordlistMutation) EdgeCleared(name string) bool {
	switch name {
	case wordlist.EdgeTargets:
		return m.clearedtargets
	case wordlist.EdgeRules:
		return m.clearedrules
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *WordlistMutation) ClearEdge(name string) error {
	switch name {
	}
	return fmt.Errorf("unknown Wordlist unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *WordlistMutation) ResetEdge(name string) error {
	switch name {
	case wordlist.EdgeTargets:
		m.ResetTargets()
		return nil
	case wordlist.EdgeRules:
		m.ResetRules()
		return nil
	}
	return fmt.Errorf("unknown Wordlist edge %s", name)
}
