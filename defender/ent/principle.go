// Code generated by ent, DO NOT EDIT.

package ent

import (
	"defly-defender/ent/principle"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// Principle is the model entity for the Principle schema.
type Principle struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Level holds the value of the "level" field.
	Level uint64 `json:"level,omitempty"`
	// Phase holds the value of the "phase" field.
	Phase int `json:"phase,omitempty"`
	// IsApplied holds the value of the "is_applied" field.
	IsApplied bool `json:"is_applied,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PrincipleQuery when eager-loading is set.
	Edges        PrincipleEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PrincipleEdges holds the relations/edges for other nodes in the graph.
type PrincipleEdges struct {
	// Rules holds the value of the rules edge.
	Rules []*Rule `json:"rules,omitempty"`
	// Defenders holds the value of the defenders edge.
	Defenders []*Defender `json:"defenders,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// RulesOrErr returns the Rules value or an error if the edge
// was not loaded in eager-loading.
func (e PrincipleEdges) RulesOrErr() ([]*Rule, error) {
	if e.loadedTypes[0] {
		return e.Rules, nil
	}
	return nil, &NotLoadedError{edge: "rules"}
}

// DefendersOrErr returns the Defenders value or an error if the edge
// was not loaded in eager-loading.
func (e PrincipleEdges) DefendersOrErr() ([]*Defender, error) {
	if e.loadedTypes[1] {
		return e.Defenders, nil
	}
	return nil, &NotLoadedError{edge: "defenders"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Principle) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case principle.FieldIsApplied:
			values[i] = new(sql.NullBool)
		case principle.FieldLevel, principle.FieldPhase:
			values[i] = new(sql.NullInt64)
		case principle.FieldName:
			values[i] = new(sql.NullString)
		case principle.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Principle fields.
func (pr *Principle) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case principle.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				pr.ID = *value
			}
		case principle.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				pr.Name = value.String
			}
		case principle.FieldLevel:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field level", values[i])
			} else if value.Valid {
				pr.Level = uint64(value.Int64)
			}
		case principle.FieldPhase:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field phase", values[i])
			} else if value.Valid {
				pr.Phase = int(value.Int64)
			}
		case principle.FieldIsApplied:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_applied", values[i])
			} else if value.Valid {
				pr.IsApplied = value.Bool
			}
		default:
			pr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Principle.
// This includes values selected through modifiers, order, etc.
func (pr *Principle) Value(name string) (ent.Value, error) {
	return pr.selectValues.Get(name)
}

// QueryRules queries the "rules" edge of the Principle entity.
func (pr *Principle) QueryRules() *RuleQuery {
	return NewPrincipleClient(pr.config).QueryRules(pr)
}

// QueryDefenders queries the "defenders" edge of the Principle entity.
func (pr *Principle) QueryDefenders() *DefenderQuery {
	return NewPrincipleClient(pr.config).QueryDefenders(pr)
}

// Update returns a builder for updating this Principle.
// Note that you need to call Principle.Unwrap() before calling this method if this Principle
// was returned from a transaction, and the transaction was committed or rolled back.
func (pr *Principle) Update() *PrincipleUpdateOne {
	return NewPrincipleClient(pr.config).UpdateOne(pr)
}

// Unwrap unwraps the Principle entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pr *Principle) Unwrap() *Principle {
	_tx, ok := pr.config.driver.(*txDriver)
	if !ok {
		panic("ent: Principle is not a transactional entity")
	}
	pr.config.driver = _tx.drv
	return pr
}

// String implements the fmt.Stringer.
func (pr *Principle) String() string {
	var builder strings.Builder
	builder.WriteString("Principle(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pr.ID))
	builder.WriteString("name=")
	builder.WriteString(pr.Name)
	builder.WriteString(", ")
	builder.WriteString("level=")
	builder.WriteString(fmt.Sprintf("%v", pr.Level))
	builder.WriteString(", ")
	builder.WriteString("phase=")
	builder.WriteString(fmt.Sprintf("%v", pr.Phase))
	builder.WriteString(", ")
	builder.WriteString("is_applied=")
	builder.WriteString(fmt.Sprintf("%v", pr.IsApplied))
	builder.WriteByte(')')
	return builder.String()
}

// Principles is a parsable slice of Principle.
type Principles []*Principle
