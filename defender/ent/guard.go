// Code generated by ent, DO NOT EDIT.

package ent

import (
	"defly-defender/ent/guard"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// Guard is the model entity for the Guard schema.
type Guard struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// ExpiredAt holds the value of the "expired_at" field.
	ExpiredAt *time.Time `json:"expired_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the GuardQuery when eager-loading is set.
	Edges        GuardEdges `json:"edges"`
	selectValues sql.SelectValues
}

// GuardEdges holds the relations/edges for other nodes in the graph.
type GuardEdges struct {
	// Users holds the value of the users edge.
	Users []*User `json:"users,omitempty"`
	// Defenders holds the value of the defenders edge.
	Defenders []*Defender `json:"defenders,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UsersOrErr returns the Users value or an error if the edge
// was not loaded in eager-loading.
func (e GuardEdges) UsersOrErr() ([]*User, error) {
	if e.loadedTypes[0] {
		return e.Users, nil
	}
	return nil, &NotLoadedError{edge: "users"}
}

// DefendersOrErr returns the Defenders value or an error if the edge
// was not loaded in eager-loading.
func (e GuardEdges) DefendersOrErr() ([]*Defender, error) {
	if e.loadedTypes[1] {
		return e.Defenders, nil
	}
	return nil, &NotLoadedError{edge: "defenders"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Guard) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case guard.FieldName:
			values[i] = new(sql.NullString)
		case guard.FieldExpiredAt:
			values[i] = new(sql.NullTime)
		case guard.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Guard fields.
func (gu *Guard) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case guard.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				gu.ID = *value
			}
		case guard.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				gu.Name = value.String
			}
		case guard.FieldExpiredAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expired_at", values[i])
			} else if value.Valid {
				gu.ExpiredAt = new(time.Time)
				*gu.ExpiredAt = value.Time
			}
		default:
			gu.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Guard.
// This includes values selected through modifiers, order, etc.
func (gu *Guard) Value(name string) (ent.Value, error) {
	return gu.selectValues.Get(name)
}

// QueryUsers queries the "users" edge of the Guard entity.
func (gu *Guard) QueryUsers() *UserQuery {
	return NewGuardClient(gu.config).QueryUsers(gu)
}

// QueryDefenders queries the "defenders" edge of the Guard entity.
func (gu *Guard) QueryDefenders() *DefenderQuery {
	return NewGuardClient(gu.config).QueryDefenders(gu)
}

// Update returns a builder for updating this Guard.
// Note that you need to call Guard.Unwrap() before calling this method if this Guard
// was returned from a transaction, and the transaction was committed or rolled back.
func (gu *Guard) Update() *GuardUpdateOne {
	return NewGuardClient(gu.config).UpdateOne(gu)
}

// Unwrap unwraps the Guard entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (gu *Guard) Unwrap() *Guard {
	_tx, ok := gu.config.driver.(*txDriver)
	if !ok {
		panic("ent: Guard is not a transactional entity")
	}
	gu.config.driver = _tx.drv
	return gu
}

// String implements the fmt.Stringer.
func (gu *Guard) String() string {
	var builder strings.Builder
	builder.WriteString("Guard(")
	builder.WriteString(fmt.Sprintf("id=%v, ", gu.ID))
	builder.WriteString("name=")
	builder.WriteString(gu.Name)
	builder.WriteString(", ")
	if v := gu.ExpiredAt; v != nil {
		builder.WriteString("expired_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// Guards is a parsable slice of Guard.
type Guards []*Guard
