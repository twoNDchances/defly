// Code generated by ent, DO NOT EDIT.

package ent

import (
	"defly-defender/ent/pattern"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// Pattern is the model entity for the Pattern schema.
type Pattern struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Phase holds the value of the "phase" field.
	Phase int `json:"phase,omitempty"`
	// Type holds the value of the "type" field.
	Type pattern.Type `json:"type,omitempty"`
	// Datatype holds the value of the "datatype" field.
	Datatype pattern.Datatype `json:"datatype,omitempty"`
	// Description holds the value of the "description" field.
	Description *string `json:"description,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PatternQuery when eager-loading is set.
	Edges        PatternEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PatternEdges holds the relations/edges for other nodes in the graph.
type PatternEdges struct {
	// Targets holds the value of the targets edge.
	Targets []*Target `json:"targets,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TargetsOrErr returns the Targets value or an error if the edge
// was not loaded in eager-loading.
func (e PatternEdges) TargetsOrErr() ([]*Target, error) {
	if e.loadedTypes[0] {
		return e.Targets, nil
	}
	return nil, &NotLoadedError{edge: "targets"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Pattern) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case pattern.FieldPhase:
			values[i] = new(sql.NullInt64)
		case pattern.FieldName, pattern.FieldType, pattern.FieldDatatype, pattern.FieldDescription:
			values[i] = new(sql.NullString)
		case pattern.FieldCreatedAt, pattern.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case pattern.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Pattern fields.
func (pa *Pattern) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case pattern.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				pa.ID = *value
			}
		case pattern.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				pa.Name = value.String
			}
		case pattern.FieldPhase:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field phase", values[i])
			} else if value.Valid {
				pa.Phase = int(value.Int64)
			}
		case pattern.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				pa.Type = pattern.Type(value.String)
			}
		case pattern.FieldDatatype:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field datatype", values[i])
			} else if value.Valid {
				pa.Datatype = pattern.Datatype(value.String)
			}
		case pattern.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				pa.Description = new(string)
				*pa.Description = value.String
			}
		case pattern.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pa.CreatedAt = value.Time
			}
		case pattern.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				pa.UpdatedAt = value.Time
			}
		default:
			pa.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Pattern.
// This includes values selected through modifiers, order, etc.
func (pa *Pattern) Value(name string) (ent.Value, error) {
	return pa.selectValues.Get(name)
}

// QueryTargets queries the "targets" edge of the Pattern entity.
func (pa *Pattern) QueryTargets() *TargetQuery {
	return NewPatternClient(pa.config).QueryTargets(pa)
}

// Update returns a builder for updating this Pattern.
// Note that you need to call Pattern.Unwrap() before calling this method if this Pattern
// was returned from a transaction, and the transaction was committed or rolled back.
func (pa *Pattern) Update() *PatternUpdateOne {
	return NewPatternClient(pa.config).UpdateOne(pa)
}

// Unwrap unwraps the Pattern entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pa *Pattern) Unwrap() *Pattern {
	_tx, ok := pa.config.driver.(*txDriver)
	if !ok {
		panic("ent: Pattern is not a transactional entity")
	}
	pa.config.driver = _tx.drv
	return pa
}

// String implements the fmt.Stringer.
func (pa *Pattern) String() string {
	var builder strings.Builder
	builder.WriteString("Pattern(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pa.ID))
	builder.WriteString("name=")
	builder.WriteString(pa.Name)
	builder.WriteString(", ")
	builder.WriteString("phase=")
	builder.WriteString(fmt.Sprintf("%v", pa.Phase))
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", pa.Type))
	builder.WriteString(", ")
	builder.WriteString("datatype=")
	builder.WriteString(fmt.Sprintf("%v", pa.Datatype))
	builder.WriteString(", ")
	if v := pa.Description; v != nil {
		builder.WriteString("description=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(pa.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(pa.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Patterns is a parsable slice of Pattern.
type Patterns []*Pattern
